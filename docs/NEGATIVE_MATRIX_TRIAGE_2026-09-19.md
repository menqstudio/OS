# Negative-matrix triage · 2026-09-19

**What this is.** Every one of the 182 `unreviewed` rows of `docs/design/SECURITY_NEGATIVE_TEST_MATRIX.md` read against the tree at `main` @ `a2614064`, by twelve readers (one per family group) whose every row was then handed to a skeptic told to refute it. 172 rows survived the skeptic, 10 were corrected. **Established by reading, not by driving the tree** — every location below is a `file:line` a reader opened and a skeptic re-opened, but no control was mutated and no test was run for this table. Every mark here is ◑, the Builder's own claim, and the readers and skeptics were the Builder's agents.

**What it changes.** The 33 rows in §1 move to `blocked` in `config/negative-matrix.json` with the `blocked_on` a skeptic confirmed names a component that does not exist; they leave `unreviewed_baseline`. Nothing else in the registry moves: §2's 79 rows become `implemented` only when a test names the ID and goes RED under mutation, and §3's 59 rows only when the test is written — both are work queues, not claims.

| | rows |
| :-- | --: |
| §1 blocked, skeptic-confirmed — moved in the registry | 33 |
| §2 already covered by an existing test — needs the ID in the test name and body, and a mutant | 79 (38 Python rows bound by #226 and 36 Rust rows by #228, each mutation-verified; NM-XBIND-13 + 4 shell/other remain) |
| §3 implementable now — control located, test described | 59 (S 42 · M 13 · L 4) |
| §4 refuted by the skeptic — corrected proposal recorded, registry untouched | 10 |
| §5 unsure | 1 |
| unreviewed after this document | 149 |


## §1 Blocked — the code under test does not exist in this tree

| ID | under test | blocked_on (now in the registry) | skeptic |
| :-- | :-- | :-- | :-- |
| `NM-TCB-04` | Lease names wrong image | The governed-turn lease validator this row names does not exist. `validate_governed_turn_lease` has ZERO occurrences in any .py/.rs/.ts/.tsx/.json/.sql under engine/, apps/desktop/src-tauri/, bridge/ or contracts/ (the same absence NM-CAP-01..06 record). MEASURED for this row specifically: the supervisor only ever WRITES the two pins into a lease from its own config (engine/runtime/governed_supervisor.py:657-663) and nothing reads a presented lease's pins back against the start-time pins — `launcher_executable_sha256` is compared to nothing in the tree, and `executor_executable_sha256` is consumed only by the launcher's exec-time image re-hash (apps/desktop/src-tauri/launcher/src/main.rs:112 | Truly absent, blocked_on is concrete — but one factual claim in it should be amended. CONFIRMED: `validate_governed_turn_lease` has 0 code occurrences (only config/negative-matrix.json:1451-1481 prose). Supervisor writes the pins from its own config (governed_supervisor.py:657-663) and serialises 5  |
| `NM-TCB-19` | Image/cgroup swap | The cgroup-mismatch refusal this row requires has no control: the launcher does not validate argv[2] (the cgroup path) against the lease nor place the process into a cgroup — apps/desktop/src-tauri/launcher/src/main.rs:571-576 states "Currently NOT performed", and apps/desktop/src-tauri/core/src/privilege_drop.rs:226-233 omits `Step::CgroupSetup` from `MANDATORY_PERFORMED_STEPS` for exactly that reason, so a mismatched cgroup argument is accepted rather than refused. What must exist first: a lease-authorized cgroup path field, a root-phase `CgroupSetup` step performed inside `perform_drop` (recorded to the `DropJournal`) that refuses on mismatch, and its inclusion in `MANDATORY_PERFORMED_STE | Cgroup limb truly absent and blocked_on is concrete. CONFIRMED: launcher main.rs:571-576 'args[2] is the cgroup path. TODO … validate args[2] against the lease-authorized cgroup path … Currently NOT performed'; :629-633 repeats it at the journal; privilege_drop.rs:225-237 omits `Step::CgroupSetup` f |
| `NM-TCB-23` | Model-identity formula | The lease field and the recompute this row names do not exist. No lease in the tree carries `model_profile_id` — engine/runtime/governed_supervisor.py:766-773 serialises five keys and apps/desktop/src-tauri/launcher/src/main.rs:264-300 parses eight, refusing unknown keys — and `engine/runtime/isolated_signer.py` (the §7 verifier) has ZERO occurrences of `model_profile` or `cfg-sha256`, so there is nothing to recompute against. The formula `"cfg-sha256:" + generation_config_sha256` is implemented once, as the derivation `GovernedGenerationConfig::model_profile_id()` at apps/desktop/src-tauri/core/src/governed_prepare.rs:371-373, and is compared to nothing. What must exist first: a supervisor- | Truly absent; blocked_on is concrete. CONFIRMED: governed_supervisor.py:766-773 `_lease_payload` serialises exactly five keys (no `model_profile_id`, no `generation_config_sha256`); launcher main.rs:264-300 parses exactly eight keys and refuses unknown ones; `grep model_profile\|cfg-sha256 engine/ru |
| `NM-CRASH-15` | mid-registry-acceptance (before COMMIT) | The §7 supervisor-side registry anti-rollback floor does not exist as code. MEASURED 2026-09-19: engine/runtime/supervisor_ledger.sql defines no registry-floor table (its CREATE TABLEs are governed_turn_acceptance/completion/outbox, governed_evidence_head_floor, governed_turn_staging{,_session,_chunk}, governed_output_streams); the challenge-key registry's epoch floor is the CONSTANT `OpenConfig.registry_epoch_floor: int = 0` (engine/runtime/governed_turn_open.py:220) handed to `challenge_key_registry.resolve_registry(epoch_floor=...)` (:349-394) and never persisted or advanced; `grep -rnE 'highest_registry_epoch\|registry_floor\|challenge_registry_floor'` over engine/runtime/*.py and *.sql  | Searched wider than the reader and found nothing: engine/runtime/supervisor_ledger.sql and the Rust twin apps/desktop/src-tauri/core/schema/supervisor_ledger.sql both define exactly governed_turn_acceptance/completion/outbox, governed_evidence_head_floor, governed_turn_staging{,_session,_chunk}, gov |
| `NM-MAN-03` | Manifest expired | The §1.6 signed `issued_at`/`expires_at` manifest fields and an expiry check in the manifest acceptance path. MEASURED: `KeyManifest` (apps/desktop/src-tauri/core/src/key_manifest.rs:41-47) has exactly `manifest_epoch`, `root_key_id`, `keys`; `grep -c expires_at key_manifest.rs` = 0 (issued_at, manifest_protocol, manifest_version also 0), and every `expires_at` under apps/desktop/src-tauri is a lease/challenge/grant field (chain_executor.rs:204, supervisor_ledger.sql:69, agent_bundle.rs:144). What must exist first: the two signed fields on `KeyManifest` plus a `now_ms > expires_at` refusal in `check_and_advance` (key_manifest.rs:289-301) or in the per-turn resolvers (manifest_resolver.rs:181 | `KeyManifest` (key_manifest.rs:41-47) has exactly manifest_epoch/root_key_id/keys; `grep -c expires_at key_manifest.rs` = 0; `issued_at`/`manifest_version`/`manifest_expires` have no manifest hit anywhere under apps/desktop/src-tauri, engine, tools, bridge (every `expires_at*` hit is a lease/challen |
| `NM-MAN-04` | Receipt-key wrong usage | The `key_usage: receipt_signing \| supervisor_attestation` discriminator on `ManifestKey` (§1.6) and a receipt-key resolver that rejects `key_usage != receipt_signing` (§1.7). MEASURED: `key_usage` has zero occurrences in any .rs/.py/.ts/.tsx/.json/.sql/.sh under apps/desktop/src-tauri, apps/desktop/src, engine, bridge, contracts, config and tools; `ManifestKey` (key_manifest.rs:26-37) is key_id/public_key_hex/trust_class/valid_from_ms/valid_to_ms/key_epoch/revoked/allowed_protocols. The signer and the supervisor-attestation key are resolved by the identical `resolve_production_key(.., RECEIPT_ENVELOPE_ARTIFACT_TYPE, ..)` predicate (manifest_resolver.rs:208-211; win-live resolver.rs:243-251; | `key_usage` = 0 hits over apps/desktop/src-tauri, apps/desktop/src, engine, bridge, contracts, config, tools (*.rs *.py *.ts *.tsx *.json *.sql *.sh *.toml); alternate names `KeyUsage`, `AttestationKey`, `resolve_attestation` also 0. `ManifestKey` (key_manifest.rs:26-37) has no usage discriminator.  |
| `NM-MAN-05` | Attestation-key wrong usage | `resolve_attestation_key(supervisor_attestation_key_id, supervisor_id, now_ms)` (§1.7) and the `key_usage` field it would reject on — neither exists. MEASURED: `resolve_attestation_key` and `key_usage` each have zero occurrences in any .rs/.py/.ts/.tsx/.json/.sql/.sh under apps/desktop/src-tauri, apps/desktop/src, engine, bridge, contracts, config, tools. The supervisor-attestation key is resolved by `resolve_production_key(&manifest, &sup_attest_key_id, RECEIPT_ENVELOPE_ARTIFACT_TYPE, now)` (manifest_resolver.rs:210; win-live resolver.rs:245-251; config.rs:238-246), i.e. by exactly the predicate a receipt-signing key satisfies, so a receipt-signing key used for attestation is accepted by de | `resolve_attestation_key` = 0 tree-wide (same seven roots + alt names); `key_usage` = 0. The attestation key is resolved by `resolve_production_key(&p.manifest, &p.sup_attest_key_id, RECEIPT_ENVELOPE_ARTIFACT_TYPE, now)` at manifest_resolver.rs:210-211, win-live resolver.rs:245-251 and config.rs:238 |
| `NM-MAN-07` | Wrong-supervisor attestation key | A `supervisor_id` binding on the manifest key and a resolver that compares it to the turn's supervisor. MEASURED: `grep -c supervisor_id` = 0 in apps/desktop/src-tauri/core/src/key_manifest.rs, manifest_authority.rs, production_trust.rs, broker/src/manifest_resolver.rs and win-live/src/resolver.rs; `ManifestKey` (key_manifest.rs:26-37) has no such field and `resolve_production_key(manifest, key_id, protocol, now_ms)` (key_manifest.rs:241-246) has no supervisor argument; the §1.7 `resolve_attestation_key(key_id, supervisor_id, now_ms)` has zero occurrences tree-wide. A test bound to this row would have to invent the field. | `grep -c supervisor_id` = 0 in key_manifest.rs, manifest_authority.rs, production_trust.rs, broker manifest_resolver.rs, win-live resolver.rs (confirmed); `bound_supervisor\|supervisor_binding` = 0; `ManifestKey` (key_manifest.rs:26-37) has no supervisor field; `resolve_production_key(manifest, key_ |
| `NM-MAN-09` | Snapshot/floor mismatch on resolve | The §1.7 two-phase durable snapshot: a manifest row + `(highest_epoch, manifest_hash)` floor in the desktop SQLite written in one BEGIN IMMEDIATE, and a per-turn `resolve(q, tx)` that re-reads both inside the verify→consume→persist transaction and returns `Unavailable` on mismatch. MEASURED: no manifest or manifest-floor table exists in apps/desktop/src-tauri/core/schema (`grep -rn manifest schema/*.sql` → a comment in 0021 and the agent-bundle digest in 0025 only); the floor is `floor.json` (main.rs:359-367; win_live_turn.rs:183) advanced under a `Mutex` in `resolve_keys` (manifest_resolver.rs:200-205) / `resolve` (win-live resolver.rs:232-238) BEFORE the chain runs, and the transaction mod | `grep -rn -i manifest apps/desktop/src-tauri/core/schema/` → only comments (0016:16, 0021:21) and the agent-bundle digest (0025:20, agent-bundle-manifest.schema.json) — no manifest or manifest-floor table. Floor is a JSON file loaded at start (main.rs:359-367 `parse_floor_json`; win_live_turn.rs:183 |
| `NM-MAN-10` | Wildcard scope | `workspace`/`allowed_audiences` scope fields on `ManifestKey` and a semantic-validation pass that rejects `"*"`. MEASURED: `ManifestKey` (apps/desktop/src-tauri/core/src/key_manifest.rs:26-37) carries no scope field (`grep -c workspace` = 0, `grep -c '"*"'` = 0; the single `audience` hit is the :35 doc comment on `allowed_protocols`); `allowed_audiences` and `KeyResolutionQuery` have zero occurrences tree-wide; there is no `fn validate` in key_manifest.rs. Workspace/install binding happens on the turn (governed_verification.rs:931-934), never on the key, so there is no wildcard for a validator to refuse. | key_manifest.rs counts confirmed: workspace 0, `"*"` 0, `fn validate` 0, audience 1 (the :35 doc comment on `allowed_protocols`); `allowed_audiences` and `KeyResolutionQuery` = 0 tree-wide; `validate_manifest\|semantic(ally) valid` = 0 in *.rs. `resolve_production_key` (key_manifest.rs:247-259) chec |
| `NM-MAN-11` | Duplicate/conflicting key_id | A manifest semantic-validation pass (a `KeyManifest::validate`-style function) that rejects duplicate/conflicting `key_id`. MEASURED: `fn validate`, `duplicate`, `HashSet`, `dedup` each have 0 occurrences in apps/desktop/src-tauri/core/src/key_manifest.rs, and `duplicate\|dedup\|HashSet` over manifest_authority.rs, production_trust.rs, broker manifest_resolver.rs, win-live resolver.rs/config.rs and preflight.rs yields only two unrelated comments (preflight.rs:1081, :1271). `resolve_production_key` selects by first match (`.iter().find(\|k\| k.key_id == key_id)`, key_manifest.rs:247), so a second conflicting entry is never examined, and the plain serde derive (key_manifest.rs:25-26) accepts r | Confirmed: `fn validate`, duplicate, HashSet, dedup each 0 in key_manifest.rs; `duplicate\|dedup\|HashSet` over manifest_authority.rs, production_trust.rs, manifest_resolver.rs, resolver.rs, config.rs, preflight.rs → only preflight.rs:1081 (comment) and :1271 (requirement-name test). `resolve_produc |
| `NM-MAN-13` | Attestation key carrying render scope | The `key_usage` discriminator on `ManifestKey` and a semantic-validation pass that refuses render-scope fields on a `supervisor_attestation` key. MEASURED: `key_usage` has zero occurrences tree-wide (apps/desktop/src-tauri, apps/desktop/src, engine, bridge, contracts, config, tools); `ManifestKey` (key_manifest.rs:26-37) makes `trust_class` and `allowed_protocols` REQUIRED on every key, and the supervisor-attestation key is provisioned with both (config.rs:388-394; manifest_resolver.rs:328-330) and REFUSED without them (`a_revoked_or_development_class_key_is_refused_even_if_the_hex_matches`, config.rs:478-501; resolver.rs:240-251). The tree's rule is the inverse of this row; nothing can go r | `key_usage` = 0 tree-wide, so no key is typed supervisor_attestation; `ManifestKey` (key_manifest.rs:26-37) makes `trust_class` and `allowed_protocols` required on every key; the attestation key is provisioned with both (config.rs:388-394; manifest_resolver.rs:328-330) and REFUSED without them — con |
| `NM-FRAME-06` | Non-canonical JCS (registry) | A registry-bytes canonicality gate — the artifact-#2 analogue of `governed_turn_open._canonicality_gate` — does not exist. `challenge_key_registry.resolve_registry` (:355) takes an already-parsed `Mapping` (`validate_registry_document` :305 refuses anything else; raw bytes → `registry_unknown`, measured) and RECOMPUTES `document_bytes = canonical_bytes({payload, root_sig})` at :397, so `registry_handle` is derived from the re-encoding and the transported bytes are never compared to it; the production loader `run_ladder_supervisor.load_tcb_json` :637-639 is `json.load(fh)` with no byte check, and no Rust code parses `brops.challenge-key-registry.v1` at all. MEASURED 2026-09-19: an `indent=2`  | Control is genuinely absent; I searched wider and re-measured. `grep -rn 'challenge-key-registry' --include=*.rs apps/desktop/src-tauri` -> 0; `grep -rn registry apps/desktop/src-tauri/win-live/src/*.rs` -> 0; no `resolve_registry_bytes`/`strict_loads` use in challenge_key_registry.py (strict_loads  |
| `NM-FRAME-08` | Oversize sign-result | The §4.5 `brops.governed-sign-result.v1` frame and its 64 KiB reader bound. governed_turn_result.py:97-99 marks the frame NOT IMPLEMENTED (no producer, consumer or schema) and config/spec-conformance.json §4.5 records the same as `partial`. The reply that exists today is the frozen `brops.sign-result.v1` op reply; its readers are bounded at 262144 (`brops_protocol.MAX_FRAME_BYTES`, reached from `brops_socket.request` :135 — the supervisor's path), 524288 (`isolated_signer_server.MAX_FRAME_BYTES` :64) and 8192 (`ipc_framing::MAX_FRAME_PAYLOAD_BYTES`, broker `recv_all`/`read_one_reply` chain_executor.rs:668, :686) — no 65536 sign-result bound exists (`grep -rn 65536` hits only containment-evid | CONFIRMED absent. governed_turn_result.py:97-99: '§4.5's own frame — brops.governed-sign-result.v1 — is NOT IMPLEMENTED: no producer, no consumer, no schema file'; config/spec-conformance.json sections/§4.5 status=partial, missing 'The brops.governed-sign-result.v1 FRAME itself...'; engine/contracts |
| `NM-FRAME-10` | Oversize diagnostic | The §4.10(h) `bridge.governed-turn-diagnostic.v1` carrier and its `MAX_DIAGNOSTIC_BYTES = 256` bound. Zero occurrences of `MAX_DIAGNOSTIC_BYTES` in engine/, bridge/, apps/desktop/; the carrier is marked NOT IMPLEMENTED at governed_bridge_result.rs:917, governed_submit.rs:181, bridge/governed_turn_submit.py:285 and bridge/engine_sidecar.py:772-775 (no producer in the sidecar, no classifier in the broker). `governed_turn_execute` (src/governed_turn.rs:214) is a thin broker proxy with no diagnostic arm. What must exist first: the diagnostic producer in `engine_sidecar` and the desktop classifier with a `len(bytes) > MAX_DIAGNOSTIC_BYTES ⇒ terminal Block` arm; until then every diagnostic of any  | CONFIRMED absent. `grep -rn 'MAX_DIAGNOSTIC_BYTES\|DIAGNOSTIC_BYTES'` over engine, bridge, src-tauri/{core,broker,src,win-live,win-broker}, apps/desktop/src -> 0. `governed-turn-diagnostic` appears in exactly 6 files: governed_bridge_result.rs:906/:921 (test fixtures), governed_submit.rs:181 ('NOT I |
| `NM-FRAME-14` | Bad b64url sig | An 86-char base64url-no-pad shape check in `isolated_signer._validate_attestation` (:429 accepts any `_capped_str`) returning `REASON_MALFORMED`, and the §4.5 `brops.governed-sign-result.v1` frame (NOT IMPLEMENTED, governed_turn_result.py:97-99). MEASURED 2026-09-19: `validate_sign_request` accepts `sig` of 85 and 87 chars, `=`-padded 86, and `"not base64!!"`; the only refusal comes downstream from the injected `verify_attestation` seam as `attestation_invalid` (test_isolated_signer.py:396). This is a gap in a shipped, audited perimeter: closing it changes the signer's contract and every fixture in its suite (`sig: "GOOD-SIG"`), an Architect-facing change rather than a sweep test. The 86-cha | RE-MEASURED 2026-09-19: `isolated_signer.validate_sign_request` (via test_negative_matrix._sign_request) ACCEPTED sig = 'not base64!!', 'A'*85, 'A'*87, '='*86 -- no refusal at the decoder. Code: isolated_signer.py:418-435 `_validate_attestation` checks `sig` only with `_capped_str` (:272 `isinstance |
| `NM-FRAME-15` | Wrong domain tag | The §4.4 `brops.governed-sign-request.v1` protocol literal and a governed decoder distinct from `isolated_signer.validate_sign_request`. The tree defines exactly one tag, `SIGN_REQUEST_PROTOCOL = "brops.sign-request.v1"` (isolated_signer.py:58), one protocol check (:484), and the governed path itself builds and sends that base tag (governed_acceptance.py:461; broker chain_executor.rs:989; win-live/servers.rs:26). `grep -rn governed-sign-request` over all .py/.rs/.ts/.json → 0; config/spec-conformance.json §4.4 = unreviewed. Until the governed tag exists there is no 'wrong domain' to present and no decoder to refuse it; a test would have to invent the literal (the NM-IPC-05 precedent). | CONFIRMED: `grep -rn governed-sign-request --include=*.py,*.rs,*.ts,*.json` -> only a doc-comment at governed_verification.rs:304; engine/contracts has brops-sign-request.v1.schema.json but NO brops-governed-sign-request.v1.schema.json, which the addendum :1060 and §4.4 :1517-1524 define. isolated_s |
| `NM-FRAME-20` | Unknown stage/reason | The §4.10(h) diagnostic decoder on the desktop with its closed `stage` and `upstream_reason` sets. The carrier `bridge.governed-turn-diagnostic.v1` has no producer and no classifier (NOT IMPLEMENTED at governed_bridge_result.rs:917, governed_submit.rs:181, bridge/governed_turn_submit.py:285, bridge/engine_sidecar.py:772-775); `grep -rn upstream_reason apps/desktop/src-tauri/core/src` hits only a test fixture (:923); `DIAGNOSTIC_STAGES` (bridge/governed_turn_submit.py:306) is a published tuple that no code path checks membership against (`UpstreamRefusal.__init__` :297 stores `stage` unchecked). Until the decoder exists, an unknown stage/reason and a known one are indistinguishable on the des | CONFIRMED absent on the desktop: `grep -rn 'upstream_reason\|DIAGNOSTIC_STAGES\|KNOWN_STAGES' core/src broker/src src` -> only the test fixture governed_bridge_result.rs:923; no decoder reads `stage`/`upstream_reason`. bridge/governed_turn_submit.py:306-312 defines `DIAGNOSTIC_STAGES` (5 members, la |
| `NM-ORACLE-11` | Governed-turn-recorder oracle | The `governed-turn-recorder` authority does not exist as code. Addendum §8 (line 3502-3515) requires it as a `bro_signature.AUTHORITY_TYPES` entry + `ARTIFACT_AUTHORITY['brops.governed-turn-record.v1']` mapping + a `broctl sign --artifact brops.governed-turn-record.v1` key class held under the supervisor principal. MEASURED 2026-09-19: (G1) `grep -rn 'governed-turn-recorder\|governed_turn_recorder'` over engine/ apps/ bridge/ contracts/ config/ tools/ code files -> 0 hits; (G2) `grep -n governed engine/runtime/bro_signature.py` -> 0 (AUTHORITY_TYPES :223-224 = operator-root, issuer, evidence-recorder, builder, verifier, release, recovery, audit-anchor, control-room, evidence-floor); (G3) `gr | CONFIRMED absent after wider search. `grep -rn 'governed-turn-recorder\|governed_turn_recorder'` over *.py *.rs *.ts *.tsx *.json *.sql *.sh *.toml in the whole tree -> hits ONLY in docs/design (WAVE_3B1B addendum :185/:226/:241/:1384/:1808/:1843/:3120/:3194/:3508/:3629/:3927/:3964, WAVE_3B1_EXECUTI |
| `NM-XBIND-01` | Forged/edited record | The signed terminal record and its authority do not exist as code. `governed-turn-recorder` has ZERO occurrences outside docs/ (grep over engine/, apps/desktop/src-tauri/, apps/desktop/src/, bridge/, config/, contracts/); `bro_signature.ARTIFACT_AUTHORITY` (engine/runtime/bro_signature.py:236) carries no `brops.governed-turn-record.v1`; `governed_supervisor.build_terminal_record` (:706) emits unsigned canonical JSON; `isolated_signer.py` never calls `verify_artifact`. The content-address + agreement substitute (`_verify_chain_handles` :771-812) does NOT satisfy "any field": MEASURED by driving the real IsolatedSigner (scratchpad/xbind/measure.py) -- a record with `completed_at_ms=999` presen | CONFIRMED absent. `grep -rIln governed-turn-recorder --exclude-dir=docs .` = 0 files. bro_signature.py ARTIFACT_AUTHORITY (:236-290) has no governed-turn-record entry; AUTHORITY_TYPES (:223) has no recorder-of-records authority; verify_artifact :981 raises `unknown artifact type` for anything not in |
| `NM-XBIND-02` | Wrong record signer | Same missing component as NM-XBIND-01, wrong-authority limb: there is no `governed-turn-recorder` authority (`bro_signature.AUTHORITY_TYPES` :223 lists OPERATOR, ISSUER, EVIDENCE, BUILDER, VERIFIER, RELEASE, RECOVERY, ...; zero occurrences of the string anywhere under engine/, apps/desktop/src-tauri/, bridge/, config/, contracts/), no `brops.governed-turn-record.v1` entry in `ARTIFACT_AUTHORITY` (:236), and the record is not signed by ANY key (`build_terminal_record` governed_supervisor.py:706-736 returns canonical bytes only), so "signed by the evidence-recorder instead" cannot even be constructed. `verify_artifact` :998-1000 already refuses a key whose `allowed_artifact_types` excludes the | CONFIRMED. bro_signature.py:171 `EVIDENCE = "evidence-recorder"`; :223-224 `AUTHORITY_TYPES = {OPERATOR, ISSUER, EVIDENCE, BUILDER, VERIFIER, RELEASE, RECOVERY, AUDIT_ANCHOR, CONTROL_ROOM, EVIDENCE_FLOOR}` (no governed-turn-recorder); :998-1000 `if artifact_type not in key.allowed_artifact_types: ra |
| `NM-XBIND-06` | `challenge_accepted_at_ms` chain | The byte-equality CHAIN this row names is one link long in this tree. MEASURED (measure.py): a record whose `challenge_accepted_at_ms` (123456) differs from the attested evidence's (NOW-3000), presented under its own handle -> `signed` -- the signer's record agreement set (`_CHAIN_AGREEMENT["record_handle"]`, isolated_signer.py:753-760) omits the field although `build_terminal_record` writes it (governed_supervisor.py:729). The lease document (`_lease_payload`, governed_supervisor.py:764-771) does not carry `challenge_accepted_at_ms`/`lease_issued_at_ms` at all, and the §4.6 bridge frame echoes no `challenge_accepted_at_ms` (governed_bridge_result.rs:380-385), so on those two links there is  | CONFIRMED. governed_verification.rs:540-548 int_pairs includes `(evidence.i("challenge_accepted_at_ms"), envelope.challenge_accepted_at_ms)` -> UpstreamBlocked; test :1616 `an_attestation_that_disagrees_with_the_envelope_about_one_fact_is_refused` case :1632 covers it (attestation->envelope link onl |
| `NM-TIME-13` | Complete time-chain in-window (verify) | The §7 lease-time invariant `completed_at_ms ≤ lease_expires_at_ms` is not implemented anywhere on the verify path. MEASURED 2026-09-19 against the real ledger (in-memory SQLite via engine/tests/test_negative_matrix.py fixtures): after `_executing()`, `record_completion(conn, 'att-1', _produced(completed_at_ms=lease_expires_at_ms + 1), 40, derived=_derived())` returned `created`, the row moved to `COMPLETED`, and `load_attestation_state` returned terminal state with completed_at_ms=1210001 against lease_expires_at_ms=1210000 — the §4.9 attestation input IS built for a completion past lease expiry; the matrix requires `lease_expired` and no envelope. What must exist first: a check that reads  | Searched wider and found no `completed_at_ms <= lease_expires_at_ms` check anywhere: signer EVIDENCE_TS_FIELDS = (requested_at, completed_at, challenge_accepted_at_ms) at isolated_signer.py:128 and `_check_timestamps` :717-731 has only the ordering limbs plus a future-skew bound (:729-730); ledger ` |
| `NM-TIME-14` | Time-chain ordering | The row's fault cannot be injected: `finished_at_ms` does not exist anywhere in the tree (zero source occurrences), and `started_at_ms` exists only as win-live's `execution_started_at_ms`, which the Windows twin compares to `completed_at_ms` in `admit_completion` (servers.rs:610-627) — a different inequality on a different field, already tested at servers.rs:2015. `build_execution_receipt` (engine/runtime/governed_supervisor.py:742-761) emits no started/finished fields at all. What must exist first: the §4.7 `started_at_ms`/`finished_at_ms` fields on the execution receipt (recorder-stamped, `started ≤ finished`) and a §7 chain-inequality check over the full four-term chain; until then the on | Confirmed: `grep -rn "finished_at_ms\|started_at_ms" --include=*.py,*.rs,*.ts,*.tsx,*.json,*.sql,*.toml .` (excluding docs) -> `finished_at_ms` zero hits; `started_at_ms` only as win-live `execution_started_at_ms` (servers.rs:540,612,624,858,946,1044,1070). `build_execution_receipt` (governed_superv |
| `NM-TIME-18` | Wall-clock NTP step (in-exec) | The §7 lease-time invariant `completed_at_ms ≤ lease_expires_at_ms` is not implemented anywhere on the verify path (identical root to NM-TIME-13 — the row differs only in HOW the stamp got past expiry, which the tree cannot distinguish because nothing reads the lease at completion). MEASURED 2026-09-19: `record_completion(completed_at_ms = lease_expires_at_ms + 1)` returned `created`, state `COMPLETED`, and `load_attestation_state` returned terminal state (completed_at_ms 1210001 vs lease_expires_at_ms 1210000). The signer's only related limb (isolated_signer.py:729-730) is a future-skew bound against its own clock, not the lease. What must exist first: a `completed_at_ms ≤ lease_expires_at_ | Same missing control as NM-TIME-13, independently confirmed: the signer's only clock-relative limb is `completed_at > now + max_future_skew_ms` (isolated_signer.py:729-730); nothing on the completion path reads-and-compares the acceptance row's `lease_expires_at_ms` (record_completion 866-912, compl |
| `NM-ACL-10` | Mode-regression guard | The `2750` store mode and the `S_IWGRP` refusal for store dirs do not exist as code. MEASURED 2026-09-19: `_harden_dir(allow_group=True)` (engine/runtime/brops_evidence_store.py:114-165, via `posix_forbidden_mode` :86-93) forbids only `S_IRWXO`; `EvidenceStore.__init__` (:239) uses that default, and engine/tests/test_brops_isolation.py:97-103 (`test_store_allows_a_group_shared_but_not_world_dir`) asserts that a `0o770` store MUST be admitted — the inverse of this row. `grep -rnE '0o2750\|\b2750\b' engine apps/desktop/src-tauri/*/src` hits only floor_writer.py:814 (`SOCKET_DIR_MODE`, the Floor Writer socket dir) and run_live_turn.sh:247 prose ("`sup/`+`rec/` namespaces at `2750`, which in tur | Control genuinely absent, searched wider: `grep -rnE 'S_IWGRP\|0o2750\|\b2750\b\|harden_dir\|harden_store'` over engine/runtime, engine/tools, engine/ci, core/broker/win-live/src, src-tauri/src, bridge, tools -> S_IWGRP refusals only in bro_completion.py:570, bro_signature.py:486/:697, floor_writer. |
| `NM-ACL-13` | Login/sidecar → evidence-head floor DB | A `brops-signer`-owned evidence-head floor DB does not exist. MEASURED 2026-09-19: `grep -nE 'evidence_head_floor\|open_ledger\|sqlite3\|LiveRunStateProvider' engine/runtime/isolated_signer.py` -> the only hit is a comment at :130 — the isolated signer opens no floor. The floor this tree has is (a) the SUPERVISOR ledger table `governed_evidence_head_floor` (engine/runtime/supervisor_ledger.sql:168-177), written by `governed_supervisor_ledger._evidence_floor_cas` (:774) inside the supervisor's own `open_ledger` file (dir `0700`/file `0600`, :194-200) and its Rust twin `supervisor_ledger.rs::evidence_floor_cas` (:900); and (b) the Floor Writer service's `floor-state.json` (engine/runtime/floor | Searched wider for a brops-signer-owned floor and found none: `grep -nE 'evidence_head_floor\|open_ledger\|sqlite3\|LiveRunStateProvider\|floor' engine/runtime/isolated_signer.py` -> only the comment at :130. `LiveRunStateProvider` DOES exist in the tree — engine/tools/brops_live_runstate.py:51, use |
| `NM-SCOPE-05` | Audience out-of-scope | The signed `allowed_audiences` / install scope does not exist as code. MEASURED 2026-09-19: `grep -rn allowed_audiences` over apps/desktop/src-tauri, apps/desktop/src, engine, bridge, config, contracts (*.rs, *.py, *.ts, *.json) returns no source hit; the only two design-doc occurrences are prose (WAVE_3_RECEIPT_PROTOCOL_V1_DESIGN.md:195, WAVE_3B_ISOLATED_SIGNER_DESIGN.md:189/537). `ManifestKey` (core/src/key_manifest.rs:26-37) carries `allowed_protocols` and no install/audience field; `resolve_production_key` (key_manifest.rs:241-261) has no install parameter; `ManifestReceiptKeyAuthority::resolve` (core/src/manifest_authority.rs:88-101) is keyed on `key_id` alone; the Python `challenge_key | Searched wider than the reader and found nothing: `grep -rn allowed_audiences` over *.rs *.py *.ts *.tsx *.json *.toml *.sh *.md in the whole tree = only docs (SECURITY_NEGATIVE_TEST_MATRIX.md:195, WAVE_3B_ISOLATED_SIGNER_DESIGN.md:189/537, WAVE_3_RECEIPT_PROTOCOL_V1_DESIGN.md:195) and the mirror js |
| `NM-FS-01` | Store symlink swap | An `O_NOFOLLOW` open in the protected content-addressed store does not exist. MEASURED 2026-09-19: `grep -n NOFOLLOW engine/runtime/brops_evidence_store.py` = 0 hits; `EvidenceStore.read` (engine/runtime/brops_evidence_store.py:323-332) is `path.read_bytes()` and `has` (320-321) is `.exists()`, both of which follow a symlink; the only O_NOFOLLOW in engine/runtime is floor_writer.py:385/482 (a different store). What must exist first: `EvidenceStore.read`/`has` opening `root/<handle>` via `os.open(path, os.O_RDONLY \| os.O_NOFOLLOW)` + `os.fstat` with an `S_ISREG` check (the §11 preamble's "O_NOFOLLOW opens, fstat the opened fd (never a path re-lookup)"), refusing a symlinked handle as an open | CONFIRMED absent in the store this row names: `grep -n NOFOLLOW engine/runtime/brops_evidence_store.py` = 0; `EvidenceStore.read` :323-332 is `path.read_bytes()` and `has` :320-321 is `.exists()` (both follow symlinks); `EvidenceStore` is the only store-handle read seam in engine/runtime (only `def  |
| `NM-OUTPUT-07` | Output timeout | No producer of `output_timeout` and no pull-loop deadline exist. MEASURED: `output_timeout` occurs only as vocabulary (engine/runtime/governed_turn_result.py:232; apps/desktop/src-tauri/core/src/governed_bridge_result.rs:272) and engine/tests/test_governed_acceptance.py:1641-1644 pins it among the reasons with no producer; `EXECUTION_TIMEOUT_MS` is defined nowhere in .py/.rs (only prose at bridge/governed_turn_submit.py:105,240); the broker waits on the §4.7 recorder with an unbounded `child.wait()` (apps/desktop/src-tauri/broker/src/chain_executor.rs:877) and neither executor/src/main.rs nor launcher/src/main.rs measures elapsed time; the §4.10(f) pull loop `ai::governed_pull_output` (apps/ | Control truly absent; re-measured wider than the reader. `output_timeout` in non-doc files: only governed_bridge_result.rs:272 (vocabulary), governed_turn_result.py:224/232 (union), test_governed_acceptance.py:1625/1643 (pinned as producer-less). `EXECUTION_TIMEOUT` appears only in prose: bridge/gov |
| `NM-REG-02` | Same-epoch / different-hash | The §7 durable registry floor tuple does not exist as code. The only floor is a bare integer, `OpenConfig.registry_epoch_floor: int = 0` (engine/runtime/governed_turn_open.py:220), handed to `challenge_key_registry.resolve_registry` (governed_turn_open.py:459, governed_acceptance.py:717) whose single floor comparison is `payload['registry_epoch'] < epoch_floor` (challenge_key_registry.py:394) — no hash, no handle. MEASURED 2026-09-19 against the real module: a document with `registry_epoch == epoch_floor` (7 == 7) resolves to a snapshot with reason None whatever its content, so a same-epoch/different-hash registry (and any handle) is admitted at the floor. `grep -rn highest_registry_epoch` o | Absence confirmed by wider search. `grep -rn highest_registry_epoch engine apps/desktop/src-tauri --include=*.py,*.rs,*.sql,*.ts` (non-target) → 0; the literal exists only in docs (SECURITY_NEGATIVE_TEST_MATRIX.md:150, WAVE_3B1B_EXECUTION_BINDING_ADDENDUM.md:3450). The only registry floor in the tre |
| `NM-REG-03` | Divergent handle at floor | The §7 durable registry floor tuple does not exist as code. The only floor is a bare integer, `OpenConfig.registry_epoch_floor: int = 0` (engine/runtime/governed_turn_open.py:220), handed to `challenge_key_registry.resolve_registry` (governed_turn_open.py:459, governed_acceptance.py:717) whose single floor comparison is `payload['registry_epoch'] < epoch_floor` (challenge_key_registry.py:394) — no hash, no handle. `registry_handle` is computed at :408 and compared to nothing. MEASURED 2026-09-19 against the real module: a document with `registry_epoch == epoch_floor` resolves to a snapshot with reason None whatever its content, so a divergent handle at the floor is admitted. `grep -rn highes | Same absence basis as NM-REG-02, independently confirmed: `RegistrySnapshot.registry_handle` is computed at challenge_key_registry.py:408 as SHA256 of the canonical `{payload, root_sig}` and is used only as acceptance-row provenance (governed_acceptance.py:411-412, supervisor_ledger.sql:55, governed |
| `NM-TERM-06` | Internal-refusal single sink | The §4.10(h) internal-refusal sink does not exist as code. `governed_internal_refusal` and `bridge.governed-turn-diagnostic.v1` have zero non-prose source occurrences: `grep -rn --include=*.py --include=*.rs --include=*.ts -E 'governed-turn-diagnostic\|governed_internal_refusal'` over engine/runtime, engine/tools, apps/desktop/src-tauri (non-target), apps/desktop/src and bridge hits only comments/docstrings — engine/runtime/governed_evidence_request.py:143-145, apps/desktop/src-tauri/core/src/governed_submit.rs:181-183 ('the `bridge.governed-turn-diagnostic.v1` carrier ... which this tree does NOT build'), core/src/governed_bridge_result.rs:249-250, bridge/engine_sidecar.py:771-775 ('That fr | Absence confirmed by a wider grep than the reader's: `grep -rn -i -E 'internal_refusal\|turn-diagnostic\|turn_diagnostic'` over the WHOLE checkout (*.rs, *.py, *.ts, *.tsx, *.json, *.ps1; excluding target/node_modules/__pycache__ and test files) hits only prose and test fixtures: governed_bridge_res |

## §2 Already covered — an existing test exercises the refusal; it needs the ID in its name and body, and a mutant

| ID | under test | existing test | control |
| :-- | :-- | :-- | :-- |
| `NM-TCB-05` | Shared UID | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/windows_broker.rs::tests::nm_tcb_05_distinct_principals_blocks_any_two_sharing_a_sid` | apps/desktop/src-tauri/core/src/windows_broker.rs:225-245 `verify_distinct_principals` — limb (c) :238-244 pairwise loop over `RUNTIME_PRINCIPALS` (:37-40, exactly seven) → `Err(WindowsBrokerViolation::PrincipalCollision(a,b))`. CAVEAT (measured): this function has no production caller — the only no |
| `NM-TCB-06` | Sidecar == login UID | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/windows_broker.rs::tests::nm_tcb_06_distinct_principals_blocks_any_principal_collapsed_onto_login` | apps/desktop/src-tauri/core/src/windows_broker.rs:229-236 limb (b): for each runtime principal `if *s == sids.login { return Err(PrincipalIsLoginSid(p)) }`. Same no-production-caller caveat as NM-TCB-05 (re-export only at win-broker/src/lib.rs:20). Linux: no code compares the sidecar uid to the logi |
| `NM-TCB-07` | Unset principal UID | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/windows_broker.rs::tests::nm_tcb_07_distinct_principals_blocks_an_unset_sid` | apps/desktop/src-tauri/core/src/windows_broker.rs:229-233 limb (a): `if s.is_empty() { return Err(WindowsBrokerViolation::PrincipalUnset(p)) }` in fixed RUNTIME_PRINCIPALS order. Same no-production-caller caveat (win-broker/src/lib.rs:20 re-export only). Linux analog: run_ladder_turn.sh:95-98 `id -u |
| `NM-TCB-09` | Broker == authority | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/windows_broker.rs::tests::nm_tcb_09_distinct_principals_blocks_broker_equal_to_authority` | apps/desktop/src-tauri/core/src/windows_broker.rs:238-244 pairwise loop → `PrincipalCollision(Principal::Broker, Principal::Authority)` (Broker/Authority are positions 0/1 of RUNTIME_PRINCIPALS :37-40). Same no-production-caller caveat. Linux: run_ladder_turn.sh:100-106 covers it as one of the seven |
| `NM-TCB-15` | Data FD CLOEXEC | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/fd_lifecycle.rs::tests::nm_tcb_15_rejects_cloexec_on_a_data_fd` | apps/desktop/src-tauri/core/src/fd_lifecycle.rs:183-185 (StoreInput 3/4/5: `if f.cloexec { return Err(DataFdCloexec(fd)) }`) and :194-196 (OutputPipe 6); the launcher derives `cloexec` from `/proc/self/fdinfo` flags in `parse_fdinfo` main.rs:145-160 (O_CLOEXEC bit :141) and feeds `verify_launcher_fd |
| `NM-TCB-16` | Wrong target UID/GID | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/privilege_drop.rs::tests::nm_tcb_16_rejects_residual_uid_gid_groups_caps_nnp` | apps/desktop/src-tauri/core/src/privilege_drop.rs:309-339 `verify_final_state`: `PrivilegedDropTarget` if exec uid/gid is 0 (:314-316), `UidNotExecutor` unless ruid==euid==suid==exec_uid (:317-319), `GidNotExecutor` unless all three gids match (:320-322); launcher calls it at :651 after the real dro |
| `NM-TCB-17` | Residual privilege | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/privilege_drop.rs::tests::nm_tcb_17_rejects_residual_groups_caps_nnp` | apps/desktop/src-tauri/core/src/privilege_drop.rs:323-331: `SupplementaryGroupsNonEmpty` (:323-325), `ResidualCapabilities` when `!s.caps.all_empty()` (:326-328; `all_empty` :274-282 checks all five sets), `NoNewPrivsUnset` (:329-331); launcher reads the real sets from /proc/self/status via `parse_s |
| `NM-TCB-18` | Failed drop primitive | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/privilege_drop.rs::tests::nm_tcb_18_a_failed_syscall_stops_the_drop_and_leaves_a_partial_journal` | apps/desktop/src-tauri/core/src/privilege_drop.rs:186-226 `perform_drop`: each seam call `if !sys.X() { return Err(Step::…) }` before its journal record, so a failing syscall stops the drop with only completed steps journaled; the launcher maps it at main.rs:1048-1052 to `Refusal::Syscall(step.name( |
| `NM-TCB-20` | Launcher-drop success | engine/ci/live/run_live_turn.sh:536-547 "POSITIVE CONTROL: the honest, policy-pinned vector still records a run" (recorder exit 0 + evidence file + `.out` file present else RED) and the terminal gate :638-653 (`bound=true` ⇒ "LIVE GOVERNED TURN: GREEN", else exit 1); the F-08 negative at :470-482 is only meaningful because this positive passes. No unit test can run it (real setuid/fexecve). | Recorder apps/desktop/src-tauri/proof/src/bin/governed_recorder.rs:743-790 (dup2 onto 0-6, clear FD_CLOEXEC on 0..=6, close_range(7,…), execve(launcher, fixed argv, empty env)); launcher apps/desktop/src-tauri/launcher/src/main.rs:563-689 (fd gate, drop via journal, final-state verify, image re-hash |
| `NM-TCB-22` | Config not in allowlist | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_tcb_22_model_profile_unknown_blocks_the_row_and_issues_no_lease` | engine/runtime/governed_acceptance.py:640-650: after the acceptance CAS and before `_publish_lease`, `if row["generation_config_handle"] not in self.config.execution_allowlist: self._advance(row, ledger.BLOCKED, "model_profile_unknown"); raise _Refuse("model_profile_unknown", …)`; `AcceptanceConfig. |
| `NM-MAN-01` | Manifest rollback | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/key_manifest.rs::tests::nm_man_01_anti_rollback_accepts_higher_refuses_lower_and_same_epoch_fork` | apps/desktop/src-tauri/core/src/key_manifest.rs:294-296 (`check_and_advance`: `manifest_epoch < floor.highest_epoch` → `RollbackError::EpochBelowFloor`), durable form key_manifest.rs:357-365 (`check_and_persist`); per-turn callers broker/src/manifest_resolver.rs:200-205 (→ `TurnReason::UpstreamBlock |
| `NM-MAN-02` | Manifest same-epoch/diff-hash | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/key_manifest.rs::tests::nm_man_02_same_epoch_different_hash_is_refused` | apps/desktop/src-tauri/core/src/key_manifest.rs:297-299 (`manifest_epoch == floor.highest_epoch && new_hash != floor.highest_hash` → `RollbackError::SameEpochDifferentHash`), reached per turn through `check_and_persist` (key_manifest.rs:361) from manifest_resolver.rs:202-203 and `check_and_advance`  |
| `NM-MAN-08` | Revoked/out-of-window attestation key | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/win-live/src/config.rs::tests::nm_man_08_a_revoked_or_development_class_key_is_refused_even_if_the_hex_matches` | apps/desktop/src-tauri/core/src/key_manifest.rs:251-253 (`if k.revoked` → `ManifestError::Revoked`) and :254-256 (`now_ms < valid_from_ms \|\| now_ms >= valid_to_ms` → `OutOfWindow`), applied to the attestation key id per turn at broker/src/manifest_resolver.rs:210-211 (→ `TurnReason::UpstreamBlocke |
| `NM-MAN-14` | Non-production trust_class render | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/production_trust.rs::tests::nm_man_14_every_resolution_failure_fails_closed_to_no_trusted_manifest` | apps/desktop/src-tauri/core/src/key_manifest.rs:248-250 (`k.trust_class != TrustClass::Production` → `ManifestError::NotProduction`); core/src/production_trust.rs:204 (`Err(ManifestError::NotProduction) => TrustState::NoTrustedManifest("signing key is not production class")`), :46-48 (`is_production |
| `NM-FRAME-05` | Non-canonical JCS (challenge) | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_turn_open.py::RefusalsAreReachableTests::test_nm_frame_05_noncanonical_on_bytes_that_are_not_the_canonical_encoding` | engine/runtime/governed_turn_open.py:366-387 `_canonicality_gate` — raises `_Refuse(REFUSE_NONCANONICAL, ...)` at :378 (transported bytes != governed canonical encoding of {payload, sig}) and :384 (document canonical under only one of the two encoders); `REFUSE_NONCANONICAL = "noncanonical"` at :145 |
| `NM-FRAME-09` | Oversize authority frame | **bound 2026-09-19 (#226)** → `engine/tests/test_challenge_authority_server.py::FrameBoundTests::test_nm_frame_09_oversize_frame_refused` | engine/runtime/challenge_authority_server.py:50 `MAX_FRAME_BYTES = 8192`; `read_frame` :143-146 raises `FrameError("frame length %d exceeds bound %d")` on the declared length BEFORE reading the body; `write_frame` :157-158 refuses an over-bound reply; `handle_connection` :256-265 turns any `FrameErr |
| `NM-FRAME-11` | Oversize output pull | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_output_pull.rs::tests::nm_frame_11_a_signed_but_absurd_output_length_is_refused_before_any_allocation` | apps/desktop/src-tauri/core/src/governed_output_pull.rs:178 `MAX_OUTPUT_BYTES = 8_388_608`; `expected_chunk_count` :384-386 refuses `output_bytes > MAX_OUTPUT_BYTES` ⇒ `PullError::OutputTooLarge` before any buffer is reserved; per-chunk `ChunkOversize` :456 and the derived `MAX_OUTPUT_CHUNKS = 46` : |
| `NM-FRAME-16` | Diagnostic masquerade | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_bridge_result.rs::tests::nm_frame_16_a_document_under_any_other_protocol_const_is_rejected` | apps/desktop/src-tauri/core/src/governed_bridge_result.rs:522-535 `parse_frame` — exact five-key set (:528-530, `FRAME_FIELDS` :226) and `protocol == BRIDGE_TURN_RESULT_PROTOCOL` (:177, :531-533) before any arm is read; every other document is `FrameError::MalformedFrame` → `TurnReason::UpstreamBloc |
| `NM-FRAME-17` | Frozen-schema collision | **bound 2026-09-19 (#226)** → `bridge/tests/test_governed_turn_result_bridge.py::DisjointnessTests::test_nm_frame_17_the_discrimination_is_not_via_envelope_jcs_b64` | Half 1 — bridge/contracts/bridge-result.schema.json:6 `"additionalProperties": false`, :7 `required: [ok, result, receipt, error]`, no `protocol` property, so any document carrying `protocol` (a diagnostic included) is invalid against the frozen schema. Half 2 — apps/desktop/src-tauri/core/src/gover |
| `NM-ORACLE-01` | Supervisor evidence endpoint | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_supervisor_server.py::AttestRunIsNotAnOracleTests::test_nm_oracle_01_the_old_protocol_facts_object_is_a_HARD_ERROR` | engine/runtime/governed_supervisor_server.py:905 `_require_exact_fields(request, OP_ATTEST_RUN, ("run_id", "execution_attempt_id"))` inside `_op_attest_run` (896-928); the helper at :432-447 raises `ServerError("%s has unexpected field(s) %s")` on any extra key; dispatch routes `op == "attest-run"`  |
| `NM-ORACLE-02` | Fabricated run | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_supervisor_server.py::AttestRunIsNotAnOracleTests::test_nm_oracle_02_a_fabricated_run_gets_no_attestation` | engine/runtime/governed_supervisor_server.py:909-918 -- `ledger.load_attestation_state(conn, run_id, attempt)` returns None ⇒ `_refusal(OP_ATTEST_RUN, REFUSE_NO_TERMINAL_RUN, ...)` (REFUSE_NO_TERMINAL_RUN = "no_terminal_run_state" at :178); governed_supervisor_ledger.load_attestation_state :1011 ret |
| `NM-ORACLE-03` | Signer caller bytes | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::NeverSignsCallerBytesTest::test_nm_oracle_03_signer_signs_only_recomputed_payload_never_output_bytes` | engine/runtime/isolated_signer.py:472-493 `validate_sign_request` (fixed top-level `{protocol, attestation, evidence}`, anything else ⇒ `_Refuse(malformed)`); :438-469 `_validate_evidence` exact key set (no inline bytes, no `*_sha256` claims); :592-601 sign_result signs `_jcs_bytes(payload)` of the  |
| `NM-ORACLE-04` | Attestation forgery | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::RefusalTest::test_nm_oracle_04_bad_attestation_is_refused` | engine/runtime/isolated_signer.py:611-629 `_verify_supervisor_attestation`: wrong `supervisor_key_id` ⇒ :616 `_Refuse(REASON_ATTESTATION_INVALID)`; verifier raising ⇒ :625; `ok is not True` ⇒ :627. Runs at sign_result step (2) (:571) BEFORE any store read or sign_fn call. A MISSING attestation objec |
| `NM-ORACLE-05` | Derived-hash trust | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::ValidSignTest::test_nm_oracle_05_inline_sha256_claim_is_rejected` | engine/runtime/isolated_signer.py:438-469 `_validate_evidence` -- `keys - allowed or allowed - keys` ⇒ `_Refuse(REASON_MALFORMED)` (EVIDENCE_FIELDS :139-146 carries only `*_handle` fields; an inline `output_sha256` is an extra key, a dropped handle is a missing key -- both refused); :732-748 `_deriv |
| `NM-ORACLE-06` | Reference-not-artifact | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::RefusalTest::test_nm_oracle_06_missing_handle_is_refused` | engine/runtime/isolated_signer.py:732-748 `_derive_hashes`: `data = self._store.read_verified(handle)`; `if data is None` ⇒ :743 `_Refuse(REASON_HANDLE_MISSING)` (containment handle ⇒ `containment_missing` :741-742); chain handles :800 `_verify_chain_handles` likewise `REASON_HANDLE_MISSING`; Artifa |
| `NM-ORACLE-08` | Authority two-step oracle | **bound 2026-09-19 (#226)** → `engine/tests/test_challenge_authority.py::ValidateCreatePendingTests::test_nm_oracle_08_extra_arbitrary_field_rejected` | engine/runtime/challenge_authority.py:158-247 `validate_create_pending` -- exact CREATE_PENDING_FIELDS (:75-86) set: extra ⇒ :178-183 `ChallengeAuthorityError('create-pending rejected: arbitrary field(s) …')`, missing ⇒ :184-188, typed/bounded values :199-224; PendingStore.create_pending :322-370 re |
| `NM-ORACLE-09` | Create-pending carries hash | **bound 2026-09-19 (#226)** → `engine/tests/test_challenge_authority.py::ValidateCreatePendingTests::test_nm_oracle_09_caller_supplied_request_sha256_rejected_as_malformed` | engine/runtime/challenge_authority.py:170-176 -- `if "request_sha256" in fields: raise ChallengeAuthorityError('create-pending rejected: request_sha256 is caller-supplied and forbidden; the authority recomputes it (§2.1)')`; ChallengeAuthorityError's default `reason=REASON_MALFORMED` (:120, REASON_M |
| `NM-ORACLE-13` | Caller-supplied `execution_attempt_id` | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_evidence_request.py::NothingGovernedIsMintedTests::test_nm_oracle_13_a_request_carrying_a_supervisor_minted_id_is_refused_malformed` | engine/runtime/governed_evidence_request.py:116-121 `EVIDENCE_REQUEST_FIELDS = (protocol, install_id, challenge_handle, request_nonce)` -- docstring :111-115 'A request carrying an execution_attempt_id is a requester naming the identity its own execution would later be judged under'; gate_evidence_r |
| `NM-XBIND-04` | Receipt handle mismatch | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::RefusalTest::test_nm_xbind_04_missing_chain_handle_is_refused` | engine/runtime/isolated_signer.py:771-812 `_verify_chain_handles`: `read_verified` None -> `REASON_HANDLE_MISSING` (:800); protocol tag check `document.get("protocol") != "brops.execution-receipt.v1"` -> handle_missing (:810); shared-field agreement for execution_receipt_handle = ("run_id", "executi |
| `NM-XBIND-07` | Transport-echo mismatch | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_bridge_result.rs::tests::nm_xbind_07_each_altered_echo_blocks_by_name` | apps/desktop/src-tauri/core/src/governed_bridge_result.rs:459-480 `SignedTurnResult::check_echoes(&self, envelope: &ReceiptEnvelope) -> Result<(), TurnReason>`: compares echo_run_id, echo_execution_attempt_id, echo_supervisor_attestation_key_id (:460-471), echo_output_sha256 (:473-475), echo_output_ |
| `NM-XBIND-08` | Identity mismatch | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::RefusalTest::test_nm_xbind_08_unknown_identity_is_refused` | engine/runtime/isolated_signer.py:678-685 `_check_identity`: `if evidence["executor_id"] not in cfg.allowed_executor_ids: raise _Refuse(REASON_IDENTITY_DENIED)` (:680-681), same for `builder_id` (:682-683) and `supervisor_id` (:684-685); allow-sets are `SignerConfig.allowed_executor_ids/allowed_buil |
| `NM-XBIND-09` | Not-completed decision | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::RefusalTest::test_nm_xbind_09_non_completed_decision_is_refused` | engine/runtime/isolated_signer.py:630-676 `_check_run_binding`: `if evidence["decision"] != "completed": raise _Refuse(REASON_NOT_COMPLETED)` (:675-676); called at sign_result step (3) (:581). Desktop-side second gate: apps/desktop/src-tauri/core/src/governed_verification.rs:510-512 `bind_attested_e |
| `NM-XBIND-11` | Policy mismatch | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer.py::RefusalTest::test_nm_xbind_11_a_policy_the_operator_never_authorized_is_refused` | engine/runtime/isolated_signer.py:687-715 `_check_policy_authorization`: `allowed = self._config.allowed_policies; if allowed is None: raise _Refuse(REASON_POLICY_MISMATCH)` (:708-709); `key = (policy_id, policy_version); expected_bundle = allowed.get(key); if expected_bundle is None: raise ...` (:7 |
| `NM-XBIND-12` | Envelope signature | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_verification.rs::tests::nm_xbind_12_a_wrong_isolated_signer_key_blocks` | apps/desktop/src-tauri/core/src/governed_verification.rs:891-1000 `verify_and_accept`: step 1 pins `envelope.key_id != keys.isolated_signer_key_id` -> UpstreamBlocked (:906-908); step 2 (:914-915) `let payload_jcs = envelope.payload_jcs()?; verify_ed25519(keys.isolated_signer_public_key, &payload_jc |
| `NM-XBIND-13` | Attestation digest bind | apps/desktop/src-tauri/core/src/governed_verification.rs::tests::attestation_evidence_digest_mismatch_blocks (:1477-1489): re-signs DIFFERENT evidence bytes under the real supervisor key so only the digest binding fails -> `Err(TurnReason::UpstreamBlocked)` -- exactly this refusal, one fault. Companion ::a_bad_supervisor_attestation_signature_blocks (:1465) covers the signature limb. Python side: engine/tests/test_isolated_signer.py::ValidSignTest::test_valid_request_returns_well_formed_envelope | apps/desktop/src-tauri/core/src/governed_verification.rs:917-926 `verify_and_accept` step 3: `verify_ed25519(keys.supervisor_attestation_public_key, attestation.evidence_jcs, attestation.signature_b64)?; if sha256_hex(attestation.evidence_jcs) != envelope.attestation_evidence_sha256 { return Err(Tur |
| `NM-TIME-03` | Challenge window (acceptance) | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_time_03_timestamp_invalid_when_the_challenge_expired_before_acceptance` | engine/runtime/governed_supervisor.py:628-629 (`if now_ms > payload["challenge_expires_at_ms"]: raise _Refuse(REFUSE_CHALLENGE_EXPIRED, ...)`) where `now_ms` is the single acceptance clock read at engine/runtime/governed_acceptance.py:596 (`accepted_at = self.clock_ms()`); mapped to the governed mem |
| `NM-TIME-04` | Challenge window lower | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_time_04_timestamp_invalid_when_the_challenge_was_issued_after_acceptance` | engine/runtime/governed_acceptance.py:732-736 in `_acceptance_time_predicate`: `issued = payload.get("challenge_issued_at_ms")` … `if not isinstance(issued, int) or isinstance(issued, bool) or issued > accepted_at: raise _Refuse("timestamp_invalid", "challenge_issued_at_ms is not <= challenge_accept |
| `NM-TIME-05` | `requested_at` ordering | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_time_05_timestamp_invalid_when_the_request_postdates_its_own_acceptance` | engine/runtime/governed_acceptance.py:733,737-739 (`requested = payload.get("requested_at_ms")` … `requested > accepted_at` ⇒ `_Refuse("timestamp_invalid", "requested_at_ms is not <= challenge_accepted_at_ms")`). Second, later belt in the signer: engine/runtime/isolated_signer.py:722-723 (`requested |
| `NM-TIME-17` | Desktop freshness skew | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_verification.rs::tests::nm_time_17_a_perfectly_signed_receipt_from_a_year_ago_is_refused` | apps/desktop/src-tauri/core/src/governed_verification.rs:828-880 `check_receipt_freshness` — inclusive window check at :871-878 (`t > future_limit \|\| t < stale_limit` ⇒ `Err(TurnReason::UpstreamBlocked)`) over both signed `_ms` fields, against `GOVERNED_TURN_FRESHNESS = FreshnessWindow::DEFAULT` ( |
| `NM-CONC-08` | Lease-not-ready trigger | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_conc_08_lease_not_ready_when_the_lease_document_cannot_be_published` | engine/runtime/governed_acceptance.py:514-517 (`_drive`: `if state == ledger.ACCEPTED_PREPARED: row = self._publish_lease(row)` — an execute trigger that finds the row before LEASE_READY is driven through the lease publish); :764-767 (`_publish_lease`: `except Exception as exc: raise _Refuse("lease_ |
| `NM-CONC-09` | Desktop double-consume | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt_store.rs::tests::nm_conc_09_concurrent_verifications_of_one_nonce_accept_exactly_once` | apps/desktop/src-tauri/core/src/receipt_store.rs:296-311 (inside `verify_and_record_receipt`'s owned tx: `let affected = tx.execute("UPDATE receipt_challenges SET consumed_at = ?1 WHERE nonce = ?2 AND consumed_at IS NULL", ...)?; if affected == 1 { NonceState::Consumed {..} } else { // Lost a race t |
| `NM-ACL-09` | desktop/login → store or keys | engine/ci/isolation_proof.sh step 7 (:186-191, `== the four denials, as the login (attacker) user ==` ... `expect_prover "ISOLATION PROOF" 0 "ISOLATION PROOF PASSED"`) running engine/tools/brops_isolation_prover.py AS the login user: `run_attacks` (:521-528) -> `2_read_signer_key` and `2_read_attestation_key` via `attack_read_file` (:326-364: `PermissionError` ⇒ DENIED, `FileNotFoundError` ⇒ INCONCLUSIVE, read succeeds ⇒ BREACH) and `3_store_no_read_write` via `attack_store` (:398-422: `_probe_w | OS custody provisioned by engine/ci/isolation_proof.sh:48-51 (`chown -R brops-signer:brops-signer signerkeys; chmod 700`; `chown -R brops-supervisor:brops-supervisor attkeys; chmod 700`; `chown -R brops-supervisor:brops-store store; chmod 2770 store`) with the login user "in NEITHER service context  |
| `NM-ACL-12` | Login/sidecar → private-key dir | engine/ci/isolation_proof.sh step 7 (:186-191) + engine/tools/brops_isolation_prover.py `attack_read_file` (:326-364) for `2_read_signer_key` and `2_read_attestation_key` (:524-525), run AS the login user; `owner_can_read` positive control (:137-149) first proves each key exists, is non-empty and is readable by its owning principal; `_control_foreign_dir` (:277-321) proves the key store dir is foreign-owned before scoring. CI: .github/workflows/ci.yml:1185-1200. The recorder-key limb has nothing | Provisioning: engine/ci/isolation_proof.sh:48-49 (`sudo chown -R brops-signer:brops-signer "$B/signerkeys"; sudo chmod 700 "$B/signerkeys"` / `... brops-supervisor "$B/attkeys"; sudo chmod 700 "$B/attkeys"`). Runtime self-checks: the key loaders refuse a group/other-accessible dir — engine/tests/tes |
| `NM-NORELAUNCH-02` | Grant reuse after recovery | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::IdempotenceTests::test_nm_norelaunch_02_a_trigger_arriving_at_a_started_attempt_moves_to_recovery_required` | engine/runtime/governed_acceptance.py:537-539 (`if state in _TERMINAL_REASONS: raise _Refuse(_TERMINAL_REASONS[state], "attempt is durably %s" % state, row["receipt_id"])`) with :192 `ledger.RECOVERY_REQUIRED: "not_completed"`; `_locate` :551+ finds the SAME row by challenge/nonce because of the led |
| `NM-NORELAUNCH-03` | EXECUTING without terminal proof | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::IdempotenceTests::test_an_attempt_left_in_executing_by_a_crash_is_never_relaunched_nm_norelaunch_03` | engine/runtime/governed_acceptance.py:519-527 (the `state in (EXECUTION_STARTING, EXECUTING)` branch -> `_advance(RECOVERY_REQUIRED)` + `_Refuse("not_completed")`); legal edge governed_supervisor_ledger.py:92 and supervisor_ledger.sql:111. |
| `NM-EVID-05` | C (regression guard) | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_supervisor_ledger.py::EvidenceFloorTests::test_nm_evid_05_a_strictly_higher_head_advances_the_floor` | engine/runtime/governed_supervisor_ledger.py:774-866 `_evidence_floor_cas`: the fork test fires only on an EQUAL head (:821-827 `if head_sequence == row["highest_head_sequence"]: ... raise EvidenceFork`); a strictly higher head passes the install ceiling (:830-845, StaleEvidence only for `<`, Eviden |
| `NM-IPC-02` | Authority channel | **bound 2026-09-19 (#226)** → `engine/tests/test_challenge_authority_server.py::PeerDenyTests::test_nm_ipc_02_non_broker_peer_denied_before_any_frame` | engine/runtime/challenge_authority_server.py:248-253 (same pre-frame gate; renderer uid != broker uid -> refused); engine/runtime/challenge_authority.py:406-417 `peer_is_broker`. Rust: apps/desktop/src-tauri/broker/src/main.rs:594-598 (`peer_cred` via SO_PEERCRED :556-580, then `authorize_peer` -> ' |
| `NM-IPC-03` | Signer socket | **bound 2026-09-19 (#226)** → `engine/tests/test_isolated_signer_server.py::PeerDenyTests::test_nm_ipc_03_sidecar_peer_denied` | engine/runtime/isolated_signer_server.py:268-273 (`peer_uid = getattr(conn, "peer_uid", None)`; `# Allowlist ONLY the broker uid; refuse BEFORE reading any frame.` -> `{"ok": False, "error": "peer not authorized"}`), `peer_is_broker` :104-117, SO_PEERCRED :83-97; v1 signer transport engine/runtime/b |
| `NM-IPC-04` | Supervisor socket | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_supervisor_server.py::PeerDenyTests::test_nm_ipc_04_non_broker_peer_denied_before_any_frame` | engine/runtime/governed_supervisor_server.py:993-1023: `broker_peer = peer_is_broker(peer_uid, allowed_broker_uid)`, `sidecar_peer = sidecar_uid is not None and peer_is_sidecar(...)`, `# Refuse an unknown peer BEFORE reading any frame.` `if not (broker_peer or sidecar_peer): reply = {"ok": False, "e |
| `NM-IPC-06` | Renderer -> any service IPC | Rust: apps/desktop/src-tauri/core/src/ipc_framing.rs:192 (RENDERER -> Denied); apps/desktop/src-tauri/core/src/windows_broker.rs:374-376. Python (renderer uid at each door): engine/tests/test_challenge_authority_server.py::PeerDenyTests::test_non_broker_peer_denied_before_any_frame (:109), engine/tests/test_isolated_signer_server.py::PeerDenyTests::test_non_broker_peer_denied_before_any_frame (:210), engine/tests/test_governed_supervisor_server.py::PeerDenyTests::test_non_broker_peer_denied_befo | Per channel, none allowlists the login/renderer uid: broker front door apps/desktop/src-tauri/broker/src/main.rs:594-598 (SO_PEERCRED read :556-580 then `authorize_peer`), apps/desktop/src-tauri/core/src/ipc_framing.rs:123-131 (renderer on the deny list -> `PeerError::Denied`); Windows apps/desktop/ |
| `NM-SCOPE-01` | Workspace mismatch | apps/desktop/src-tauri/core/src/receipt.rs::tests::every_expected_value_mismatch_blocks (lines 1058-1090): case `("workspace_id", \|e\| **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt.rs::tests::nm_scope_01_every_expected_value_mismatch_blocks` | apps/desktop/src-tauri/core/src/receipt.rs:465-495 (`Verified::bind`: `("workspace_id", req.workspace_id)` in the `equals` table at 480, `Err(ReceiptError::Mismatch { field })` at 491-493); consumed by receipt_store.rs:594-597 (`verified.bind(&turn.expected, ...)` → `CoreVerdict::Failed`) → receipt_ |
| `NM-SCOPE-02` | Install mismatch | apps/desktop/src-tauri/core/src/receipt.rs::tests::every_expected_value_mismatch_blocks (1058-1090): case `("install_id", \|e\| **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt.rs::tests::nm_scope_02_install_id_mismatch_blocks` | apps/desktop/src-tauri/core/src/receipt.rs:481 (`("install_id", req.install_id)` in `Verified::bind`'s equals table → `ReceiptError::Mismatch { field: "install_id" }` at 491-493) → receipt_store.rs:594-597 → Blocked at 322-346. Broker twin: governed_verification.rs:933 (`envelope.install_id != expec |
| `NM-SCOPE-03` | Supervisor mismatch | apps/desktop/src-tauri/core/src/receipt.rs::tests::every_expected_value_mismatch_blocks (1058-1090): case `("supervisor_id", \|e\| **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt.rs::tests::nm_scope_03_supervisor_id_mismatch_blocks` | apps/desktop/src-tauri/core/src/receipt.rs:487 (`("supervisor_id", expected.supervisor_id)` in `Verified::bind` → `Mismatch { field: "supervisor_id" }`). Python twins: engine/runtime/isolated_signer.py:684 (`if evidence["supervisor_id"] not in cfg.allowed_supervisor_ids` → refuse) and governed_accep |
| `NM-SCOPE-07` | Bare key escape | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt.rs::tests::nm_scope_07_claimed_key_id_must_equal_resolved_key_id` | apps/desktop/src-tauri/core/src/receipt.rs:199-227 (`ResolvedManifestKey` private fields, `pub(crate) fn manifest_resolved` is the only non-test mint, called solely by manifest_authority.rs:91-95); receipt.rs:358-368 (`Parsed::verify` refuses `self.key_id() != resolved_key.key_id` → `ReceiptError::K |
| `NM-FS-04` | Handle traversal | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/proof/src/bin/governed_recorder.rs::tests::nm_fs_04_a_store_publication_refuses_a_name_that_is_not_a_content_address` | Three seams, all refuse before any path join: apps/desktop/src-tauri/proof/src/bin/governed_recorder.rs:179-181 `is_store_handle` (len 64, `[0-9a-f]` only) + 196-208 `store_blob_path` (Err "is not a 64-char lowercase sha256 content address"); engine/runtime/brops_evidence_store.py:241-244 `EvidenceS |
| `NM-FS-07` | Writable ancestor dir | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/tcb_integrity.rs::tests::nm_fs_07_writable_ancestor_is_violation` | apps/desktop/src-tauri/core/src/tcb_integrity.rs:285-309 (`for anc in ancestor_dirs(&art.path) { ... if af.is_world_or_group_writable \|\| af.writable_by_login_or_runtime { return Err(TcbViolation::AncestorWritable { logical_name, ancestor }) } }`) inside `verify_tcb_integrity` (213-234); start-refu |
| `NM-FS-08` | Orphan temp sweep | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_staging_upload.py::StagingSweepTests::test_nm_fs_08_an_orphan_temp_is_unlinked_only_once_no_write_could_own_it` | engine/runtime/governed_staging_upload.py:991-1016 `_sweep_orphan_temps` (unlinks `TEMP_CHUNK_PREFIX`/`TEMP_CHUNK_SUFFIX` files older than `STAGING_CLEANUP_DEADLINE_MS`; constants derived from the writer at 933-934), called from `sweep_staging` 1018-1089 (1074) after the ledger half; engine/runtime/ |
| `NM-OUTPUT-01` | Single-byte flip | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_output_pull.rs::tests::nm_output_01_a_one_byte_substitution_fails_the_digest_gate_and_not_the_length_gate` | apps/desktop/src-tauri/core/src/governed_output_pull.rs:572-574 (OutputPull::finish digest gate: `if sha256_hex(&self.assembled) != self.expected_sha256 { return Err(PullError::DigestMismatch) }`, mapped to TurnReason::CommitReadbackMismatch); second gate at apps/desktop/src-tauri/core/src/governed_ |
| `NM-OUTPUT-02` | Length mismatch | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_output_pull.rs::tests::nm_output_02_a_truncated_chunk_fails_the_length_gate` | apps/desktop/src-tauri/core/src/governed_output_pull.rs:568-570 (`if self.assembled.len() as u64 != self.expected_bytes { return Err(PullError::LengthMismatch) }`); apps/desktop/src-tauri/core/src/governed_verification.rs:977-979 (`if output.len() as u64 != envelope.output_bytes { return Err(TurnRea |
| `NM-OUTPUT-03` | Normalization smuggle | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_output_pull.rs::tests::nm_output_03_a_crlf_normalized_stream_blocks` | apps/desktop/src-tauri/core/src/governed_output_pull.rs:560-575 (finish(): length gate then digest gate over `self.assembled` with no transformation; doc line 562-563 'no trim, no NFC/NFKC, no CRLF conversion, no lossy decode'); governed_verification.rs:975-982 likewise hashes `output` untouched |
| `NM-OUTPUT-04` | Invalid UTF-8 | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_verification.rs::tests::nm_output_04_invalid_utf8_output_blocks_even_when_the_digest_matches` | apps/desktop/src-tauri/core/src/governed_verification.rs:983-987 (`// Only after the digest gate: strict-UTF8 decode ... let accepted_body = match std::str::from_utf8(output) { Ok(s) => ..., Err(_) => return Err(TurnReason::UpstreamBlocked) }`), placed after the length/digest gates at 977-982 |
| `NM-OUTPUT-06` | Stream-id as authority | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_bridge_result.rs::tests::nm_output_06_the_echoes_have_no_accessor_so_no_gate_can_be_aimed_at_them` | apps/desktop/src-tauri/core/src/governed_output_pull.rs:330-349 `OutputStreamCapability::from_envelope(output_stream_id, envelope: &ReceiptEnvelope)` -- the token is shape-checked only; receipt/attempt ids and (at :481-508) expected_bytes/expected_sha256 come solely from the envelope; `pub fn pull_o |
| `NM-OUTPUT-08` | Stream binding | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_output_read.py::VerdictOrderTests::test_nm_output_08_another_turns_live_token_cannot_be_redeemed_against_this_turns_ids` | engine/runtime/governed_output_read.py:398-411 in `gate_output_read`: `if row is None: ... REFUSE_STREAM_UNKNOWN` (398-399), `if streams.is_expired(row, now_ms): ... REFUSE_STREAM_EXPIRED` (404-405), `if (row["receipt_id"] != parsed.receipt_id or row["execution_attempt_id"] != parsed.execution_attem |
| `NM-OUTPUT-09` | Seq out of range | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_output_read.py::VerdictOrderTests::test_nm_output_09_seq_out_of_range_is_the_last_verdict` | engine/runtime/governed_output_read.py:413-414 (`if parsed.seq > last_seq(row["output_bytes"]): return None, output_read_refused(REFUSE_SEQ_OUT_OF_RANGE, **echo)`), after the binding compare (409-411); negative seq is `malformed` at the shape stage (test 391) |
| `NM-PARITY-03` | `output` (raw UTF-8, no trim) | **bound 2026-09-19 (#226)** → `engine/tests/test_brops_parity.py::BropsParityTests::test_nm_parity_03_output_formula` | Python engine/runtime/brops_canonical.py:113-117 `output_bytes(output) = output.encode("utf-8")` / :134 `output_sha256`; Rust apps/desktop/src-tauri/core/src/receipt.rs:1229-1232 (`sha256_hex("the answer".as_bytes())`), and the live gates hash raw bytes: governed_verification.rs:980, governed_output |
| `NM-PARITY-04` | `generation_config` (object-JCS, §4.10(g)) | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_turn_submit_e2e.py::TheCanonicalBytesAreTheFixturesTests::test_nm_parity_04_the_governed_generation_config_hashes_to_the_digest_the_design_publishes` | Python engine/runtime/brops_canonical.py:326-342 `governed_generation_config_bytes` = canonical_bytes(validate_governed_generation_config(obj)) / `governed_generation_config_sha256`; Rust apps/desktop/src-tauri/core/src/governed_prepare.rs:702 `sha256_hex(&generation_config_jcs)` over `receipt::jcs_ |
| `NM-PARITY-08` | `request_sha256` (8-field envelope) | **bound 2026-09-19 (#226)** → `engine/tests/test_brops_parity.py::BropsParityTests::test_nm_parity_08_request_envelope_formula` | apps/desktop/src-tauri/core/src/receipt.rs:263 `pub fn request_envelope_sha256(...)` <-> engine/runtime/brops_canonical.py:158 `request_sha256(...)`; the live signer recomputes the same 8-field envelope at engine/runtime/isolated_signer.py:836-856 `_recompute_request_sha256` ('the SAME JCS formula a |
| `NM-REPLAY-01` | Desktop nonce consume | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt_store.rs::tests::nm_replay_01_replayed_nonce_is_blocked_without_a_second_message_or_consume` | apps/desktop/src-tauri/core/src/receipt_store.rs:296-299 (`Some((_, Some(_consumed), _)) => NonceState::Replay` inside `record()`), :353-354 (`!nonce_state.is_consumed()` ⇒ blocked verdict), :656 (block_reason "request_nonce already consumed (replay)"); 3b-1B mirror apps/desktop/src-tauri/core/src/g |
| `NM-REPLAY-02` | `receipt_id` global uniqueness | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt_store.rs::tests::nm_replay_02_duplicate_receipt_id_is_blocked_no_message_no_ledger_dup` | apps/desktop/src-tauri/core/src/receipt_store.rs:360-361 (`receipt_id_seen(tx, &receipt_id)?` ⇒ "receipt_id already accepted (replay)"), :682-690 (`receipt_id_seen` EXISTS query, same BEGIN IMMEDIATE tx), :470-474 / :502-506 (the `receipt_ids_seen` INSERT only on accept); 3b-1B mirror governed_verif |
| `NM-REPLAY-03` | Supervisor exec replay (same nonce, same challenge) | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::IdempotenceTests::test_a_second_trigger_for_a_completed_turn_returns_the_identical_frame_nm_replay_03` | engine/runtime/governed_acceptance.py:506-551 (`AcceptanceDriver._drive`: `_locate` returns the stored row; COMPLETED falls through to `_attest_and_sign(row)`, no execute branch), :552-580 (`_locate` by challenge_handle); engine/runtime/governed_supervisor_ledger.py:410-443 (`reuse_or_prepare` retur |
| `NM-REPLAY-04` | Ledger `challenge_replay` | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_replay_04_challenge_replay_when_the_nonce_already_bought_a_different_challenge` | engine/runtime/governed_acceptance.py:576-579 (`_locate`: challenge finds no row but `ledger.load_lease_by_nonce` finds the nonce ⇒ `_Refuse("challenge_replay", ...)`), backed by `UNIQUE (install_id, request_nonce)` engine/runtime/supervisor_ledger.sql:88 and governed_supervisor_ledger.py:397-406 |
| `NM-REPLAY-07` | Conflicting retry facts | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_turn_open.py::RefusalsAreReachableTests::test_nm_replay_07_retry_conflict_on_a_different_challenge_under_the_same_nonce` | engine/runtime/governed_staging_ledger.py:240-256 (`_matches` compares run_id/task_id/workspace_id/digests) + :289-297 (`Conflict` when the stored (install_id, request_nonce) row disagrees) ⇒ engine/runtime/governed_turn_open.py:610-611 (`except staging.Conflict: return refused(REFUSE_RETRY_CONFLICT |
| `NM-REPLAY-09` | Authority pending single-use | **bound 2026-09-19 (#226)** → `engine/tests/test_challenge_authority.py::IssueTests::test_nm_replay_09_issue_persists_and_replays_byte_for_byte` | engine/runtime/challenge_authority.py:585-598 (ISSUED row: past `ISSUED_RETENTION_MS` ⇒ `REASON_PENDING_EXPIRED`; otherwise returns the STORED document, never re-signs), :600-611 (PENDING ⇒ sign ONCE then `store.mark_issued`), :378-396 (`mark_issued` raises for any non-PENDING row — a CAS loser neve |
| `NM-REPLAY-10` | Replayed output chunk | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_output_pull.rs::tests::nm_replay_10_a_replayed_chunk_with_mutated_bytes_fails_the_digest_gate` | apps/desktop/src-tauri/core/src/governed_output_pull.rs:560-571 (`OutputPull::finish`: length gate then `sha256_hex(&self.assembled) != self.expected_sha256` ⇒ `PullError::DigestMismatch`), :285-290 (`to_turn_reason` ⇒ `TurnReason::CommitReadbackMismatch` Block); a replayed `seq` is caught earlier a |
| `NM-REG-06` | Registry rotated between open and accept | **bound 2026-09-19 (#226)** → `engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_reg_06_the_registry_is_re_resolved_for_every_acceptance` | engine/runtime/governed_acceptance.py:706-731 (`_acceptance_time_predicate`: `self.resolve_registry_document()` re-resolved FRESH, `registry.resolve_registry(...)`, `registry.select_key(snapshot, challenge_key_id, accepted_at)`; any failure ⇒ `_Refuse("challenge_invalidated", ...)` at :712/:721/:727 |
| `NM-TERM-01` | Terminal-refusal-once (transport) | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt_store.rs::tests::nm_term_01_pre_verification_block_records_the_real_reason_and_consumes_the_nonce` | apps/desktop/src-tauri/core/src/receipt_store.rs:209-246 (`record_pre_verification_block`: one `in_immediate_tx`, `UPDATE receipt_challenges SET consumed_at` :218-223, one `insert_attempt` with `outcome: "blocked"`, `message_id: None`, real `reason` :224-244). Wiring: apps/desktop/src-tauri/src/comm |
| `NM-TERM-03` | No message on Block | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/receipt_store.rs::tests::nm_term_03_bad_signature_blocks_and_never_writes_a_message` | apps/desktop/src-tauri/core/src/receipt_store.rs:327-345 and :365-381 (every Block arm of `record()` inserts one `AttemptRow{outcome: "blocked", message_id: None}` and never reaches the `messages` insert or the `receipt_ids_seen` insert at :470/:502), :209-246 (transport Block: same, `wire_env: ""`) |
| `NM-TERM-05` | Sidecar fabricates success | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_verification.rs::tests::nm_term_05_a_sidecar_fabricated_signed_result_fails_signature_verification` | apps/desktop/src-tauri/core/src/governed_verification.rs:915 and :919 (`verify_ed25519(keys.isolated_signer_public_key, &payload_jcs, envelope_signature_b64)?` and the supervisor-attestation verify, impl :736-780) inside `verify_and_accept` :891-1000; echo binding apps/desktop/src-tauri/core/src/gov |
| `NM-TERM-07` | Namespace disjointness | **bound 2026-09-19 (#228)** -> `apps/desktop/src-tauri/core/src/governed_bridge_result.rs::tests::nm_term_07_a_reason_outside_the_closed_union_is_a_malformed_frame_not_a_thirtieth_reason` | apps/desktop/src-tauri/core/src/governed_bridge_result.rs:253-296 (the closed 29-member `GOVERNED_REFUSAL_REASONS` array) + :303-308 (`GovernedRefusal::parse` — anything outside ⇒ `None` ⇒ `FrameError::MalformedFrame`); engine mirror engine/runtime/governed_turn_result.py:359 (`_checked(body["reason |

## §3 Implementable — control located, test described, sized

| ID | size | under test | control | test to write |
| :-- | :-- | :-- | :-- | :-- |
| `NM-CAP-03` | S | Base-validator confusion | engine/runtime/bro_execution_lease.py:160-180 (`validate_execution_lease`: `required = {"schema","lease_id","nonce","task_id","agent_id","session_id","repository","branch","worktree","head_sha","tree_identity","allowed_capabilities","allowed_egress","issued_at_epoch","expires_at_epoch","max_tool_cal | Python, engine/tests/test_negative_matrix.py `test_nm_cap_03_a_governed_turn_lease_is_refused_by_the_base_validator`: build the §4.3 `brops.governed-turn-lease.v1` payload literal (schema 1, lease_id, nonce 32 chars, run_id, execution_attempt_id, task_id, agent_id, session_id, workspace_id, install_id, supervisor_id, task_class "governed-model-turn-v1", allowed_capabilities ["INVOKE_GOVERNED_MODEL"], max_tool_calls 0, launcher/executor sha256, model_profile_id "cfg-sha256:"+64hex, generation_config_sha256, lease_issued_at_ms, lease_expires_at_ms) and, as a second sub-case, the tree's own `gove |
| `NM-CONC-07` | S | Duplicate artifact publish | engine/runtime/brops_evidence_store.py:283-296 (`_atomic_publish`: `if not atomic_link_or_create(tmp, target, data): self._verify_idempotent(target, handle)`); :187-224 (`atomic_link_or_create`: `os.link` -> `except FileExistsError: return False`; O_EXCL fallback likewise); :298-303 (`_verify_idempo | Python, engine/tests/test_negative_matrix.py `test_nm_conc_07_republishing_a_handle_is_idempotent_and_a_collision_is_refused`: `store = EvidenceStore(tmpdir)`; `h1 = store.publish(b"artifact")`; assert `store.publish(b"artifact") == h1` and `[p.name for p in store.root.iterdir()] == [h1]` (idempotent limb); then `(store.root / h1).write_bytes(b"different bytes")` (the 'other collision' — an existing handle whose bytes do not address to it); `with assertRaises(EvidenceStoreError) as c: store.publish(b"artifact")`; `assertIn("content-address collision", str(c.exception))`; assert no `.tmp-*.part |
| `NM-CRASH-01` | S | in `VERIFYING`/`UPLOADING`/`INPUTS_READY` (pre-accept staging) | engine/runtime/governed_staging_upload.py:1018-1090 (`sweep_staging`: ledger half then filesystem half; docstring :1036-1038 'will not ... consume a challenge nonce'), :991-1015 (`_sweep_orphan_temps`, prefix/suffix constants :934-935 `.tmp-` / `.part` derived from the writer at :466); engine/runtim | Python, engine/tests/test_governed_staging_upload.py::StagingSweepTests::test_nm_crash_01_...: open one session (turn in UPLOADING), write a stale `.tmp-*.part` under its session_dir (utime older than gsl.STAGING_CLEANUP_DEADLINE_MS) -- the durable footprint of a crash mid-upload; assert `SELECT COUNT(*) FROM governed_turn_acceptance == 0`; call `gsu.sweep_staging(conn, staging_root, EXPIRES + 1)`; assert the temp is unlinked, the staging row is gone, and `gsl.open_staging(...)` under the SAME (install_id, request_nonce, challenge_handle) returns (CREATED, UPLOADING). One fault (the crash), th |
| `NM-CRASH-02` | S | before acceptance commit | engine/runtime/governed_supervisor_ledger.py:247-272 (`_Tx`: owned BEGIN IMMEDIATE, COMMIT on success, ROLLBACK on exception) and :410-443 (`reuse_or_prepare`: the absent->ACCEPTED_PREPARED CAS inside that one tx); engine/runtime/governed_acceptance.py:591-603 (the driver calls `reuse_or_prepare`; n | Python, engine/tests/test_governed_acceptance.py (new class CrashCutTests(_Case) or IdempotenceTests): `ready_turn()`; driver with `clock_ms` raising `KeyboardInterrupt` on its FIRST read (the §5 step-2 acceptance clock, read before the CAS) -> `assertRaises(KeyboardInterrupt)` around `trigger` (the front door does not catch it: governed_evidence_request.py:415 calls `drive_acceptance` bare); assert `acceptance_row(document) is None`, `SELECT COUNT(*) FROM governed_turn_acceptance == 0`, staging row still INPUTS_READY; then `trigger(document)` with a normal driver -> `status == signed`, exactl |
| `NM-CRASH-03` | S | after commit, before signature | engine/runtime/governed_acceptance.py:511-517 (`_drive` resumes an ACCEPTED_PREPARED row through `_publish_lease`) and :741-781 (`_publish_lease`: publishes the EXACT persisted `row['lease_payload_bytes']`, refuses unless the store's handle == `lease_payload_sha256`, then `mark_lease_ready`; docstri | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; first trigger with `publish_artifact` raising `KeyboardInterrupt` (the supervisor died after the CAS commit and before any lease publish) -> assertRaises; assert row state ACCEPTED_PREPARED, `lease_handle IS NULL`, no lease blob in `store.blobs`; second trigger with the normal driver -> `status == signed`; assert `acceptance_row['lease_handle'] == acceptance_row['lease_payload_sha256']` (the lease re-derived from the persisted bytes, deterministic), still one acceptance row, `executor.runs == 1`. The docstring MUST state that §5 |
| `NM-CRASH-04` | S | after signature, before publish | engine/runtime/governed_acceptance.py:757-763 (`publish_artifact(bytes(row['lease_payload_bytes']))`, handle re-checked against `lease_payload_sha256`); production create-if-absent primitive engine/runtime/brops_evidence_store.py:184 (`atomic_link_or_create`) used at :305-309; the test store's `publ | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; wrap `publish_artifact` in a counter that delegates to `self.store.publish`; crash the first trigger after the CAS (KeyboardInterrupt from the wrapper BEFORE storing) -> row ACCEPTED_PREPARED, no lease blob; retrigger -> signed; assert the retry's publish returned `lease_payload_sha256`, `store.blobs` holds exactly ONE lease blob, and a further identical trigger does not add a blob or change `lease_handle` (create-if-absent, idempotent), `executor.runs == 1`. Optional store-level limb in engine/tests/test_evidence_store_shared_a |
| `NM-CRASH-05` | S | after publish, before `LEASE_READY` | engine/runtime/governed_acceptance.py:757-770 (re-publish is idempotent; `handle.lower() != row['lease_payload_sha256']` -> `hash_mismatch`; then `ledger.mark_lease_ready`), engine/runtime/governed_supervisor_server.py:684-692 (same on the wire); engine/runtime/governed_supervisor_ledger.py:502-507  | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; first trigger with a `publish_artifact` wrapper that calls `self.store.publish(data)` and THEN raises `KeyboardInterrupt` (crash after the publish, before the LEASE_READY CAS) -> assertRaises; assert row ACCEPTED_PREPARED and `store.blobs` already contains `lease_payload_sha256`; retrigger with the normal driver -> signed; assert `lease_handle == lease_payload_sha256`, still exactly one lease blob, `executor.runs == 1`. Docstring: the re-hash is asserted; `validate_governed_turn_lease` is not implemented (same gap as NM-CAP-01.. |
| `NM-CRASH-08` | S | after `EXECUTION_STARTING` commit, before launcher call | engine/runtime/governed_acceptance.py:519-528 (`_drive`: a trigger finding EXECUTION_STARTING or EXECUTING -> `_advance(RECOVERY_REQUIRED, 'no terminal proof for a started attempt')`, refuse `not_completed`, no launch; comment quotes §5 step 10); engine/runtime/governed_supervisor_ledger.py:92 (RECO | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; executor whose `run` raises `KeyboardInterrupt` BEFORE calling `on_started` (the supervisor died after the gate CAS; the launcher was never reached) -> assertRaises; assert row state EXECUTION_STARTING with `process_group_id IS NULL`; retrigger with a fresh `_Executor` -> refused `not_completed`, row RECOVERY_REQUIRED, `fresh.runs == []`; then `gsl.gate_and_start(conn, attempt, NOW)` raises IllegalTransition (no path back to a launch). |
| `NM-CRASH-10` | S | immediately after `exec` / child exits before `EXECUTING` persistence | engine/runtime/governed_acceptance.py:519-528 (`_drive`: EXECUTION_STARTING found by the next trigger -> RECOVERY_REQUIRED) and :823-836 (seam failure -> RECOVERY_REQUIRED); the completion guard for a child that exited before `on_started`: `record_completion`'s `_advance(COMPLETED)` from EXECUTION_S | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; `_Executor(skip_started=True)` (child ran and exited before EXECUTING was persisted) -> refused `not_completed`; assert the durable state as it actually is (EXECUTION_STARTING, `process_group_id IS NULL`) and completion rows == 0; retrigger with a fresh `_Executor` -> refused `not_completed`, row RECOVERY_REQUIRED, `fresh.runs == []`, completion rows still 0. Report to the parent: the tree reaches RECOVERY_REQUIRED one trigger later than the row's wording; the 'never relaunch' invariant holds at every step. |
| `NM-CRASH-16` | S | Launch-start marker misuse | The marker is write-only bookkeeping: engine/runtime/governed_supervisor_ledger.py:510-517 (`mark_executing` persists it under `WHERE state IN ('EXECUTION_STARTING')`); engine/runtime/governed_supervisor_server.py:712-731 (`execution-started` is the only wire op carrying it; on any state but EXECUTI | Python, one fault (the marker), two seams: (a) engine/tests/test_governed_acceptance.py: a `_Killed`-style executor calling `on_started(gac.StartedExecution(process_group_id='7', cgroup_id='cg-1', execution_started_marker='launch-nonce-1'))` then raising `KeyboardInterrupt`; assert row EXECUTING with the marker persisted; retrigger with a fresh executor -> refused `not_completed`, row RECOVERY_REQUIRED, `fresh.runs == []`; (b) engine/tests/test_governed_supervisor_server.py::LaunchGateDispatchTests: on an EXECUTING/RECOVERY_REQUIRED attempt send `execution-started` again carrying the same mark |
| `NM-EVID-12` | S | Sidecar cannot reach floor | engine/runtime/governed_supervisor_ledger.py:186-213 `open_ledger`: `os.makedirs(parent, mode=0o700, exist_ok=True)` (:195) and `os.chmod(db_path, 0o600)` on a fresh file (:198-202, OSError swallowed off-POSIX with the comment 'the directory ACL is the real control'); docstring :189-191 'created 060 | Python, engine/tests/test_negative_matrix.py: `conn = gsl.open_ledger(str(tmp / "sup" / "ledger.db"))`; on POSIX (`os.name == "posix"`, following the `_POSIX` precedent at test_brops_isolation.py:124-132) assert `stat.S_IMODE(os.stat(dir).st_mode) == 0o700` and `stat.S_IMODE(os.stat(file).st_mode) == 0o600`, and that `governed_evidence_head_floor` is a table of THAT file (sqlite_master) -- i.e. the floor lives behind the owner-only mode bits. Mutation: delete the chmod at :200 -> RED. The row's literal outcome (EACCES/denied for the SIDECAR uid) needs the multi-principal Linux runner: an isola |
| `NM-EVID-13` | S | Out-of-scope honesty | absent as code, present as prose: docs/design/WAVE_3B1B_EXECUTION_BINDING_ADDENDUM.md:3423-3431 ('This local table CANNOT detect a full-DB restore to an older self-consistent backup -- no external anchor exists ... OUT of the §0 threat model ... External monotonic anti-rollback anchoring ... is DEFE | Python, engine/tests/test_negative_matrix.py::NegativeMatrixEvidenceFloorTests (positive/honesty row, same class as NM-REG-09/NM-TIME-06/NM-TIME-09 per the module docstring :21-23): bootstrap head 12 (`_complete`), snapshot the floor row (or `conn.backup()` to a scratch connection), advance to head 13 from a second attempt (case C), then 'restore' the older self-consistent snapshot by rewriting the floor row to head 12 (direct table write -- the offline restore is the DB owner acting outside the running service; same rationale as NM-EVID-11's direct UPDATE at :651-652). Present head 13 again f |
| `NM-FS-03` | S | TCB path symlink | apps/desktop/src-tauri/broker/src/tcb_probe.rs:55-70 `LinuxFsProbe::stat_nofollow`: `libc::open(path, O_PATH \| O_NOFOLLOW \| O_CLOEXEC)` then `fstat(fd)`; 74-86 `digest(path, mode)` returns `""` unless `mode & S_IFMT == S_IFREG`; 88-108 `FsProbe::stat` derives `is_world_or_group_writable = (mode &  | Rust, apps/desktop/src-tauri/broker/src/tcb_probe.rs tests, `#[cfg(target_os = "linux")]`: `fn nm_fs_03_a_symlinked_tcb_path_is_measured_as_the_link_not_its_target()` — tempdir; write F (bytes B, `chmod 0644`), `symlink(F, L)`; `let p = LinuxFsProbe { login_and_runtime_uids: vec![] }`; `let t = p.stat(F).unwrap()`, `let l = p.stat(L).unwrap()`; assert `t.sha256 == sha256_hex(B)` and `!t.is_world_or_group_writable` (positive control) and `l.sha256.is_empty() && l.is_world_or_group_writable` (a link can never satisfy the content pin or the non-writability floor, so `verify_artifact` refuses it). |
| `NM-FS-05` | S | Hardlink to store | engine/runtime/brops_evidence_store.py:323-332 `EvidenceStore.read`: `data = path.read_bytes(); if sha256_hex(data) != handle: raise EvidenceStoreError(f"evidence store corruption: {handle} bytes do not hash to it")`; publish-side idempotency re-hash `_verify_idempotent` 313-318. Rust twin: recorder | Python, engine/tests/test_negative_matrix.py (NegativeMatrixFilesystemTests): `test_nm_fs_05_bytes_swapped_through_a_hardlink_are_refused_by_the_re_hash` — `store = EvidenceStore(root)`; `h = store.publish(b"original")`; `assertEqual(store.read(h), b"original")`; `try: os.link(root/h, attacker/"swap") except OSError as e: self.skipTest(f"hardlinks unsupported here: {e}")`; `(attacker/"swap").write_bytes(b"swapped")`; `assertRaisesRegex(EvidenceStoreError, "bytes do not hash to it", store.read, h)`; body carries "NM-FS-05". Mutation: delete brops_evidence_store.py:330-331 → returns b"swapped" → |
| `NM-IPC-01` | S | Authority create-pending/issue | engine/runtime/challenge_authority_server.py:248-253: `peer_uid = getattr(conn, "peer_uid", None)`; `# §2.1: allowlist ONLY the broker uid; refuse BEFORE reading any frame.` `if not peer_is_broker(peer_uid, allowed_broker_uid): reply = {"ok": False, "error": "peer not authorized"}; _try_write(conn,  | Python, engine/tests/test_negative_matrix.py (or extend PeerDenyTests): for op in (OP_CREATE_PENDING with VALID_FIELDS, OP_ISSUE with a pending id) build `FakeConn(SIDECAR_UID, inbound=_frame({...}))`, call `handle_connection(conn, BROKER_UID, store, _config(), _sign_fn, _clock)`; assert reply == {"ok": False, "error": "peer not authorized"}, the inbound bytes were never read (the frame-untouched check used at test_isolated_signer_server.py:223), and `store._rows` is empty for BOTH ops. Mutation: delete :250-253 -> the create-pending frame is dispatched and a pending row appears -> RED. |
| `NM-MAN-12` | S | Trust-class inference attempt | apps/desktop/src-tauri/core/src/key_manifest.rs:25-37 — `ManifestKey` derives `Deserialize` with `pub trust_class: TrustClass` as a required field (no `Option`, no `#[serde(default)]`), and `TrustClass` (:17-22) is a closed `snake_case` enum with no `#[serde(other)]`, so a key lacking `trust_class`  | Rust, apps/desktop/src-tauri/core/src/key_manifest.rs tests: `fn nm_man_12_a_key_without_a_signed_trust_class_does_not_parse()` — `serde_json::from_value::<KeyManifest>(json!({"manifest_epoch":2,"root_key_id":"root-1","keys":[{"key_id":"k","public_key_hex":"00"*32,"valid_from_ms":1,"valid_to_ms":9,"key_epoch":1,"revoked":false,"allowed_protocols":["brops.governed-receipt-envelope.v1"]}]}))` (no `trust_class`) is `Err` whose message contains "missing field `trust_class`"; a second limb with `"trust_class":"staging"` is `Err` (unknown variant); body carries "NM-MAN-12". Optional second assertion |
| `NM-NORELAUNCH-01` | S | Post-start relaunch ban | engine/runtime/governed_acceptance.py:519-527 (`if state in (ledger.EXECUTION_STARTING, ledger.EXECUTING): self._advance(row, ledger.RECOVERY_REQUIRED, "no terminal proof for a started attempt"); raise _Refuse("not_completed", "attempt was already %s; moved to RECOVERY_REQUIRED, never relaunched")`) | Python, engine/tests/test_negative_matrix.py (importing the `_Case` harness) or engine/tests/test_governed_acceptance.py::IdempotenceTests. Subclass `_Executor` whose `run` appends the request then raises `KeyboardInterrupt` BEFORE calling `on_started` (the §5 crash 'after EXECUTION_STARTING commit, before the launcher confirms'; `except Exception` at :831 deliberately does not catch it, per the :683 precedent). Assert `acceptance_row(document)["state"] == gsl.EXECUTION_STARTING` and `row["lease_expires_at_ms"] > self.clock` (window still valid: LEASE_DURATION_MS = 210_000 at governed_supervis |
| `NM-NORELAUNCH-04` | S | Dual-destination edge | The ONLY producer of EXECUTING is engine/runtime/governed_supervisor_ledger.py:510-517 `mark_executing(... process_group_id, cgroup_id, execution_started_marker ...)`, reached only through the launcher's `on_started` callback engine/runtime/governed_acceptance.py:818-827 (which requires a `StartedEx | Python, engine/tests/test_negative_matrix.py via the `_Case` harness: executor whose `run` never calls `on_started` and raises `KeyboardInterrupt` (child gone before metadata was persisted). Assert the row is `gsl.EXECUTION_STARTING` with `process_group_id IS NULL AND cgroup_id IS NULL` (SELECT on governed_turn_acceptance), that no row was ever EXECUTING, and that a direct `gsl.record_completion(...)` on it raises `gsl.IllegalTransition` (no implicit edge); then a fresh trigger -> `not_completed`, `fresh.runs == []`, state == RECOVERY_REQUIRED, `load_attestation_state` None. Mutation: remove : |
| `NM-ORACLE-10` | S | Authority re-mints nonce | Engine: engine/runtime/challenge_authority.py:527-532 issue copies `request_nonce` verbatim from the stored row (never mints; PendingStore.create_pending :322-370 stores the broker's nonce); engine/runtime/governed_turn_open.py:489-490 `if request['request_nonce'] != payload['request_nonce']: raise  | Python, engine/tests/test_negative_matrix.py (new class NegativeMatrixOracleTests, reuse test_governed_turn_open.py's _payload/_document/_request/OpenService fixture). Inject: build a PendingStore row via challenge_authority.PendingStore.create_pending with request_nonce='authority-minted-nonce', issue via issue_challenge_document (sign_fn seam), then present the signed document in a brops.governed-turn-open.v1 request whose request_nonce is the broker's 'broker-nonce'. Assert: reply['reason'] == governed_turn_open.REFUSE_CONTEXT_MISMATCH, no staging/acceptance row written, and (positive contr |
| `NM-ORACLE-12` | S | Cross-authority sign | engine/runtime/bro_signature.py:236-292 `ARTIFACT_AUTHORITY` ("execution-lease": ISSUER :240; "evidence-event"/"evidence-head": EVIDENCE :279-282; "verifier-receipt": VERIFIER :284); :973-1006 `verify_artifact` -> :998-1000 `if artifact_type not in key.allowed_artifact_types: raise SignatureError(f" | Python, engine/tests/test_negative_matrix.py (NegativeMatrixOracleTests) using test_signature_authority.py's SignatureFixture (`self.artifact(authority, artifact_type, **fields)` + `self.load()`), or broctl.generate_key/build_registry/sign_payload as test_execution_leases.py:305-318 does. Inject (1): sign a `verifier-receipt` payload with the `issuer` key -> assert `verify_artifact(document, 'verifier-receipt', keys, now=…)` raises SignatureError containing '(issuer) may not sign verifier-receipt'. Inject (2): sign an `execution-lease` payload with the `evidence-recorder` key -> SignatureError |
| `NM-OUTPUT-05` | S | Recorder output vs receipt | engine/runtime/isolated_signer.py:771-826 `_verify_chain_handles` with `_CHAIN_AGREEMENT["execution_receipt_handle"] = ("brops.execution-receipt.v1", ("run_id","execution_attempt_id","output_handle"))` (753-764): a receipt document whose `output_handle` differs from the attested `output_handle` rais | Python, engine/tests/test_negative_matrix.py, new `NegativeMatrixOutputTests::test_nm_output_05_execution_receipt_output_handle_disagreeing_with_output_sha256_is_hash_mismatch`: `store, handles = _build_store(receipt_overrides={"output_handle": "9"*64})` (reuse test_isolated_signer helpers or inline the 3-doc store); `signer, _s, _h, recorder = _make_signer(prepared=(store, handles))`; `out = signer.sign_result(_request(_evidence(handles)))`; assert `out["artifact_type"] == REFUSAL_ARTIFACT_TYPE`, `out["reason"].startswith(isg.REASON_CHAIN_DISAGREEMENT + ":execution_receipt_handle.output_handl |
| `NM-PARITY-01` | S | `system` (raw UTF-8) | Python engine/runtime/brops_canonical.py:99-101 `system_bytes(system) = system.encode("utf-8")` / :126 `system_sha256`; Rust apps/desktop/src-tauri/core/src/receipt.rs:1224-1227 (`sha256_hex("You are Bro.".as_bytes())`), and the shipping preparation hashes `system.as_bytes()` (governed_prepare.rs pi | Python engine/tests/test_negative_matrix.py::test_nm_parity_01_system_formula_is_raw_utf8_across_unicode_emoji_and_nul: for V = the Unicode+emoji+NUL literal assert `bc.system_bytes(V) == V.encode("utf-8")` (NUL and all multibyte sequences preserved, len 31) and `bc.system_sha256(V) == PINNED`; Rust: add the same literal + hex to receipt.rs::brops_all_formula_parity_matches_python (or a sibling `nm_parity_01_...` fn) as `assert_eq!(sha256_hex(V.as_bytes()), PINNED)`. Mutation on either side (strip/rstrip('\x00')/NFC in system_bytes) -> RED. |
| `NM-PARITY-02` | S | `history` (JCS `[{content,role}]`) | Python engine/runtime/brops_canonical.py:105-111 `history_bytes` / :130 `history_sha256`; Rust apps/desktop/src-tauri/core/src/receipt.rs:1237-1256 (serde_json::to_vec of Vec<BTreeMap<&str,&str>>) and apps/desktop/src-tauri/src/ai.rs:2691 `governed_history_sha256` (ai.rs:4378 asserts it equals the b | Python engine/tests/test_negative_matrix.py::test_nm_parity_02_history_formula_pins_empty_history_and_unicode: assert `bc.history_bytes([]) == b"[]"`, `bc.history_sha256([]) == "4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945"`, and re-assert the Unicode vector fbd46857...; Rust: sibling assertion in receipt.rs (empty Vec -> b"[]" -> same hex) and `ai::governed_history_sha256(&[])` equal to it. Mutation (e.g. Python emitting `null`/omitting empty list, Rust special-casing empty) -> RED. |
| `NM-PARITY-09` | S | Time-unit parity | Rust apps/desktop/src-tauri/core/src/governed_verification.rs:772-773 `MIN_GOVERNED_MS = 1`, `MAX_GOVERNED_MS = (1<<53)-1`, enforced at 843-854 on `now_ms`, `challenge_accepted_at_ms`, `completed_at_ms`; bare-integer JCS at 163-170. Python engine/runtime/isolated_signer.py:276-278 `_is_u64_ms` (= `0 | Python engine/tests/test_negative_matrix.py::test_nm_parity_09_ms_integers_serialize_identically_on_both_sides: assert the exact JCS bytes and pinned sha256 above for the three-field object (boundary value 9007199254740991 included, bare integer, no exponent, no quotes); Rust: sibling in governed_verification.rs asserting `serde_json::to_vec` of the same sorted Map (or a `ReceiptEnvelope` with those `_ms` values via `payload_jcs()`) yields identical bytes/hash, and `MAX_GOVERNED_MS == 9_007_199_254_740_991`. The Python test should additionally document the range gap (`_is_u64_ms(2**53)` is Tru |
| `NM-REG-13` | S | Revocation seconds-not-ms | engine/runtime/challenge_key_registry.py:281-282 (`if revoked_at < entry["valid_from_ms"]: raise _Reject("...revoked_at_ms precedes valid_from_ms")`) — the consistency limb; the range predicate `_is_ms` at :195-198 accepts 1..2^53-1 and does NOT distinguish a 10-digit seconds value | Python, engine/tests/test_negative_matrix.py::NegativeMatrixRegistryTests::test_nm_reg_13_a_seconds_valued_revoked_at_ms_is_refused — build the key with a REALISTIC ms window (do not use the module fixture scale, measured to admit the value): `entry = _key(valid_from_ms=1_700_000_000_000, valid_to_ms=1_700_000_200_000, revoked=True, revoked_at_ms=1_700_000_100)` (10 digits vs 13); `snapshot, reason = _resolve(_document([entry]))`; assert `snapshot is None`, `reason == registry.REGISTRY_UNKNOWN`, `self.assert_no_usable_key(snapshot, reason)`; body string "NM-REG-13". Goes RED when challenge_key |
| `NM-REG-15` | S | Registry bounds | engine/runtime/challenge_key_registry.py:330-331 (`if len(entries) > MAX_REGISTRY_KEYS: raise _Reject(...)`, constant :74) and :397-399 (`document_bytes = canonical_bytes({payload, root_sig}); if len(document_bytes) > MAX_REGISTRY_DOC_BYTES: return None, REGISTRY_UNKNOWN`, constant :77); both surfac | Python, engine/tests/test_negative_matrix.py::NegativeMatrixRegistryTests — two tests (one fault each, per plan §0.1): (1) `test_nm_reg_15_more_than_256_keys_is_refused`: `keys = [_key(challenge_key_id="ck-%d" % i) for i in range(registry.MAX_REGISTRY_KEYS + 1)]`; `snapshot, reason = _resolve(_document(keys))`; assert `snapshot is None`, `reason == registry.REGISTRY_UNKNOWN`, `assert_no_usable_key`. (2) `test_nm_reg_15_a_document_over_256_kib_is_refused`: `doc = _document(); doc["root_sig"] = "s" * (registry.MAX_REGISTRY_DOC_BYTES + 1)`; `snapshot, reason = _resolve(doc)`; same assertions; and |
| `NM-SCOPE-04` | S | Protocol out-of-scope | apps/desktop/src-tauri/core/src/key_manifest.rs:257-259 (`if !k.allowed_protocols.iter().any(\|p\| p == protocol) { return Err(ManifestError::ProtocolNotAllowed); }`) inside `resolve_production_key` (241-261); apps/desktop/src-tauri/core/src/manifest_authority.rs:72-79 (`ManifestReceiptKeyAuthority: | Rust, apps/desktop/src-tauri/core/src/manifest_authority.rs tests: `fn nm_scope_04_a_key_whose_allowed_protocols_exclude_the_receipt_protocol_is_unavailable()` — manifest with `prod-1` (allowed_protocols [PROTO]) and `scoped-1` Production key with `allowed_protocols: vec!["brops.other.v1".into()]`, root-signed, floor at epoch; `ManifestReceiptKeyAuthority::new(.., PROTO, 1000)`; assert `matches!(auth.resolve("scoped-1").unwrap(), KeyResolution::Unavailable(_))` and `prod-1` still `Trusted` (positive control); body carries "NM-SCOPE-04". Mutation: delete key_manifest.rs:257-259 → `scoped-1` res |
| `NM-SCOPE-06` | S | Unsigned-field trust attempt | apps/desktop/src-tauri/core/src/receipt.rs:326-347: `pub struct Parsed { fields: BTreeMap<String,String>, canonical_bytes: Vec<u8> }` (private fields), `impl Parsed` exposes only `pub fn key_id(&self)` (346) and `pub fn verify(self, ..)` (358); `Debug` redacted to key_id + bytes_len (332-340). recei | Rust, apps/desktop/src-tauri/core/src/receipt.rs tests: `fn nm_scope_06_parsed_exposes_only_key_id_before_verification()` — (1) `let src = include_str!("receipt.rs")`, slice the `impl Parsed {` block, collect `pub fn` names, assert == ["key_id", "verify"]; (2) `parse_strict(&env)` Debug contains key_id and none of the other 22 field values (extend the existing Debug assertion). Body carries "NM-SCOPE-06". Mutation: add `pub fn workspace_id(&self) -> &str` to `impl Parsed` → (1) RED. Optional Python twin in engine/tests/test_negative_matrix.py reading the same file if a source-reading Rust test |
| `NM-SCOPE-08` | S | Create-pending context divergence | apps/desktop/src-tauri/core/src/receipt_store.rs:357-362 (`else if nonce_state.issued_request_sha256() != Some(turn.expected.request.request_sha256().as_str()) { Some("challenge request_sha256 does not match the expected request envelope") }` → Blocked at 380-400); pre-store at receipt_store.rs:134- | Rust, apps/desktop/src-tauri/core/src/receipt_store.rs tests: `fn nm_scope_08_a_challenge_pre_stored_under_another_workspace_id_blocks_every_turn()` — `let fx = Fx::new(now, "nonce-A")`; create conversation; `issue_challenge(&conn, &conv.id, &IssuedRequest { workspace_id: "ws-2", ..fx.issued() }, now)`; `vrec(&conn, &fx, &env, &sig, now, TrustClass::Development)` → `matches!(out, ReceiptOutcome::Blocked { error, .. } if error.contains("challenge request_sha256 does not match"))`; assert `COUNT(messages)==0`, `COUNT(receipt_ids_seen)==0`, `consumed_at IS NOT NULL`. Sibling for `install_id: "ins |
| `NM-TCB-01` | S | Login-writable executor | apps/desktop/src-tauri/core/src/tcb_integrity.rs:213-233 `verify_tcb_integrity` → verify_artifact :263-269 returns `TcbViolation::WritableByUntrusted` when `is_world_or_group_writable \|\| writable_by_login_or_runtime`; caller apps/desktop/src-tauri/broker/src/main.rs:296-320 maps any Err to `fail_c | Rust, apps/desktop/src-tauri/core/src/tcb_integrity.rs tests module: `fn nm_tcb_01_login_writable_executor_image_refuses_start()` — `let m = manifest(); let mut fs = clean_fs(); fs.files.get_mut("/opt/brops/contained-executor.bin").unwrap().writable_by_login_or_runtime = true; let err = verify_tcb_integrity(&m,&fs,&runtime_uids(),LOGIN).unwrap_err(); assert!(matches!(err, TcbViolation::WritableByUntrusted{..})); assert_eq!(err.logical_name(), "contained-executor.bin");` with the string "NM-TCB-01" in the body. Mutation: delete the `writable_by_login_or_runtime` disjunct at :263 → RED. |
| `NM-TCB-03` | S | Wrong-owner TCB | apps/desktop/src-tauri/core/src/tcb_integrity.rs:252-262 (`WrongOwner` when owner ∈ runtime_uids ∪ {login} or ≠ the manifest's expected uid) and :290-311 (`AncestorWrongOwner` / `AncestorWritable` for every ancestor up to `/`, from `ancestor_dirs` :319-330); caller apps/desktop/src-tauri/broker/src/ | Rust, apps/desktop/src-tauri/core/src/tcb_integrity.rs tests: `fn nm_tcb_03_wrong_owner_launcher_signer_or_config_refuses_start()` looping over `["/opt/brops/privileged-launcher.bin","/opt/brops/isolated-signer.bin","/opt/brops/supervisor.config"]`: `let mut fs = clean_fs(); fs.files.get_mut(p).unwrap().owner_uid = LOGIN; match verify_tcb_integrity(&manifest(),&fs,&runtime_uids(),LOGIN).unwrap_err() { TcbViolation::WrongOwner{expected_uid,actual_uid,logical_name,..} => { assert_eq!((expected_uid,actual_uid),(ROOT,LOGIN)); assert_eq!(logical_name, <name>) } other => panic!(..) }`; plus the ance |
| `NM-TCB-08` | S | Verifier == renderer | apps/desktop/src-tauri/core/src/windows_broker.rs:229-236 — the (b) loop visits `Principal::Broker` first (RUNTIME_PRINCIPALS :37-40), so `broker == login` yields `Err(PrincipalIsLoginSid(Principal::Broker))`. Same no-production-caller caveat (win-broker/src/lib.rs:20 re-export only). Linux: no brok | Rust, apps/desktop/src-tauri/core/src/windows_broker.rs tests: `fn nm_tcb_08_verifier_broker_equal_to_login_renderer_blocks() { let mut s = sids(); s.broker = s.login.clone(); assert_eq!(verify_distinct_principals(&s), Err(WindowsBrokerViolation::PrincipalIsLoginSid(Principal::Broker))); }` with "NM-TCB-08" in the body. Mutation: remove the `*s == sids.login` branch (:233-235) → RED. Note in the test that the verifier has no production caller so this proves the predicate only. |
| `NM-TCB-14` | S | Launcher extra argv/env/FD | argv/env: apps/desktop/src-tauri/launcher/src/main.rs:566-569 `if args.len() != 3 \|\| std::env::vars_os().next().is_some() { return Err(Refusal::BadArgv) }` — the FIRST check in real_main. Extra FD: apps/desktop/src-tauri/core/src/fd_lifecycle.rs:160-165 `UnexpectedFd(fd)` for any fd outside 0..=6, | Two parts. (1) Rust: add "NM-TCB-14" to fd_lifecycle.rs:273 `rejects_a_descriptor_at_or_above_7` (or launcher main.rs:1270) name/body — already exercises UnexpectedFd(7). (2) Shell, engine/ci/live/run_live_turn.sh: `env -i "$TCB/privileged-launcher.bin" "$TCB/executor.lease" "$TCB/contained-executor.bin" "$CGROUP_ARG" extra </dev/null >/dev/null 2>"$ERR"; [ $? = 1 ] && grep -qF 'BadArgv' "$ERR"` and `env -i FOO=1 "$TCB/privileged-launcher.bin" "$TCB/executor.lease" "$TCB/contained-executor.bin" "$CGROUP_ARG" …` → same assertion; label `NM-TCB-14 NEGATIVE`. Mutation: change `!= 3` to `< 3` or d |
| `NM-TCB-21` | S | Config mutation (generation_config) | engine/runtime/governed_staging_upload.py:857-859: after reassembly, `if recomputed != turn[staging.ARTIFACT_DIGEST_COLUMN[artifact]]: raise _Refuse(REFUSE_HANDLE_NOT_CHALLENGE, "assembled digest is not the challenge's committed digest")` (REFUSE_HANDLE_NOT_CHALLENGE = "handle_not_challenge" :157);  | Python, engine/tests/test_negative_matrix.py (or test_governed_staging_upload.py): `test_nm_tcb_21_staged_generation_config_that_differs_from_the_challenge_digest_is_handle_not_challenge` — `sid = self.open_session("generation_config", GENCFG_BYTES); self.send_all_chunks(sid, GENCFG_BYTES); self.conn.execute("DROP TRIGGER trg_governed_turn_staging_immutable_binding"); self.conn.execute("UPDATE governed_turn_staging SET generation_config_sha256 = ? WHERE challenge_handle = ?", (sha(b"a different generation_config"), self.turn.challenge_handle)); self.assertEqual(self.refuse(self.final_request(s |
| `NM-TERM-02` | S | Nonce not retryable | apps/desktop/src-tauri/core/src/receipt_store.rs:218-223 (`record_pre_verification_block` sets `consumed_at`), :296-299 (`Some((_, Some(_consumed), _)) => NonceState::Replay`), :353-354 (`!nonce_state.is_consumed()` ⇒ blocked verdict), :656 ("request_nonce already consumed (replay)") | Rust, apps/desktop/src-tauri/core/src/receipt_store.rs `mod tests`, `fn nm_term_02_a_nonce_spent_by_a_terminal_block_is_a_replay_on_re_ask()`: `let fx = Fx::new(now, "nonce-A"); seed_turn(&conn, &fx, now); record_pre_verification_block(&conn, "nonce-A", "governed engine sidecar timed out", now).unwrap();` then `let (env, sig) = fx.wire_of(&fx.fields("receipt-1")); let out = vrec(&conn, &fx, &env, &sig, now, TrustClass::Development).unwrap();` assert `matches!(out, ReceiptOutcome::Blocked { ref error, .. } if error == "request_nonce already consumed (replay)")`, `count(messages) == 0`, `count(r |
| `NM-TERM-04` | S | No partial output | apps/desktop/src-tauri/core/src/governed_output_pull.rs:611-622 (`pull_output`: `fetch(&request)?` / `pull.accept(&reply)?` on every read; `finish()` only after the loop), :528-553 (`accept`: a §4.10(f) refusal frame ⇒ `Err(PullError::Refused(..))`, parsed at :412-470/:436), :560-571 (`finish` is th | Rust, apps/desktop/src-tauri/core/src/governed_output_pull.rs `mod tests`, `fn nm_term_04_a_refusal_after_a_served_chunk_yields_no_bytes()`: build an output longer than one stride (as ::a_multi_chunk_output_reassembles_and_verifies at 762 does), `let sha = sha256_hex(&output)`; a fetch closure that answers seq 0 with `ok_reply(token, 0, first_chunk, false)` and every later request with `refused_reply("stream_expired")`, counting calls; assert `pull(&output, &sha, fetch).unwrap_err() == PullError::Refused(StreamRefusal::StreamExpired)`, that exactly two requests were made, and `err.to_turn_reas |
| `NM-TERM-08` | S | UNSEEN → BLOCKED | engine/runtime/governed_supervisor_ledger.py:84-93 (`LEGAL_PREDECESSORS[BLOCKED] = (ACCEPTED_PREPARED, LEASE_READY)` — no UNSEEN/absent), :459-494 (`_advance`: an `UPDATE ... WHERE execution_attempt_id = ? AND state IN (...)`; zero rows and no row ⇒ `raise NotFound` :492-493 — an UPDATE can never cr | Python, engine/tests/test_negative_matrix.py (e.g. a new `NegativeMatrixTerminalTests(_MatrixCase)`), `test_nm_term_08_a_pre_row_refusal_cannot_write_a_blocked_acceptance_row`: `conn = _conn()`; `with self.assertRaises(gsl.NotFound): gsl.advance(conn, "att-unseen", gsl.BLOCKED, 10, failure_reason="peer_denied")`; `self.assertEqual(conn.execute("SELECT COUNT(*) FROM governed_turn_acceptance").fetchone()[0], 0)`; `self.assertEqual(gsl.LEGAL_PREDECESSORS[gsl.BLOCKED], (gsl.ACCEPTED_PREPARED, gsl.LEASE_READY))` (UNSEEN is not a predecessor, asserted against the matrix rather than prose); `self.ass |
| `NM-TIME-01` | S | Unit confusion | No pure RANGE reject of a 10-digit value exists on either side — engine/runtime/isolated_signer.py:276-278 `_is_u64_ms` is `0 <= v < 2**64` (MEASURED `_is_u64_ms(1_700_000_000) == True`; same predicate at governed_supervisor_ledger.py:158-159 and governed_staging_ledger.py), and Rust apps/desktop/sr | Python, engine/tests/test_negative_matrix.py::NegativeMatrixTimeTests (reuse test_isolated_signer.py's `_make_signer`/`_evidence`/`_request`, whose fixture has requested_at=NOW_MS-5000 and challenge_accepted_at_ms=NOW_MS-3000, 13-digit). Inject ONE fault: `ev["completed_at"] = 1_700_000_000` (10-digit seconds). Assert `signer.sign_result(_request(ev))["reason"] == signer.REASON_TIMESTAMP_INVALID` and `recorder.signed_messages == []` (no envelope). Goes RED when the `requested_at > completed_at` limb at isolated_signer.py:722-723 is removed. Optional Rust twin in governed_verification.rs tests: |
| `NM-TIME-02` | S | Overflow / negative / zero | engine/runtime/isolated_signer.py:462-464 (`for field in EVIDENCE_TS_FIELDS: if not _is_u64_ms(evidence[field]): raise _Refuse(REASON_MALFORMED)`) with `_is_u64_ms` at :276-278 = `isinstance int, not bool, 0 <= value < 2**64`; same predicate in governed_supervisor_ledger.py:158-159 (applied to compl | Python, engine/tests/test_negative_matrix.py::NegativeMatrixTimeTests, one fault per assertion using `_sign_request()`: (a) `request["evidence"]["completed_at"] = -1` ⇒ `signer.validate_sign_request(request)` raises `signer._Refuse` with `.reason == signer.REASON_MALFORMED`; (b) same with `2**64` (overflow); (c) same with `True` (bool smuggled as int). Goes RED when the EVIDENCE_TS_FIELDS loop at isolated_signer.py:462-464 is removed. Do NOT assert `0` or `2**53` ⇒ `malformed` at the Python signer — MEASURED `_is_u64_ms(0) == True`, `_is_u64_ms(2**53) == True`, `_is_u64_ms(2**64-1) == True`: t |
| `NM-TIME-12` | S | Wall-clock NTP step (pre-launch) | engine/runtime/governed_acceptance.py:788 — `state = ledger.gate_and_start(self.conn, attempt, self.clock_ms())` reads the wall clock FRESH at the gate, distinct from the LEASE_READY persist read at :775 and the acceptance read at :596; engine/runtime/governed_supervisor_ledger.py:544-565 `gate_and_ | Python, engine/tests/test_governed_acceptance.py (it needs `_Case`'s driver + `clock_ms` injection; test_negative_matrix.py has no driver fixture): clock returns NOW for reads 1 and 2 (acceptance and LEASE_READY persist) and NOW + gsl.LEASE_DURATION_MS + 1 from read 3 (the gate) onward. Assert `assertRefused(self.trigger(document, driver=self.driver(clock_ms=clock)), "lease_expired")`, `self.acceptance_row(document)["state"] == gsl.EXPIRED`, `failure_reason == "lease_expired"`, and `self.executor.runs == []`. Goes RED if :788 is changed to reuse the persist-time instant instead of re-reading t |
| `NM-TIME-19` | S | Evidence seconds leak | Structural absence, which is the control the row asks to pin: `grep -n "issued_at_epoch" engine/runtime/governed_*.py engine/runtime/isolated_signer.py engine/runtime/challenge_*.py` → 0; tree-wide `grep -rn "issued_at_epoch" apps/desktop/src-tauri --include=*.rs` → only audit-signer/provision (lega | Python, engine/tests/test_negative_matrix.py::NegativeMatrixTimeTests. (a) Field-set assertions: `"issued_at_epoch" not in signer.EVIDENCE_FIELDS`, `signer.EVIDENCE_TS_FIELDS == ("requested_at", "completed_at", "challenge_accepted_at_ms")`, `set(gsl.DERIVED_EVIDENCE_FIELDS) == {"evidence_final_event_hash", "evidence_event_count", "evidence_last_sequence", "evidence_head_sequence"}`. (b) Behavioural: a `_sign_request()` whose evidence carries an extra `issued_at_epoch` key is refused `REASON_MALFORMED` by `validate_sign_request` (the exact-key rule is what keeps the seconds field out of `_check |
| `NM-XBIND-03` | S | Lease handle mismatch | engine/runtime/isolated_signer.py:771-812 `_verify_chain_handles`: loops `EVIDENCE_CHAIN_HANDLE_FIELDS` (:118-122: record_handle, lease_handle, execution_receipt_handle), `raw = self._store.read_verified(handle)` (:798) -> `if raw is None: raise _Refuse(REASON_HANDLE_MISSING)` (:800); `ArtifactStore | Python, engine/tests/test_negative_matrix.py, new class `NegativeMatrixCrossBindingTests(_MatrixCase)` reusing the fixtures of engine/tests/test_isolated_signer.py (`_make_signer`, `_evidence`, `_request`, `_Recorder`). `test_nm_xbind_03_a_lease_handle_that_names_no_stored_lease_is_refused`: build signer + evidence, set `ev["lease_handle"] = "e"*64` (a well-formed 64-hex naming bytes never stored; `ArtifactStore.put` :285-297 refuses a lying handle so a stored-but-mismatched lease cannot be constructed -- the reachable literal is `handle_missing`, `hash_mismatch` is the `read_verified` corrupt |
| `NM-CONC-03` | M | Evidence-floor serialization | engine/runtime/governed_supervisor_ledger.py:247-266 (`_Tx.__enter__`: `if self._conn.in_transaction: raise LedgerError("ledger operation must own its BEGIN IMMEDIATE")` then `BEGIN IMMEDIATE`); :774-864 (`_evidence_floor_cas`: per-(install,task) row read, equal head -> idempotent/EvidenceFork, inst | Python, engine/tests/test_negative_matrix.py (NegativeMatrixConcurrencyTests) `test_nm_conc_03_concurrent_same_chain_heads_commit_exactly_one_floor_row`: file-backed ledger `gsl.open_ledger(tmp/'sup.db')` (busy_timeout 5000 at :212); drive two attempts to EXECUTING on the same install under DIFFERENT task_ids (att-1/task-1, att-2/task-2) each with the identical derived head (head 12, count 3, last 3, H_C); two threads, each its own `open_ledger` connection, `threading.Barrier(2)`, each calls `gsl.record_completion(conn, att, _produced(), now, derived=...)`. Assert: neither thread raised `sqlit |
| `NM-CONC-04` | M | Concurrent re-anchor vs extension | engine/runtime/governed_supervisor_ledger.py:247-266 (`_Tx` BEGIN IMMEDIATE) + :826-864 (`_evidence_floor_cas`: `if head_sequence < highest: raise StaleEvidence(...)`, `if head_sequence == highest: raise EvidenceFork(...)`, else `UPDATE governed_evidence_head_floor SET highest_head_sequence=?, event | Python, engine/tests/test_negative_matrix.py `test_nm_conc_04_a_re_anchor_racing_an_extension_commits_one_and_re_evaluates_the_other`: file-backed ledger; seed att-1 completion (install-1/task-1, head 12, count 3, last 3, H_C); prepare att-C (task-1, head 13, count 3, last 3, H_C = unchanged re-anchor) and att-D (task-1, head 14, count 4, last 4, 'd'*64 = extension), both EXECUTING. Limb (a) deterministic orderings on two connections: D-then-C -> C raises `gsl.StaleEvidence` (judged on the NEW floor 14), floor row == (14,4,4,'d'*64); C-then-D -> both `gsl.CREATED`, floor == 14. Limb (b) genuin |
| `NM-CRASH-06` | M | `LEASE_READY` found on restart, lease still valid | engine/runtime/governed_acceptance.py:529-531 (`_drive`: LEASE_READY -> `_gate_and_execute`) and :787-800 (`ledger.gate_and_start(self.conn, attempt, self.clock_ms())` -- a FRESH wall-clock read); engine/runtime/governed_supervisor_ledger.py:544-565 (`gate_and_start`: gate over the PERSISTED `lease_ | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; first trigger with a clock that raises `KeyboardInterrupt` on its THIRD read (reads: 1 = acceptance clock, 2 = `mark_lease_ready`, 3 = the gate) -> assertRaises; assert row LEASE_READY and `executor.runs == []`; model the restart: `self.conn.close(); self.conn = gsl.open_ledger(self.ledger_path)`; fresh driver with `clock_ms = lambda: NOW + 1_000` (inside the window, remaining >= MIN_LAUNCH_REMAINING_MS) -> `status == signed`; assert `executor.runs == 1`, row COMPLETED; a further trigger returns the identical frame and `executor |
| `NM-CRASH-12` | M | after receipt/evidence, before terminal record | WIRE PATH: engine/runtime/governed_supervisor_server.py:769-860 (`complete_governed_run`: reads the RECORDER's chain :793-806, builds + publishes lease/record/receipt through create-if-absent `publish_artifact` :812-824, then `ledger.record_completion` :826); comment :826-833 'a failure here is reso | Python, engine/tests/test_governed_supervisor_server.py::CompleteRunDispatchTests: `attempt = self._accept(); self._to_executing(attempt)` (recorder chain present); first `complete-run` through a `publish_artifact` seam that raises BEFORE storing anything (crash after evidence, before the terminal record) -> reply `ok == False`, reason REFUSE_MALFORMED_STATE, `gsl._current_state == EXECUTING`, completion rows 0; re-send the byte-identical `complete-run` with the normal seam -> `ok`, `recorded == gsl.CREATED`, state COMPLETED, `terminal_record_handle` set; then `launch-gate` and `execution-star |
| `NM-CRASH-13` | M | after terminal record, before ledger `COMPLETED` | engine/runtime/governed_supervisor_server.py:817-826 (the record is published to the store BEFORE `record_completion` runs), engine/runtime/governed_supervisor_ledger.py:919-975 (`record_completion`: one BEGIN IMMEDIATE holding the completion INSERT, the floor, and `_advance(COMPLETED, terminal_reco | Python, engine/tests/test_governed_supervisor_server.py::CompleteRunDispatchTests: drive to EXECUTING; first `complete-run` with a `publish_artifact` wrapper that stores each blob and raises once the record blob has been stored (crash after the terminal record is durable, before `record_completion`) -> `ok == False`, state EXECUTING, completion rows 0, the record blob IS in the store; re-send the identical `complete-run` with the normal seam -> `ok`, `recorded == CREATED`, state COMPLETED, `terminal_record_handle == sha256(record bytes)`, and the store still holds exactly one record blob (idem |
| `NM-CRASH-14` | M | after evidence-head floor commit, before envelope response | Floor commit: engine/runtime/governed_supervisor_ledger.py:774-863 (`_evidence_floor_cas`, case B idempotent limb :812-817) executed inside `record_completion`'s tx at :956-959, so this cut leaves a COMPLETED row + floor row. Retry: engine/runtime/governed_acceptance.py:540-553 (`_drive` -> `_attest | Python, engine/tests/test_governed_acceptance.py: `ready_turn()`; driver whose `sign_result` raises `KeyboardInterrupt` on its first call (crash after the floor+completion commit, before the envelope reply) -> assertRaises; assert state COMPLETED, `governed_turn_completion` count 1, floor row `highest_head_sequence == 7` (the case's first head, `next_head_sequence` at :232); retrigger with a normal driver -> `status == signed`; assert the floor table is byte-identical before/after (one row, same head/content), `executor.runs == 1`, and a third trigger returns the identical frame. |
| `NM-FRAME-12` | M | Oversize stdout | apps/desktop/src-tauri/core/src/governed_sidecar.rs:122 `pub const MAX_STDOUT_BYTES: u64 = 9 * 1024 * 1024`; `read_stdout_capped` :462-466 (`reader.take(MAX_STDOUT_BYTES).read_to_end`) — TRUNCATES at the cap (docstring :453-456); the Block then follows from `round_trip` :771-772 `serde_json::from_st | Rust, apps/desktop/src-tauri/core/src/governed_sidecar.rs tests mod (:808). Build `GovernedSidecar::as_calling_principal(<interpreter>, <script>, temp cwd, trust())` where the child writes MAX_STDOUT_BYTES + 1 bytes of `x` to stdout and exits 0 (a tiny script in the test's temp dir run by the platform shell, or `std::env::current_exe()` under a self-test env var the way the crate's other subprocess tests spawn real children, e.g. :1382). Call `round_trip(&submit_frame())`; assert `Err(e)` with `e.contains("could not parse bridge-result")` and that no `Value` came back; then assert the next lay |
| `NM-FRAME-19` | M | Unparseable stdout | apps/desktop/src-tauri/core/src/governed_sidecar.rs:771-772 `serde_json::from_str(stdout.trim()).map_err(\|e\| format!("could not parse bridge-result ({e})"))` — empty and garbage stdout both return `Err`, never a document (docstring :692-695: 'Every local failure — spawn, read, deadline, unexpected | Rust, apps/desktop/src-tauri/core/src/governed_sidecar.rs tests mod (:808). For each fixture stdout ∈ {"", "garbage", "{\"ok\":"}: spawn a real child via `GovernedSidecar::as_calling_principal(<interpreter that echoes the fixture and exits 0>, <script>, temp cwd, trust())` (a temp-dir script run by the platform shell, or `std::env::current_exe()` under a self-test env var), call `round_trip(&submit_frame())`, assert `Err(e)` with `e.contains("could not parse bridge-result")` and never `Ok(Value)`. Then, next layer (the :1125 pattern): a `SubmitTransport` whose `call` returns that `Err(String)` |
| `NM-FS-06` | M | Partial-read TOCTOU | engine/runtime/brops_evidence_store.py:246-299 `EvidenceStore.publish`: `tempfile.mkstemp(dir=self.root, prefix=".tmp-", suffix=".part")` (254) → write+flush+`os.fsync` (258-261) → re-read + size/byte verify (278-280) → `handle = sha256_hex(written)` (281) → `_atomic_publish` (301-311) → `atomic_lin | Python, engine/tests/test_negative_matrix.py (NegativeMatrixFilesystemTests): `test_nm_fs_06_a_handle_is_unreadable_until_the_exclusive_publish_completes` — `data=b"mid-write"; h=sha256_hex(data)`; `real = store_module.atomic_link_or_create`; wrapper(tmp, target, d): assert `not store.has(h)`; `assertRaisesRegex(EvidenceStoreError, "not in store", store.read, h)`; assert `[p.name for p in root.iterdir() if p.read_bytes()==data] == [tmp.name]` and `tmp.name.startswith(".tmp-") and tmp.name.endswith(".part")`; return `real(tmp, target, d)`; with `unittest.mock.patch.object(store_module, "atomic_ |
| `NM-MAN-06` | M | Unknown attestation key id | apps/desktop/src-tauri/core/src/governed_verification.rs:909-911 (`if envelope.supervisor_attestation_key_id != keys.supervisor_attestation_key_id { return Err(TurnReason::UpstreamBlocked) }`, step 1 of `verify_and_accept`, before any signature check), where `keys.supervisor_attestation_key_id` is o | Rust, two assertions. (a) apps/desktop/src-tauri/core/src/governed_verification.rs tests: `fn nm_man_06_an_attestation_key_id_not_pinned_from_the_manifest_blocks()` — take `envelope(&f)`, set `supervisor_attestation_key_id = "sup-att-unknown"`, re-sign `env.payload_jcs()` with `signing_key(7)` (as :1562 does), keep the genuine `attest(&f)`; assert `verify_and_accept(&expected(&f), &env, &resigned, &a, &keys(&f), OUTPUT, &CTX, &mut ledger, &fresh())` is `Err(TurnReason::UpstreamBlocked)` and `ledger` holds no accepted row; body carries "NM-MAN-06". Mutation check: deleting :909-911 lets the re- |
| `NM-PARITY-07` | M | full-field receipt + governed envelope | 21-field receipt: engine/runtime/brops_canonical.py:183 `receipt_envelope_bytes` <-> apps/desktop/src-tauri/core/src/receipt.rs:253 `jcs_bytes`. §4.9 governed envelope (23 fields, 17 strings + 6 bare integers): engine/runtime/isolated_signer.py:243-260 `_jcs_bytes` (json.dumps sort_keys, separators= | Python engine/tests/test_negative_matrix.py::test_nm_parity_07_governed_envelope_jcs_and_sha256_match_the_broker: build the 23-key payload from fixed literals (the 17 §4.9 strings incl. a non-ASCII `task_id`, plus output_bytes, challenge_accepted_at_ms, completed_at_ms, evidence_event_count, evidence_last_sequence, evidence_head_sequence), assert `isolated_signer._jcs_bytes(payload)` equals the exact expected bytes and `_sha256_hex(...)` equals one pinned hex; also re-assert the 21-field pair (bc.receipt_envelope_bytes -> 37075e5f...). Rust apps/desktop/src-tauri/core/src/governed_verification |
| `NM-TCB-13` | M | Launcher wrong caller | apps/desktop/src-tauri/launcher/src/main.rs:859-880 `verify_invoker_is_recorder`: getresuid/getresgid, `if ruid != recorder_uid \|\| rgid != recorder_gid { return Err(Refusal::TcbIntegrity("invoker-not-recorder")) }`; called at :598 (step 3) after argv (:566-569), lease (:578) and attested-config (: | Shell, engine/ci/live/run_live_turn.sh (after `$TCB/executor.lease` and the pin manifest exist; script is root): `echo "== NEGATIVE NM-TCB-13: a caller that is not the recorder must be refused by the launcher =="; ERR=$(mktemp); env -i "$TCB/privileged-launcher.bin" "$TCB/executor.lease" "$TCB/contained-executor.bin" "$CGROUP_ARG" </dev/null >/dev/null 2>"$ERR"; rc=$?; [ "$rc" = 1 ] && grep -qF 'TcbIntegrity("invoker-not-recorder")' "$ERR" && [ -z "$(ls -A "$REPORT" 2>/dev/null \| grep tcb13)" ] \|\| { echo "NM-TCB-13: RED"; cat "$ERR"; exit 1; }` — asserting the exact reason string (per the s |
| `NM-TCB-25` | M | Allowlist mutated post-execution | The property is the ABSENCE of any allowlist read on the verification path, measured: `execution_allowlist` exists only on `AcceptanceConfig` (engine/runtime/governed_acceptance.py:336-349) and is read exactly once, at acceptance (:646); engine/runtime/isolated_signer.py has no execution allowlist ( | Python, engine/tests/test_negative_matrix.py: `test_nm_tcb_25_an_allowlist_mutated_after_signing_leaves_the_record_verifying_identically` — (1) drive one turn to a signed envelope through the real §5 `AcceptanceDriver` with `execution_allowlist=frozenset({sha(GENCFG_BYTES)})` and the real `isolated_signer` (reuse test_governed_chain_e2e's harness); capture `envelope_payload`, `signature`, and the derived `generation_config_sha256`; (2) construct a NEW `AcceptanceConfig` with `execution_allowlist=frozenset()` and a fresh signer over the SAME keys/store; assert `receipt_key.verify(supervisor_can |
| `NM-ACL-11` | L | Any non-owner → artifact | engine/runtime/brops_evidence_store.py:262-274 (`if os.name == "posix": ... os.chmod(tmp, 0o640)` before the atomic link; comment "Owner rw + GROUP read (0640) ... World stays denied"); :187-224 `atomic_link_or_create` never overwrites an existing target (EEXIST ⇒ False). Windows: no chmod branch; t | Linux CI (L): engine/tools/brops_isolation_prover.py — new probe `attack_overwrite_artifact(path)` = `os.open(path, os.O_WRONLY)` on an EXISTING published artifact (`$STORE_DIR/<handle>` from the positive-control round-trip), scored `PermissionError` ⇒ DENIED, `FileNotFoundError` ⇒ INCONCLUSIVE, success ⇒ BREACH, with a positive control that the owner (`brops-supervisor`) CAN open it for write; engine/ci/isolation_proof.sh — run that probe via `sudo -u brops-signer` (a `brops-store` member that is not the owner) and an `expect_prover`-style label carrying the ID. The `.py` carrier for the matr |
| `NM-ACL-14` | L | Login/sidecar → acceptance ledger / staging | Ledger: engine/runtime/governed_supervisor_ledger.py:194-200 (`os.makedirs(parent, mode=0o700, exist_ok=True)`; `if fresh: os.chmod(db_path, 0o600)`; docstring :187-189 "no other uid may write it"). Staging: engine/runtime/governed_staging_upload.py:444 `return harden_private_dir(pathlib.Path(sessio | Linux CI (L): engine/ci/isolation_proof.sh — start the supervisor with an explicit ledger DB path and staging root under `$B/state` (tighten :52 from 750 to 700 for those), export `BROPS_PROVE_LEDGER_DB` and `BROPS_PROVE_STAGING_DIR` in `prover_env` (:164-168), extend the `owner_can_read` positive control (:137-149) to them; engine/tools/brops_isolation_prover.py — attack 5 `5_ledger_no_read_write` = `attack_read_file(ledger_db)` (:326) + `attack_store(staging_root)` (:398) reusing the existing probes (PermissionError ⇒ DENIED, ENOENT ⇒ INCONCLUSIVE, success ⇒ BREACH), listed in `run_attacks`  |
| `NM-FS-02` | L | Executor image symlink | apps/desktop/src-tauri/launcher/src/main.rs:1088-1132 `open_executor_image(path, expected_sha256)`: `libc::open(.., O_RDONLY \| O_NOFOLLOW \| O_CLOEXEC)` (1091-1096; `fd < 0` → `Refusal::ImageIntegrity` 1097-1099), `fstat(fd)` (1105-1110), `image_owner_mode_ok(st_mode, st_uid, TCB_OWNER_BROPS_ADMIN_ | Rust, apps/desktop/src-tauri/launcher/src/main.rs, `#[cfg(all(test, target_os = "linux"))]` test inside `mod linux` (the fn is private): tempdir, regular file F with bytes B (sha S), `std::os::unix::fs::symlink(F, L)`, assert `open_executor_image(L, &S) == Err(Refusal::ImageIntegrity)`; body carries "NM-FS-02". To be mutation-visible pick ONE of: (a) make the open step return a distinguishable reason (e.g. `Refusal::TcbIntegrity("image-open")` at 1097-1099, keeping `ImageIntegrity` for owner/hash) and assert that reason — then removing O_NOFOLLOW yields `ImageIntegrity` (owner) instead → RED;  |
| `NM-TCB-02` | L | Executor swap after pin | apps/desktop/src-tauri/launcher/src/main.rs:1087-1132 `open_executor_image`: open O_RDONLY\|O_NOFOLLOW\|O_CLOEXEC, fstat the opened fd (:1105-1113 owner/mode), read_to_end, `if brops_core::receipt::sha256_hex(&bytes) != expected_sha256 { close; return Err(Refusal::ImageIntegrity) }` (:1128-1131), th | Shell, engine/ci/live/run_live_turn.sh (runs as root on the Linux CI job ci.yml:130-185): after `$TCB/executor.lease` is written (:137-147), build `$SWAP=$TCB/contained-executor.swapped` = different bytes, root:root 0755; invoke the LAUNCHER directly as the recorder principal with the exact §2.7 table so every earlier gate passes and the image re-hash is the first refusal: `sudo -u "$RECORDER_USER" env -i "$TCB/privileged-launcher.bin" "$TCB/executor.lease" "$SWAP" "$CGROUP_ARG" 0</dev/null 1>/dev/null 2>"$ERR" 3<"$STORE/system" 4<"$STORE/history" 5<"$STORE/generation_config" 6>"$PIPE"` (fd 6  |

## §4 Refuted by the skeptic — the reader's proposal did not survive; the correction is recorded, the registry is untouched

| ID | reader proposed | skeptic's correction | why |
| :-- | :-- | :-- | :-- |
| `NM-TCB-12` | unsure | blocked | One grep decides it. docs/design/WAVE_3B1B_EXECUTION_BINDING_ADDENDUM.md:3555-3558: '(f) … ⇒ `platform_governed_execution_supported()` == false …; (g) on a non-supported platform the gate is false …; (h) the all-present 7-distinct-UID Linux fixture ⇒ the gate is true and the four §2.1 same-service-UID isolation proofs still hold.' 'The gate' in (h) is the same symbol (f)/(g) name, and NM-TCB-10/11 are already blocked on that symbol not being defined (config/negative-matrix.json:1355,1361; spec-c |
| `NM-TCB-24` | unsure | blocked | Decided by grep on the named layer. The row's control is the §7 signer holding a LEASE field `generation_config_sha256` against the challenge's: `grep -i lease engine/runtime/isolated_signer.py` → only `lease_handle` custody/consistency (:114-119, :175, :662-667, :765-766) — the signer reads no lease `generation_config_sha256` (it DERIVES the digest from store bytes by handle, :148-155 HANDLE_TO_DERIVED_HASH, :836-856 `_recompute_request_sha256`); and the supervisor lease has no such field (gove |
| `NM-CRASH-07` | already-covered | implementable (S): add a test NAMED with the ID, e.g. engine/tests/test_negative_matrix.py::NegativeMatrixTimeTests::test_nm_crash_07_a_lease_ready_row_found_expired_on_restart_expires_and_launches_nothing, carrying the string "NM-CRASH-07" in its body -- optionally the close/reopen-the-file-ledger restart variant before `gate_and_start`. Adding the string to test_nm_time_07's body is not enough. | The substance holds: `lease_launch_gate` :531-541 returns 'lease_expired' when now_ms > lease_expires_at_ms; `gate_and_start` :544-565 advances to EXPIRED under LEASE_READY; LEGAL_PREDECESSORS EXPIRED:(LEASE_READY,) :91; DDL trigger supervisor_ledger.sql:109; driver arm governed_acceptance.py:795-800; test_nm_time_07 (:389) asserts EXPIRED + failure_reason 'lease_expired' + nothing renders; test_an_expired_attempt_stays_expired_on_a_retry (:1036) asserts executor.runs == []; Rust test :1436-1455 |
| `NM-CRASH-09` | already-covered | implementable (S): a test NAMED with the ID (e.g. engine/tests/test_governed_acceptance.py::IdempotenceTests::test_nm_crash_09_a_launcher_that_dies_before_exec_lands_in_recovery_required_and_is_never_relaunched) with "NM-CRASH-09" in its body, re-asserting what :666 asserts (fail before on_started -> not_completed, RECOVERY_REQUIRED, fresh.runs == []). Adding the string to the existing test's body | The coverage claim is correct: test_governed_acceptance.py:666-681 `_Executor(self, fail=RuntimeError('boom'), skip_started=True)` -> refused `not_completed`, state RECOVERY_REQUIRED, second trigger with a fresh executor refused and `fresh.runs == []`; the control is the seam-failure edge at governed_acceptance.py:829-839 (comment: cannot distinguish never-launched from launched-and-died); default seam `RefusingExecutor` :298; the §2.7 launcher exists (apps/desktop/src-tauri/launcher/src/main.rs |
| `NM-CRASH-11` | already-covered | implementable (S): a test NAMED with the ID (e.g. engine/tests/test_governed_acceptance.py::RefusalsAreReachableTests::test_nm_crash_11_a_child_that_ran_and_produced_no_output_lands_in_recovery_required) carrying "NM-CRASH-11" in its body and asserting what :1082 asserts plus `executor.runs == 1` and, via a fresh executor, `fresh.runs == []`. Adding the string to the existing test's body alone doe | The coverage claim is correct: test_governed_acceptance.py:1082-1086 `_Executor(self, fail=RuntimeError('launcher exited 1'))` with skip_started=False, so `run` calls `on_started` first (:200-202) -> `mark_executing` persists EXECUTING (driver :818-827) -> then raises -> seam edge :829-839 advances to RECOVERY_REQUIRED (assertion at :1086, reader said :1087); never-relaunch for that durable state is at :683-711. Ledger predecessors :92; DDL supervisor_ledger.sql:111. The closure mechanism is wro |
| `NM-XBIND-05` | blocked | blocked -- but with a corrected blocked_on: the cross-bind control PARTIALLY EXISTS in the Rust twin. apps/desktop/src-tauri/win-live/src/servers.rs CHAIN_AGREEMENT entry 4 (:1305-1309) `("containment_evidence_handle", Some("brops.containment-evidence.v1"), &["run_id", "execution_attempt_id", "output_handle"])` and its test `a_containment_report_about_another_run_is_refused` (:2457-2469) inject a  | The reader's blocked_on makes a false absence claim: 'No test anywhere injects a containment artifact with foreign run/attempt/lease/runner' and 'there is no ... run/attempt/lease/runner comparison'. Both are true of engine/runtime/isolated_signer.py only. The win-live twin (servers.rs:1295-1309 comment: 'It now has to agree with the attested evidence on the run it describes and on the reply digest') cross-binds run_id/execution_attempt_id/output_handle of `brops.containment-evidence.v1` and tes |
| `NM-XBIND-10` | blocked | blocked -- same status, corrected blocked_on. The claim 'the only producer of the literal in the tree is the 3b-1A tools service' is false: the Rust twin signer apps/desktop/src-tauri/win-live/src/servers.rs:1446-1449 emits it -- `let request_nonce = match get_str(evidence, "request_nonce") { Some(s) if !s.is_empty() => s, _ => return self.refuse_sign("run_binding_invalid") }` -- which is exactly  | Status is right, one factual claim is wrong. CONFIRMED: isolated_signer.py:195 defines REASON_RUN_BINDING_INVALID and `grep -c '_Refuse(REASON_RUN_BINDING_INVALID'` = 0; docstring :659-663 says 'NM-XBIND-10 expects the first ... This signer raises NEITHER'; evidence shape has no lease_id; measure.py re-run: `receipt.run_id != evidence.run_id -> refused reason=chain_document_disagrees_with_attested_evidence:execution_receipt_handle.run_id`, `lease.execution_attempt_id != evidence -> ...lease_hand |
| `NM-TIME-15` | blocked | blocked -- same status, but blocked_on must drop the false claim '`LiveRunStateProvider` -- zero occurrences'. Corrected blocked_on: "The §7 verify-time equality `lease_expires_at_ms - lease_issued_at_ms == LEASE_DURATION_MS` is not implemented. Duration is only CONSTRUCTED (governed_supervisor.py:660, win-live servers.rs:852); `grep -rn LEASE_DURATION_MS engine/runtime apps/desktop/src-tauri` sho | The status is right -- no verify-time duration check exists anywhere (confirmed: LEASE_DURATION_MS uses at governed_supervisor.py:26,65,84,86,243,596,660 and governed_supervisor_ledger.py:97,1121 are definition/construction; Rust uses at supervisor_ledger.rs:46 const, :1408/:1442 tests, governed_verification.rs:2041-2057 freshness-budget arithmetic; `_prepare_locked` :361-403 INSERTs both values at :371/:383 with no comparison; signer has no lease fields). But the reader's blocked_on states '`Li |
| `NM-PARITY-05` | blocked | implementable | The Rust half of SHA256(JCS(containment object)) DOES exist in this tree; the reader searched only core/src, broker/src, src. (1) apps/desktop/src-tauri/provision/src/canonical.rs:56 `pub fn canonical_bytes(value: &Value)` is documented (:1-7) as the exact twin of `bro_signature.canonical_bytes` -- which is precisely what brops_canonical.containment_evidence_bytes calls (brops_canonical.py:25 `from bro_signature import canonical_bytes`, :145) -- and it encodes booleans/null/nested values (test : |
| `NM-PARITY-06` | blocked | implementable | The Rust half exists in the win-live kit the reader did not search. Policy bundle is loaded as exact bytes and content-addressed by `sha256_hex(content)`: apps/desktop/src-tauri/win-live/src/bin/win_provision.rs:52-56 `seed_blob` and :246 `seed_blob(&store, b"brops-policy-bundle-v1")`; win-live/src/proof.rs:154-158,221. The Rust isolated-signer twin then derives `policy_bundle_sha256` from the store by handle with `store_read` verifying `crypto::sha256_hex(&bytes) == handle` (win-live/src/server |

## §5 Unsure

| ID | under test | what would decide it |
| :-- | :-- | :-- |
| `NM-ORACLE-07` | Handle byte tamper | Measured by code read, not inferred: isolated_signer.py:738-747 -- `data = self._store.read_verified(handle)`; `if data is None: … handle_missing`; `digest = _sha256_hex(data)`; `if digest != handle:  # pragma: no cover - read_verified already guards` ⇒ REASON_HASH_MISMATCH. read_verified (:331-336) either returns bytes whose digest equals the handle or raises, so the branch cannot be entered. What would decide this row: an Owner/Architect call on whether NM-ORACLE-07 binds to the tree's actual  |

## Method

Readers: one per family group (TCB · CRASH · MAN · FRAME · ORACLE · XBIND · TIME · ACL+CAP+CONC · EVID+IPC+NORELAUNCH · FS+SCOPE · OUTPUT+PARITY · REG+REPLAY+TERM), each told to list its unreviewed IDs from the registry and handle every one: quote the matrix row, locate the control (three or more named greps before saying "absent"), find an existing test, propose. Skeptics: one per group, told to refute — grep wider for every `blocked`, open every cited location for every `implementable` / `already-covered`, default to `unsure` when they could not check. 24 agents, 882 tool calls, read-only throughout; the tree was not modified and no suite was run. The raw table with full evidence is the Builder's session artifact, not committed.

