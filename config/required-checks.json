{
  "note": "The branch-protection state main is EXPECTED to have, committed so it can be checked instead of described. tools/check_repo_state.py compares this against live GitHub and refuses on drift. Written for the eighth audit's H-04, which found SEVEN documents still asserting main had no protection nine days after it was turned on - one of them stating the command, its output and a verification date. Prose about repository settings goes stale silently; this does not. Any change to protection must move this file in the same pull request, which is the same discipline current_state.json already carries for pull requests.",
  "enforce_admins": true,
  "strict": true,
  "required_linear_history": true,
  "allow_force_pushes": false,
  "allow_deletions": false,
  "contexts": [
    "Action pins - consistency gate (F-46)",
    "Bridge · engine adapter (python)",
    "Canonical law · read receipt · roadmap order · update law",
    "Capability · inventory gate (T-010)",
    "Cockpit · SQLite data core (cargo test)",
    "Cockpit · Tauri host (cargo check + ai tests)",
    "Cockpit · computed style (real Chromium)",
    "Cockpit · design tokens (drift + contrast)",
    "Cockpit · frontend (typecheck + build + test)",
    "Cockpit · frontend accessibility (axe)",
    "Cockpit · frontend bundle-size budget (build + check)",
    "Coordination · docs consistency gate",
    "Engine · governance runtime (python)",
    "Engine · governance runtime on Windows (python)",
    "Engine · signer isolation proof (linux, dedicated principals)",
    "Governed chain · production crates (cargo test)",
    "Linux · LIVE 7-service governed turn (real uids, setuid launcher)",
    "Linux · §4.10(g) LADDER round trip (adapter → real supervisor → real contained execution)",
    "Negative matrix · every declared test ID says what establishes it",
    "Node - npm audit (fail-closed high+ filter)",
    "Python - pip-audit (PyPI advisories)",
    "Reachability - nothing implemented-and-uncalled",
    "Release signing + updater - fail-closed coupling gate",
    "Repo-state · live GitHub truth verifier",
    "Residual engine items O-1..O-5 - inventory gate",
    "Runbooks - pasted Python matches the code it calls",
    "Rust - cargo-audit (RustSec advisories)",
    "Rust - cargo-deny (advisories + bans + licenses + sources)",
    "SBOM - CycloneDX (rust + npm + python)",
    "Secrets - gitleaks",
    "Spec · every § reference declares whether it is implemented",
    "Supervisor · durable-ledger DDL single-source gate (F-01)",
    "Trust provisioning + audit signer (both platforms) (ubuntu-latest)"
  ],
  "deliberately_excluded": {
    "AI-surface inventory gate": "Carries a `paths:` filter (.github/workflows/ai-surface.yml:9-15), so it does not report on a pull request that touches no Rust. GitHub treats a skipped required context as PENDING, so requiring it as-is would block every merge that does not touch apps/desktop/src-tauri/src/**. It can be required once the filter is removed or a no-op guard job reports in its place.",
    "Trust provisioning + audit signer (both platforms) (windows-latest)": "T-023. An intermittent custody refusal on the runner's inherited ACL, observed on PR #125, #132, #155 and #157. (This reason said '#125, #132 and #148' until 2026-08-29; #148 is not one of the four and #155/#157 were missing -- ninth audit `I-08`. ci.yml:473 and the T-023 row in TASKS.md both name the same four, and the fourth is the occurrence whose ACE dump identified the cause, so the wrong list pointed away from the evidence. The context list here is machine-compared against branch protection; these reasons are not, which is how a wrong history sat inside the artifact H-04 built to make claims checkable.) The job's own failure text says a custody assertion failing there is a real refusal rather than an elevated-token artefact, which is exactly why it must not become a context everyone learns to rerun. Requiring it before the ACL is characterised would make the flake close main.",
    "Windows · §0.W broker syscall proof (named-pipe peer-auth)": "Made required on 2026-08-18 and it failed the same day, on a SECOND distinct Windows flake: `head_sequence::tests::concurrent_allocations_never_return_the_same_number` panicked with `head-sequence counter unreadable ... Access is denied. (os error 5)` while 106 other tests in the same binary passed. Read the code before concluding: `read_hint` (win-live/src/head_sequence.rs) treats NotFound as 0 and refuses every other error DELIBERATELY - its own comment says a mark that stops existing must not read as no-mark-required - so the refusal is correct and uniqueness is preserved. What is not robust is liveness: on Windows a concurrent writer briefly excludes readers, and the allocation fails rather than serialising. That is T-039, and it must not be closed with a retry loop around a floor read without deciding whether a retry weakens the floor. Excluded until it is characterised - which is the same rule T-023 gets, applied consistently, because the eighth audit's H-06 is precisely that the two decisions were pointing opposite ways.",
    "Release preflight - fail closed without the Owner's signing material": "release.yml triggers on 'push: tags: v*' and workflow_dispatch only (.github/workflows/release.yml:23-26), so it NEVER reports on a pull request. GitHub treats a required context that never reports as PENDING, so requiring it would block every merge forever with no message. Measured 2026-08-30 (T-055): of the 36 job names declared across .github/workflows/, this was the only one named in neither 'contexts' nor 'deliberately_excluded' -- it was uncovered by accident rather than by decision, which is why tools/check_repo_state.py::verify_deferred_enforcement now derives that population from the filesystem and refuses a job that is required by nothing, excluded by nothing and deferred by nothing."
  }
}
