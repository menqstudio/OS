{
  "$comment": [
    "Declared unreachable surfaces, with a WRITTEN REASON for each. Enforced by",
    "tools/check_reachability.py (CI job 'Reachability - nothing implemented-and-uncalled').",
    "",
    "Unreachable is sometimes exactly correct: a capability-denied command MUST have no caller,",
    "and a superseded sibling legitimately outlives its last call site. What hides a defect is",
    "unreachable-AND-undeclared - the state in which nobody can tell which of those it is. So",
    "this file is the point of the gate, not an escape hatch from it: every entry costs a",
    "sentence that a reviewer can disagree with, and 'unreachable, no reason given' is refused",
    "by the gate itself (see MIN_REASON_CHARS / PLACEHOLDER_REASONS).",
    "",
    "Adding an entry is a claim. If the command later gains a real caller, the gate turns RED",
    "and asks you to DELETE the entry - deliberately, so an exception cannot silently outlive",
    "the condition it described.",
    "",
    "`rust_symbols` covers apps/desktop/src-tauri/** the same way `engine_symbols` covers the",
    "python engine. It needs `module` as well as `defined_in`, because a Rust caller must NAME",
    "the symbol (`module::name(`): a bare-name scan would count any unrelated function of the",
    "same name as a caller, and names like `create_schema` / `resolve` / `sweep` are shared by",
    "half a dozen sibling modules in brops-core. rustc never warns about an uncalled `pub fn`",
    "in a library crate, which is why this whole class was invisible here until 2026-08-09.",
    "",
    "`rust_symbols` is NO LONGER EMPTY. As of 2026-08-12 it holds EIGHT entries in two groups, and the",
    "distinction between them is the point. FIVE are the halves of the rev-30 4.10(f) DESKTOP output pull",
    "(governed_output_pull::pull_output, ai::governed_pull_output, ai::governed_turn_output_read) and the",
    "rev-30 4.6 result frame that would deliver its capability (governed_bridge_result::parse,",
    "::check_echoes). THREE are the 4.10(g) submit-frame writer landed on 2026-08-12",
    "(governed_prepare::prepare_governed_turn_v1b, ::resolve_governed_generation_config_v1b,",
    "governed_submit::governed_turn_submit_prepared) - built, tested, and deliberately not wired, because",
    "wiring it would move the shipped broker off its fail-closed executor.",
    "",
    "2026-08-12, later the same day: the bridge SPAWN moved from apps/desktop/src-tauri/src/ai.rs into",
    "apps/desktop/src-tauri/core/src/governed_sidecar.rs, together with the trust environment it applies",
    "(core/src/engine_trust.rs). None of the entries below changed expectation as a result. Two things",
    "about this gate are worth recording. First, the new seam is reached as `GovernedSidecar::new(` and",
    "`.round_trip(` - INHERENT associated and method calls, the blind spot named below - so if either were",
    "ever declared here, the declaration could go stale without turning the gate red. Second,",
    "`SubmitTransport for GovernedSidecar` is a trait impl with no constructor of its own; a trait impl",
    "cannot be expressed in `rust_symbols` at all, so the fact that it is reachable only through",
    "governed_turn_submit_prepared (which has no caller) is recorded in that entry's prose rather than",
    "enforced here. Neither is a hole this change opened; both are the gate's stated limits, restated",
    "where they now apply.",
    "",
    "A SIXTH pull-side entry, governed_bridge_result::parse_frame, was DELETED on 2026-08-12 when",
    "governed_turn_submit_prepared gave it a real production caller. That deletion was made by hand rather",
    "than forced by the gate, and the gap is worth naming: the gate matches `module::name(`, and an",
    "inherent associated call reads as `BridgeTurnResult::parse_frame(`, so it would have stayed green on a",
    "declaration that had become false. A stale `declared_unreachable` on live code is the one failure this",
    "file cannot be trusted to catch, which is why an entry costs a sentence somebody has to keep true.",
    "",
    "Reasons: capability-denied | superseded | governed-wall | backend-only | not-yet-wired.",
    "'not-yet-wired' is an honest OPEN gap and must name where it is tracked.",
    "",
    "Observed state recorded 2026-08-07 on branch wave/phase-push-1; the gate re-derives it on",
    "every run rather than trusting these notes."
  ],
  "tauri_commands": {
    "decide_approval": {
      "reason": "capability-denied",
      "note": "The generic approve verb is DENIED to the main window in capabilities/default.json (deny-decide-approval, T-011): approving must require a renderer-independent native confirmation, so a compromised webview cannot approve anything. Having no caller is the ENFORCED state, not an oversight - apps/desktop/src/features/approvalsAuthority.ts names it only as DENIED_DECIDE_COMMAND, a constant that exists so the page can assert it never renders a control for it. The reachable approve path is confirm_approval; the fail-safe path is reject_approval.",
      "observed": "2026-08-07: no invoke; one non-call mention as DENIED_DECIDE_COMMAND in features/approvalsAuthority.ts."
    },
    "post_message": {
      "reason": "superseded",
      "note": "Superseded by post_user_message for every renderer path (P1-6): post_user_message fixes the role server-side, so the webview cannot choose it, while post_message only VALIDATES the role against WEBVIEW_MESSAGE_ROLES = [\"user\"]. services/desktop.ts exposes postMessage(), which invokes post_user_message. post_message is therefore the WIDER of the two - it accepts a role and checks it, where the superseding command never lets the webview name one at all - and it stays registered and allow-granted with nothing calling it. That is residual invokable surface with no user; flipping its grant to deny is the cheapest way to remove it, and the gate will say so again the moment anyone wonders.",
      "observed": "2026-08-07: zero invocations anywhere under apps/desktop/src/**, production or test."
    },
    "reply_in_conversation": {
      "reason": "superseded",
      "note": "The non-streaming twin of stream_reply: same context assembly, same one-time-challenge -> governed_turn -> verify_and_record_receipt pipeline, differing only in delivery. Every chat surface uses the streaming sibling (services/desktop.ts streamReply), so the buffered variant has no frontend wrapper. It is kept because the governed pipeline it carries is the same audited one and a non-streaming caller (automation, a headless turn) is a plausible future consumer; until such a caller exists it is unreached by design.",
      "observed": "2026-08-07: zero invocations under apps/desktop/src/**; commands.rs keeps it on the governed path."
    },
    "set_run_step_status": {
      "reason": "superseded",
      "note": "No surface authors a run-step status directly. Command.tsx drives steps through stream_run_step (the governed execution path, which sets status as a consequence of real work) and advance_run (which moves the run forward transactionally). A renderer that could stamp an arbitrary step status could claim a step succeeded without it having run, which is the exact honesty defect the six-surface work closed, so leaving this uncalled is the safer state.",
      "observed": "2026-08-07: zero invocations under apps/desktop/src/**; add_run_step / advance_run / stream_run_step are the live callers of the run-step repo."
    },
    "create_decision": {
      "reason": "not-yet-wired",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "note": "An OPEN gap, declared rather than dressed up. The Decisions page is a read-only mirror today - it renders list_decisions plus the engine evidence-chain read - and offers no control that records a decision, so create_decision is registered and allow-granted with nothing invoking it. Two honest closures exist: wire a create control on the Decisions page, or flip the grant to deny because authoring governance decisions from the webview is not wanted. Until one is chosen this is invokable surface with no user.",
      "observed": "2026-08-07: zero invocations under apps/desktop/src/**; grant is allow-create-decision, policy tier L1."
    }
  },
  "engine_symbols": {
    "assert_no_bytecode_shadow": {
      "kind": "function",
      "defined_in": "engine/runtime/bro_protected.py",
      "expectation": "must_have_caller",
      "residual_item": "O-1",
      "tracked_by": "docs/PHASE_10_PRODUCTION_ITEMS.md",
      "reason": "The worked example this gate was built around, and it CHANGED STATE while the gate was being written. Residual item O-1 (HIGH) is the canonical 'implemented and nothing calls it': the function exists, is documented, raises ProtectedScopeError for __pycache__/.pyc/.pyo under a digest root - and for the whole life of the repository had zero callers, so a forged .pyc could shadow a digest-verified .py and nothing noticed. It now has real callers on the wall's own path, so the expectation is must_have_caller and the gate protects those call sites: if either is refactored away the gate turns RED rather than the defect silently reopening. NOTE THE LIMIT - this proves the calls EXIST; it does not verify the rest of O-1 closure (hooks running python -B, compileall scoped off digest roots, the stray-.pyc regression test), and O-1's status in docs/PHASE_10_PRODUCTION_ITEMS.md is owned there, not here.",
      "observed": "2026-08-07: two call sites in engine/runtime/bro_control_plane.py - line 80 in _bind_workspace (before any binding is loaded) and line 267 in the settlement path (a second process the binding path never covers). Both were UNCOMMITTED working-tree changes by a concurrent agent at the time of this run; the gate re-derives them every run, so a revert turns it RED."
    },
    "head_anchor_payload": {
      "kind": "function",
      "defined_in": "engine/runtime/bro_audit_log.py",
      "expectation": "declared_unreachable",
      "residual_item": "O-2",
      "tracked_by": "docs/PHASE_10_PRODUCTION_ITEMS.md",
      "reason": "The OWNER-FACING half of the anchor API. O-2's fix landed: `append` now assembles the anchor payload itself and refuses any document that is not a signature over exactly those bytes, via the private `_anchor_payload` this function also wraps. What stays caller-less in this repo is deliberate -- the caller is the owner's out-of-band signing command, reached through BRO_AUDIT_ANCHOR_SIGNER, and a key the ledger's own writer could reach would prove nothing. So this is an exported API with an external consumer, NOT dead code. It gains an in-repo caller only if a signing helper ships inside the engine, which would defeat the point.",
      "observed": "2026-08-07, after O-2 landed: zero call-shaped references outside bro_audit_log.py. The enforcement now lives in `append` + `verify(keys=...)`, which the gate does not track by name."
    },
    "attach_head_anchor": {
      "kind": "function",
      "defined_in": "engine/runtime/bro_audit_log.py",
      "expectation": "declared_unreachable",
      "residual_item": "O-2",
      "tracked_by": "docs/PHASE_10_PRODUCTION_ITEMS.md",
      "reason": "The install half of the same owner-facing API: it verifies a signed anchor against the trusted registry and against the ledger's current chain before storing it. Caller-less in this repo for the same reason as `head_anchor_payload` -- the party holding the signing key is outside the engine by design. Not dead code, and not the O-2 gap: that gap closed when `append` started attaching an anchor and keyed `verify` started requiring one.",
      "observed": "2026-08-07, after O-2 landed: zero call-shaped references outside bro_audit_log.py."
    },
    "verify_conductor_session_token": {
      "kind": "function",
      "defined_in": "engine/runtime/bro_policy.py",
      "expectation": "must_have_caller",
      "residual_item": "O-3",
      "reason": "ENFORCED, not excused: this is the one of the five that genuinely has a caller, and the gate now protects that. If the call site is removed or refactored away the gate turns RED, because losing it would silently return conductor identity to a plain environment-variable claim.",
      "observed": "2026-08-07: called from engine/runtime/bro_completion.py (authorize_conductor_stop path), which also audit-logs the exemption."
    },
    "require_conductor_session_token": {
      "kind": "policy_flag",
      "defined_in": null,
      "expectation": "must_have_caller",
      "residual_item": "O-3",
      "read_via": {
        "constant": "CONDUCTOR_SESSION_POLICY_KEY",
        "bound_in": "engine/runtime/bro_policy.py"
      },
      "reason": "A policy KEY, not a function, so `defined_in` is null and the flag is 'called' by being read. It is read through the named constant CONDUCTOR_SESSION_POLICY_KEY rather than as a bare literal, so the declaration states that one hop and the gate verifies the binding is real before following it - an undeclared indirection would otherwise read as zero readers. IMPORTANT LIMIT, and the reason this entry is worth reading twice: the gate proves the flag is READ by the enforcement path and proves NOTHING about whether it is SET. That second half is residual item O-3 (MEDIUM), needs an operator-signed conductor-session artifact only the Owner can mint, and is outside this gate by construction.",
      "observed": "2026-08-07: engine/runtime/bro_policy.py binds CONDUCTOR_SESSION_POLICY_KEY to the flag and reads it in conductor_session_token_required, which now treats an UNDECLARED requirement as a REQUIRED one (an uncommitted concurrent change - the previous code defaulted to False). Still absent from engine/.bro/policy.json."
    }
  },
  "rust_symbols": {
    "pull_output": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/core/src/governed_output_pull.rs",
      "module": "governed_output_pull",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "The rev-30 4.10(f) DESKTOP pull loop: it drives the chunked read, reassembles into a bounded buffer, and applies the 4.6/7.1 whole-output length+digest gate against the SIGNED envelope (never 4.10(e)'s TRANSPORT-ONLY echo of the same two values). The pull needs an `output_stream_id`, and rev-30 4.10(f) permits exactly one source for it: the 4.10(e) `brops.governed-turn-result.v1` `signed` frame. That frame NOW HAS a supervisor-side producer - `engine/runtime/governed_acceptance.py::AcceptanceDriver`, which requires an OutputReadService and carries the minted token; it landed in this same tree from a concurrent change and was uncommitted when this entry was written, so read the citation as of that moment. 4.6's `bridge.governed-turn-result.v1` - the only frame that moves that token across the sidecar boundary - now EXISTS on both hops (2026-08-10): the re-framer in bridge/governed_turn_result_bridge.py and the strict parser in apps/desktop/src-tauri/core/src/governed_bridge_result.rs. What is still missing is one hop further out: 4.6 is the REPLY to 4.10(g)'s `bridge.governed-turn-submit.v1`, and 4.10(g) is NOT IMPLEMENTED - no submit branch in engine_sidecar._dispatch and no orchestrator that drives 4.10(a0)/(a)/(b)/(c)/(d) inside one one-shot subprocess - so nothing ever holds a 4.10(e) reply to re-frame, and wiring a caller today would still mean inventing a token, which is the single thing 4.10(f) says may never happen. The dependency is typed rather than described: `OutputStreamCapability` can only be built from a verified 4.9 envelope plus a 43-char token, so the day a 4.6 frame delivers one the compiler names every place it has to reach. A second, larger disagreement would survive fixing that: the broker's Linux execution (`broker/src/chain_executor.rs::LinuxGovernedExecution`) reads the recorder's output off the local filesystem with `std::fs::read(&report_path)` instead of through this egress at all, so even a delivered token would not by itself put the pull on the live path. This entry is deliberately the same shape as the one that held `governed_output_stream::mint/resolve/sweep` before that ladder was deleted, and it is NOT the same claim: that one said wiring a caller was 'a rewrite, not a hookup' and that one of its refusals could not be produced at all. Every gate here is produced by name from a reply a real supervisor can send, and the supervisor half it talks to is shipped and served. UPDATED 2026-08-12 (the §4.10(g) submit-frame WRITER): the sentence 'nothing writes a submit frame' is now HISTORY too. `prepare_governed_turn_v1b`, `PreparedGovernedTurnV1B`, `GovernedGenerationConfig` and `resolve_governed_generation_config_v1b` exist in `apps/desktop/src-tauri/core/src/governed_prepare.rs`, and `governed_turn_submit_prepared` exists in `core/src/governed_submit.rs`: it asserts the §4.10(g) cross-bindings, builds the `bridge.governed-turn-submit.v1` frame, and strict-decodes the §4.6 reply. It is the Rust half of the governed `SHA256(JCS(flat string->string object))` formula whose Python half already shipped, and it reproduces §4.10(g)'s printed digest 732b58634d0a83e9b7fdf1ca69db78df145bd9dd79ac8922fed3e79cf5faab22 byte-for-byte. What blocks a caller is now TWO named things rather than an absence. (1) The subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) and NO production code implements it: the tree's one bridge-spawn seam, `ai::governed_sidecar_call`, is async tokio in the renderer-hosting app crate and carries `engine_trust::apply`, while the broker binary is synchronous and does not depend on that crate; a second spawn implementation is the half-wired export that seam's own comment warns about. (2) Nothing CALLS `governed_turn_submit_prepared`: the broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same §4.10(a0)/(a)(b)(c)/(d) hops over DIRECT AF_UNIX and spawns the recorder rather than a sidecar, and its `ProductionResolver` supplies the three artifact digests from static deployment config instead of from a prepared turn - so wiring the writer means reconciling two architectures for the same hops, which is an Architect's call and not this change's. The live-supervisor blocker above is unchanged. The `parse_frame` entry that used to sit beside these was DELETED in the same change, because `governed_turn_submit_prepared` gives it a real production caller and a stale `declared_unreachable` on live code is a lie this gate cannot catch - it matches `governed_bridge_result::parse_frame(`, and an inherent associated call reads as `BridgeTurnResult::parse_frame(`.",
      "observed": "2026-08-10: zero callers outside governed_output_pull.rs and its #[cfg(test)] module. Its async transport adapter is ai::governed_pull_output, declared below. 2026-08-10 (§4.10(g) sidecar half): re-derived by the gate in the same run — still zero production callers. 2026-08-12 (the submit-frame writer): re-derived in the same run - still zero production callers, and the reason is now the two named blockers above rather than a missing producer."
    },
    "governed_pull_output": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/src/ai.rs",
      "module": "ai",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "The async half of the same hop: it turns `governed_output_pull::OutputPull` into round trips over one-shot sidecar subprocesses, one per chunk, and maps a local sidecar failure to a TRANSPORT error rather than to one of the five stream reasons (4.10(f) P1-5). The pull needs an `output_stream_id`, and rev-30 4.10(f) permits exactly one source for it: the 4.10(e) `brops.governed-turn-result.v1` `signed` frame. That frame NOW HAS a supervisor-side producer - `engine/runtime/governed_acceptance.py::AcceptanceDriver`, which requires an OutputReadService and carries the minted token; it landed in this same tree from a concurrent change and was uncommitted when this entry was written, so read the citation as of that moment. 4.6's `bridge.governed-turn-result.v1` - the only frame that moves that token across the sidecar boundary - now EXISTS on both hops (2026-08-10): the re-framer in bridge/governed_turn_result_bridge.py and the strict parser in apps/desktop/src-tauri/core/src/governed_bridge_result.rs. What is still missing is one hop further out: 4.6 is the REPLY to 4.10(g)'s `bridge.governed-turn-submit.v1`, and 4.10(g) is NOT IMPLEMENTED - no submit branch in engine_sidecar._dispatch and no orchestrator that drives 4.10(a0)/(a)/(b)/(c)/(d) inside one one-shot subprocess - so nothing ever holds a 4.10(e) reply to re-frame, and wiring a caller today would still mean inventing a token, which is the single thing 4.10(f) says may never happen. The dependency is typed rather than described: `OutputStreamCapability` can only be built from a verified 4.9 envelope plus a 43-char token, so the day a 4.6 frame delivers one the compiler names every place it has to reach. A second, larger disagreement would survive fixing that: the broker's Linux execution (`broker/src/chain_executor.rs::LinuxGovernedExecution`) reads the recorder's output off the local filesystem with `std::fs::read(&report_path)` instead of through this egress at all, so even a delivered token would not by itself put the pull on the live path. It is deliberately NOT a `#[tauri::command]` and is absent from `generate_handler!`, because 4.10(f) requires the pull to be an internal backend helper that never round-trips the webview; an ai.rs test asserts both of those against the sources so the attribute cannot be added quietly. UPDATED 2026-08-12 (the §4.10(g) submit-frame WRITER): the sentence 'nothing writes a submit frame' is now HISTORY too. `prepare_governed_turn_v1b`, `PreparedGovernedTurnV1B`, `GovernedGenerationConfig` and `resolve_governed_generation_config_v1b` exist in `apps/desktop/src-tauri/core/src/governed_prepare.rs`, and `governed_turn_submit_prepared` exists in `core/src/governed_submit.rs`: it asserts the §4.10(g) cross-bindings, builds the `bridge.governed-turn-submit.v1` frame, and strict-decodes the §4.6 reply. It is the Rust half of the governed `SHA256(JCS(flat string->string object))` formula whose Python half already shipped, and it reproduces §4.10(g)'s printed digest 732b58634d0a83e9b7fdf1ca69db78df145bd9dd79ac8922fed3e79cf5faab22 byte-for-byte. What blocks a caller is now TWO named things rather than an absence. (1) The subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) and NO production code implements it: the tree's one bridge-spawn seam, `ai::governed_sidecar_call`, is async tokio in the renderer-hosting app crate and carries `engine_trust::apply`, while the broker binary is synchronous and does not depend on that crate; a second spawn implementation is the half-wired export that seam's own comment warns about. (2) Nothing CALLS `governed_turn_submit_prepared`: the broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same §4.10(a0)/(a)(b)(c)/(d) hops over DIRECT AF_UNIX and spawns the recorder rather than a sidecar, and its `ProductionResolver` supplies the three artifact digests from static deployment config instead of from a prepared turn - so wiring the writer means reconciling two architectures for the same hops, which is an Architect's call and not this change's. The live-supervisor blocker above is unchanged. The `parse_frame` entry that used to sit beside these was DELETED in the same change, because `governed_turn_submit_prepared` gives it a real production caller and a stale `declared_unreachable` on live code is a lie this gate cannot catch - it matches `governed_bridge_result::parse_frame(`, and an inherent associated call reads as `BridgeTurnResult::parse_frame(`.",
      "observed": "2026-08-10: zero callers outside ai.rs; referenced from its own #[cfg(test)] module by the_output_pull_is_never_spawned_without_the_provisioned_trust_environment, which proves `engine_trust::apply` is on this path and that a failed spawn arrives as Transport. 2026-08-10 (§4.10(g) sidecar half): re-derived by the gate in the same run — still zero production callers. 2026-08-12 (the submit-frame writer): re-derived in the same run - still zero production callers, and the reason is now the two named blockers above rather than a missing producer."
    },
    "governed_turn_output_read": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/src/ai.rs",
      "module": "ai",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "One `bridge.governed-turn-output-read.v1` round trip - the single stdin request / single stdout reply of a fresh one-shot sidecar. It has no caller of its own outside ai.rs because its only caller is `governed_pull_output` in the same module, and that function is itself unreached for the reason recorded above. It is listed separately rather than folded into that entry so the gate keeps reporting BOTH halves: if the loop ever gains a caller and this does not, the pull has grown a second spawn implementation, which is exactly the half-wired export `governed_sidecar_call`'s own comment warns about. The gap it inherits MOVED on 2026-08-10: 4.6's `bridge.governed-turn-result.v1` now exists on both hops (bridge/governed_turn_result_bridge.py + apps/desktop/src-tauri/core/src/governed_bridge_result.rs), so the missing hop is now 4.10(g)'s `bridge.governed-turn-submit.v1`, not the result frame. UPDATED 2026-08-12 (the §4.10(g) submit-frame WRITER): the sentence 'nothing writes a submit frame' is now HISTORY too. `prepare_governed_turn_v1b`, `PreparedGovernedTurnV1B`, `GovernedGenerationConfig` and `resolve_governed_generation_config_v1b` exist in `apps/desktop/src-tauri/core/src/governed_prepare.rs`, and `governed_turn_submit_prepared` exists in `core/src/governed_submit.rs`: it asserts the §4.10(g) cross-bindings, builds the `bridge.governed-turn-submit.v1` frame, and strict-decodes the §4.6 reply. It is the Rust half of the governed `SHA256(JCS(flat string->string object))` formula whose Python half already shipped, and it reproduces §4.10(g)'s printed digest 732b58634d0a83e9b7fdf1ca69db78df145bd9dd79ac8922fed3e79cf5faab22 byte-for-byte. What blocks a caller is now TWO named things rather than an absence. (1) The subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) and NO production code implements it: the tree's one bridge-spawn seam, `ai::governed_sidecar_call`, is async tokio in the renderer-hosting app crate and carries `engine_trust::apply`, while the broker binary is synchronous and does not depend on that crate; a second spawn implementation is the half-wired export that seam's own comment warns about. (2) Nothing CALLS `governed_turn_submit_prepared`: the broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same §4.10(a0)/(a)(b)(c)/(d) hops over DIRECT AF_UNIX and spawns the recorder rather than a sidecar, and its `ProductionResolver` supplies the three artifact digests from static deployment config instead of from a prepared turn - so wiring the writer means reconciling two architectures for the same hops, which is an Architect's call and not this change's. The live-supervisor blocker above is unchanged. The `parse_frame` entry that used to sit beside these was DELETED in the same change, because `governed_turn_submit_prepared` gives it a real production caller and a stale `declared_unreachable` on live code is a lie this gate cannot catch - it matches `governed_bridge_result::parse_frame(`, and an inherent associated call reads as `BridgeTurnResult::parse_frame(`.",
      "observed": "2026-08-10: zero callers outside ai.rs; reaches the engine only through governed_sidecar_call, the one seam in the application that spawns the bridge. 2026-08-10 (§4.10(g) sidecar half): re-derived by the gate in the same run — still zero production callers. 2026-08-12 (the submit-frame writer): re-derived in the same run - still zero production callers, and the reason is now the two named blockers above rather than a missing producer."
    },
    "parse": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/core/src/governed_bridge_result.rs",
      "module": "governed_bridge_result",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "The success-only entry point over `parse_frame`: it returns the decoded SignedTurnResult and turns a well-formed `ok:false` frame into Err(FrameError::Refused), so a caller cannot reach the success path by forgetting to match an arm. It was listed separately from `parse_frame` so that if one gained a caller and the other did not, the frame having grown a second decode path would be visible. On 2026-08-12 exactly that happened, in the benign direction: `governed_turn_submit_prepared` calls `parse_frame` (it needs a refusal's forensic `receipt_id`, which the success-only wrapper discards), `parse_frame`'s declaration was deleted, and this one stays - so `parse` is now the surface to watch, and a caller appearing here without a reason would be the second decode path. It has zero callers because 4.6 is the REPLY to 4.10(g)'s `bridge.governed-turn-submit.v1`, and 4.10(g) is NOT IMPLEMENTED: there is no submit branch in bridge/engine_sidecar.py's _dispatch, no orchestrator driving 4.10(a0) -> 4.10(a)(b)(c) -> 4.10(d) inside one one-shot subprocess, and therefore nothing that ever holds a 4.10(e) reply to re-frame. Both ends of the join exist and are tested - the supervisor produces the 4.10(e) frame (engine/runtime/governed_acceptance.py), the sidecar re-frames it (bridge/governed_turn_result_bridge.py, 44 tests), and this module reads the result - and the carriage between the desktop and the sidecar does not. A second, larger divergence would survive fixing that: the broker's Linux execution (broker/src/chain_executor.rs::LinuxGovernedExecution) reads the recorder's output off the local filesystem with std::fs::read(&report_path) instead of through the 4.10(f) egress at all, so even a delivered token would not by itself put the pull on the live path. UPDATED 2026-08-12 (the §4.10(g) submit-frame WRITER): the sentence 'nothing writes a submit frame' is now HISTORY too. `prepare_governed_turn_v1b`, `PreparedGovernedTurnV1B`, `GovernedGenerationConfig` and `resolve_governed_generation_config_v1b` exist in `apps/desktop/src-tauri/core/src/governed_prepare.rs`, and `governed_turn_submit_prepared` exists in `core/src/governed_submit.rs`: it asserts the §4.10(g) cross-bindings, builds the `bridge.governed-turn-submit.v1` frame, and strict-decodes the §4.6 reply. It is the Rust half of the governed `SHA256(JCS(flat string->string object))` formula whose Python half already shipped, and it reproduces §4.10(g)'s printed digest 732b58634d0a83e9b7fdf1ca69db78df145bd9dd79ac8922fed3e79cf5faab22 byte-for-byte. What blocks a caller is now TWO named things rather than an absence. (1) The subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) and NO production code implements it: the tree's one bridge-spawn seam, `ai::governed_sidecar_call`, is async tokio in the renderer-hosting app crate and carries `engine_trust::apply`, while the broker binary is synchronous and does not depend on that crate; a second spawn implementation is the half-wired export that seam's own comment warns about. (2) Nothing CALLS `governed_turn_submit_prepared`: the broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same §4.10(a0)/(a)(b)(c)/(d) hops over DIRECT AF_UNIX and spawns the recorder rather than a sidecar, and its `ProductionResolver` supplies the three artifact digests from static deployment config instead of from a prepared turn - so wiring the writer means reconciling two architectures for the same hops, which is an Architect's call and not this change's. The live-supervisor blocker above is unchanged. The `parse_frame` entry that used to sit beside these was DELETED in the same change, because `governed_turn_submit_prepared` gives it a real production caller and a stale `declared_unreachable` on live code is a lie this gate cannot catch - it matches `governed_bridge_result::parse_frame(`, and an inherent associated call reads as `BridgeTurnResult::parse_frame(`.",
      "observed": "2026-08-10: zero callers outside governed_bridge_result.rs and its #[cfg(test)] module. A bare `parse(` elsewhere cannot count - the gate requires `governed_bridge_result::parse(` or a `use` naming it from that module, and no file has either. 2026-08-10 (§4.10(g) sidecar half): re-derived by the gate in the same run — still zero production callers. 2026-08-12 (the submit-frame writer): re-derived in the same run - still zero production callers, and the reason is now the two named blockers above rather than a missing producer."
    },
    "check_echoes": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/core/src/governed_bridge_result.rs",
      "module": "governed_bridge_result",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "7.1's echo equality: 'every bridge.governed-turn-result.v1 / brops.governed-turn-result.v1 echo equals the verified envelope; a mismatch Blocks.' It is the ONLY way to read the frame's TRANSPORT-ONLY echoes at all - SignedTurnResult has no output_bytes()/output_sha256() accessor, deliberately, so no caller can aim an output length or digest gate at the transport's echo of its own digest (governed_output_pull reads both off the SIGNED envelope and has no parameter for either). What it is worth is stated in its own doc comment and is narrow: it catches a proxy that ALTERED what the supervisor said, and nothing against one that copies faithfully. It has zero callers because 4.6 is the REPLY to 4.10(g)'s `bridge.governed-turn-submit.v1`, and 4.10(g) is NOT IMPLEMENTED: there is no submit branch in bridge/engine_sidecar.py's _dispatch, no orchestrator driving 4.10(a0) -> 4.10(a)(b)(c) -> 4.10(d) inside one one-shot subprocess, and therefore nothing that ever holds a 4.10(e) reply to re-frame. Both ends of the join exist and are tested - the supervisor produces the 4.10(e) frame (engine/runtime/governed_acceptance.py), the sidecar re-frames it (bridge/governed_turn_result_bridge.py, 44 tests), and this module reads the result - and the carriage between the desktop and the sidecar does not. A second, larger divergence would survive fixing that: the broker's Linux execution (broker/src/chain_executor.rs::LinuxGovernedExecution) reads the recorder's output off the local filesystem with std::fs::read(&report_path) instead of through the 4.10(f) egress at all, so even a delivered token would not by itself put the pull on the live path. UPDATED 2026-08-12 (the §4.10(g) submit-frame WRITER): the sentence 'nothing writes a submit frame' is now HISTORY too. `prepare_governed_turn_v1b`, `PreparedGovernedTurnV1B`, `GovernedGenerationConfig` and `resolve_governed_generation_config_v1b` exist in `apps/desktop/src-tauri/core/src/governed_prepare.rs`, and `governed_turn_submit_prepared` exists in `core/src/governed_submit.rs`: it asserts the §4.10(g) cross-bindings, builds the `bridge.governed-turn-submit.v1` frame, and strict-decodes the §4.6 reply. It is the Rust half of the governed `SHA256(JCS(flat string->string object))` formula whose Python half already shipped, and it reproduces §4.10(g)'s printed digest 732b58634d0a83e9b7fdf1ca69db78df145bd9dd79ac8922fed3e79cf5faab22 byte-for-byte. What blocks a caller is now TWO named things rather than an absence. (1) The subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) and NO production code implements it: the tree's one bridge-spawn seam, `ai::governed_sidecar_call`, is async tokio in the renderer-hosting app crate and carries `engine_trust::apply`, while the broker binary is synchronous and does not depend on that crate; a second spawn implementation is the half-wired export that seam's own comment warns about. (2) Nothing CALLS `governed_turn_submit_prepared`: the broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same §4.10(a0)/(a)(b)(c)/(d) hops over DIRECT AF_UNIX and spawns the recorder rather than a sidecar, and its `ProductionResolver` supplies the three artifact digests from static deployment config instead of from a prepared turn - so wiring the writer means reconciling two architectures for the same hops, which is an Architect's call and not this change's. The live-supervisor blocker above is unchanged. The `parse_frame` entry that used to sit beside these was DELETED in the same change, because `governed_turn_submit_prepared` gives it a real production caller and a stale `declared_unreachable` on live code is a lie this gate cannot catch - it matches `governed_bridge_result::parse_frame(`, and an inherent associated call reads as `BridgeTurnResult::parse_frame(`.",
      "observed": "2026-08-10: zero callers outside governed_bridge_result.rs and its #[cfg(test)] module, where each of the five echoes is altered one at a time and required to Block. 2026-08-10 (§4.10(g) sidecar half): re-derived by the gate in the same run — still zero production callers. 2026-08-12 (the submit-frame writer): re-derived in the same run - still zero production callers, and the reason is now the two named blockers above rather than a missing producer."
    },
    "prepare_governed_turn_v1b": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/core/src/governed_prepare.rs",
      "module": "governed_prepare",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "§4.10(g)'s SINGLE immutable object-JCS-hash source: in one pass it validates the flat string->string `generation_config` object, computes `JCS(object)` and `SHA256(JCS(object))` once, normalizes system (raw UTF-8) and history (JCS) once, mints the `request_nonce` once, and returns the one object from which the `receipt_challenges` pre-store, the create-pending request, the submit frame and the final `Expected` all derive. It exists because §4.10(g) FORBIDS reusing the frozen `prepare_governed_turn(&str)`: that one hashes `generation_config` as a raw UTF-8 string, the two digests differ by construction (asserted by tests on both sides), and reusing it would make every legitimate turn Block at every gate on the path. It has no caller for the same two named reasons: the §4.10(g) subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) that no production code implements - `ai::governed_sidecar_call`, the tree's one bridge-spawn seam, is async tokio in the renderer-hosting app crate while the broker binary is synchronous - and the broker orchestration step that would call it (§4.10(g) `governed_turn_execute` steps 1-3: prepare, `receipt_challenges` pre-store, authority create-pending/issue) is not written. The broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same hops over DIRECT AF_UNIX and its `ProductionResolver` reads the three artifact digests from static deployment config, so wiring this means reconciling two architectures for one hop - an Architect's call. It is declared rather than left silent because rustc never warns about an uncalled `pub fn` in a library crate, which is the whole reason this section exists. Deliberately NOT wired: doing so would move the shipped broker off its fail-closed executor, and the Owner's standing constraint is that the production gate stays shut until an independent audit passes.",
      "observed": "2026-08-12: zero callers outside governed_prepare.rs and its #[cfg(test)] module. `governed_submit` uses the TYPE (`PreparedGovernedTurnV1B`) but not this constructor; `ai.rs`'s parity tests call it under #[cfg(test)] only."
    },
    "resolve_governed_generation_config_v1b": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/core/src/governed_prepare.rs",
      "module": "governed_prepare",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "§4.10(g) P0-1(B)'s ONE trusted backend source of the full `generation_config` object: the five frozen literal defaults plus the exact four-variable `BROPS_GOVERNED_*` trusted-host override contract (an unset/empty var yields the default; a set-but-invalid value returns Err and the turn dies fail-closed; `engine_id` has no variable at all). It is the ONLY function in this module that touches the process environment, and it does so through `resolve_governed_generation_config_from`, which is what every test drives - so the override contract is exercised without mutating a global. This wrapper, the one that reads `std::env::var`, is the half with no caller. It has no caller for the same two named reasons: the §4.10(g) subprocess spawn is an INJECTED seam (`governed_submit::SubmitTransport`) that no production code implements - `ai::governed_sidecar_call`, the tree's one bridge-spawn seam, is async tokio in the renderer-hosting app crate while the broker binary is synchronous - and the broker orchestration step that would call it (§4.10(g) `governed_turn_execute` steps 1-3: prepare, `receipt_challenges` pre-store, authority create-pending/issue) is not written. The broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same hops over DIRECT AF_UNIX and its `ProductionResolver` reads the three artifact digests from static deployment config, so wiring this means reconciling two architectures for one hop - an Architect's call. It is declared rather than left silent because rustc never warns about an uncalled `pub fn` in a library crate, which is the whole reason this section exists. Deliberately NOT wired: doing so would move the shipped broker off its fail-closed executor, and the Owner's standing constraint is that the production gate stays shut until an independent audit passes.",
      "observed": "2026-08-12: zero callers anywhere. Its injected sibling `resolve_governed_generation_config_from` is called from the #[cfg(test)] modules of governed_prepare.rs, governed_submit.rs and ai.rs; the env-reading wrapper is called by nothing, which is exactly the shape a reviewer should be able to see."
    },
    "governed_turn_submit_prepared": {
      "kind": "function",
      "defined_in": "apps/desktop/src-tauri/core/src/governed_submit.rs",
      "module": "governed_submit",
      "expectation": "declared_unreachable",
      "tracked_by": "docs/REACHABILITY_GATE.md",
      "reason": "The §4.10(g) INTERNAL helper that writes the `bridge.governed-turn-submit.v1` frame - deliberately NOT a #[tauri::command] and absent from generate_handler!, and taking the WHOLE `GovernedTurnExecutionV1B` because `PreparedGovernedTurnV1B` alone does not carry `task_id`/`run_id` (P0-1(A)). Before it writes anything it asserts the cross-bindings §4.10(g) makes a terminal Block, and a test proves no frame is written and no transport call is made when one fails. Its counterparty, `bridge/governed_turn_submit.py`, is complete and proven end to end against the real supervisor services. It had no caller for two named reasons; ONE OF THEM IS NOW CLOSED. The §4.10(g) subprocess spawn is still the INJECTED seam `governed_submit::SubmitTransport`, but as of 2026-08-12 production code implements it: `governed_sidecar::GovernedSidecar`, in brops-core, is the single place in the tree that starts the bridge, and the desktop app's governed turn, governance mirror and §4.10(f) pull all reach it through the same implementation instead of a second one that could drift on `engine_trust`. (The old blocker - `ai::governed_sidecar_call` being async tokio in the renderer-hosting app crate while the broker binary is synchronous - was resolved by MOVING the spawn down into the shared crate, not by writing a second one.) What remains open, and is the reason this entry stands, is the OTHER half: the broker orchestration step that would call it (§4.10(g) `governed_turn_execute` steps 1-3: prepare, `receipt_challenges` pre-store, authority create-pending/issue) is not written. The broker's one production `GovernedExecutor` (`broker/src/chain_executor.rs::ChainExecutor`) drives the same hops over DIRECT AF_UNIX and its `ProductionResolver` reads the three artifact digests from static deployment config, so wiring this means reconciling two architectures for one hop - an Architect's call. It is declared rather than left silent because rustc never warns about an uncalled `pub fn` in a library crate, which is the whole reason this section exists. Deliberately NOT wired: doing so would move the shipped broker off its fail-closed executor, and the Owner's standing constraint is that the production gate stays shut until an independent audit passes.",
      "observed": "2026-08-12: zero callers outside governed_submit.rs and its #[cfg(test)] module. It DOES call `governed_bridge_result::BridgeTurnResult::parse_frame`, whose declaration was deleted in the same change for that reason. Same day, later: `SubmitTransport` gained a production implementation (`governed_sidecar::GovernedSidecar`) and this function still gained no caller - giving the writer a transport is not giving it a caller, and the distinction is the whole reason this entry is worded the way it is."
    }
  },
  "tools_gates": {
    "$comment": [
      "Gates under tools/ that NO workflow executes, each with a written reason.",
      "Enforced by check_reachability.py section 5. Same contract as tauri_commands: the",
      "gate re-derives the state every run, and if a workflow later starts running an entry",
      "listed here the gate turns RED and asks you to delete it."
    ],
    "tools/check_prior_art.py": {
      "reason": "Not a CI gate and deliberately not run as one: it answers a per-SESSION question (has this session recorded what it searched for before creating a new file?) against a receipt that lives in the OS temp directory and never exists on a CI runner. Its caller is .claude/hooks/canonical_law_gate.py at PreToolUse. Running it in CI could only ever pass vacuously, and a check that cannot fail is worse than an absent one because it reads as coverage. Its self-tests DO run, in the ci.yml canonical-law job.",
      "observed": "2026-08-09: invoked from .claude/hooks/canonical_law_gate.py; self-tests run by ci.yml job canonical-law."
    }
  }
}
