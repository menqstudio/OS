{
  "schema": 2,
  "purpose": "Machine-readable current-state SNAPSHOT for menqstudio/OS. STATE: settled. main is at settled_at_main_head (b3010f6); PR #81 was the last to merge; prs[] is empty and active.branch is the settle carrier's branch (see active.branch and current_workflow_pr), so no implementation work is queued here. What is blocked, and on whom, is docs/OWNER_ACTION_REQUIRED.md. AUDIT POSITION: the last INDEPENDENT audit -- apps/desktop/AUDIT/2026-08-06-remediation-audit.md, of main @ 219c763 -- returned RED (4 of 18 blockers confirmed closed; 45 surviving findings, 1 P0), and no independent audit has been run on any later head, so RED is the standing verdict; the index is apps/desktop/AUDIT/AUDIT_LEDGER.md. The production trusted_verified gate stays SHUT (status_tokens.CURRENT_PRODUCTION_VERIFIED: false) until an independent audit passes and the Owner approves. PROVENANCE, not open work: the Wave 3b design+implementation+live proof were consolidated as PR #48 and merged with the Phase-2 slices #49/#50/#51/#52; PR #53 (the Windows LIVE machine-proof) merged after them; CURRENT_DESIGN_PR/CURRENT_IMPL_PR: 48 records where that work landed. tools/check_coordination.py checks the human docs agree with this file; tools/check_repo_state.py verifies it against LIVE GitHub in CI. Update in the same commit as any state change. (This field described PR #53 as the active workflow until 2026-08-09, four merged PRs after it stopped being true; and it asserted \"active.branch is main\" while active.branch was a branch, which is now machine-checked.) CANONICAL LAW (2026-08-09): the read receipt, roadmap order, prior-art search and update law are enforced at the repository ROOT by .claude/hooks/canonical_law_gate.py and four gates in tools/. The receipt mechanism already existed in engine/ and was wired only at engine/.claude/settings.json, so a root session got one Stop guard and nothing else -- which is how three days of work happened against a stale roadmap. A phase counts complete only when its DoD checkboxes and its status-board row agree; there is no machine-readable completion signal and the gate says so. Shell writes are not gated and CANONICAL_LAW=off disables the hook, both deliberately and both recorded. PHASE 1 RULINGS (2026-08-09): two long-open Phase-1 questions were settled in writing rather than left to disagree with the code. The Settings governed-provider TOGGLE was AMENDED to a read-only three-state status control (default/on/blocked) -- the provider is resolved from the backend process environment and Phase 1's own gate is \"Desktop never holds lease/key/env\", so a webview-writable switch is not buildable honestly, and governed_verification_unconfigured() returns Some(...) unconditionally so a flip could not change an outcome anyway; the control was also keyboard-unreachable (`disabled` removes it from the tab order) and is now aria-disabled and focusable. Governed DELTA-STREAMING (slice 3) is DESCOPED, not deferred: a governed turn is buffered by construction because the desktop's authority is a signature over the whole output. core/src/governed_output_stream.rs was NOT that streaming -- it was the rev-30 4.10(f) chunked PULL of a completed output, with ZERO production callers and a table that diverged from the design it cited; it was declared under the new rust_symbols section of config/reachability-declarations.json and was DELETED on 2026-08-10 when 4.10(f) was actually built (see WAVE 3b-1B STEP 5 below). tools/check_reachability.py gained that section because the whole src-tauri Rust tree was outside its scope, which is why nothing caught the ladder. PHASE 1 REMAINS OPEN: the DoD box for governed output delivery is unchecked and the status-board row agrees. EVIDENCE-FLOOR CLUSTER (2026-08-10): the anti-rollback floor was partitioned by an attacker-chosen task_id while head_sequence is minted by ONE per-install counter, so the partition could only ever create buckets to hide in; it is now an install-wide ceiling, and the Rust test that asserted the defect as a feature is rewritten. The rev-30 s2.5 TCB pin was self-referential; 4 of 21 artifacts now take their digest from --source-dir and every entry records digest_origin, but the compiled binaries and provisioned material still have no origin outside the deployment host -- that needs release-signed digests or an operator signature and is OPEN. The bro_completion head floor is configuration-impossible on Windows (no posture satisfies both the custody rule and the write), pinned by tests and warnings rather than worked around; consolidating onto the supervisor ledger floor is the likely answer and is the OWNER's call. BRO_OPERATOR_ROOT_PIN_SELF_OWNED is now CI-gated like its siblings with a file-based production form, and engine_trust refuses BOTH names; it is not unforgeable by an environment-setting adversary and says so. Engine suite 1330 OK (43 skipped). PHASE 1 CHECKBOX CORRECTION (2026-08-10): Phase 1's nine [x] boxes are six distinct facts, and one was false -- 'One governed round-trip proven end-to-end -- done'. The production order at commands.rs:1338-1428 returns at :1382, before ai::governed_turn and before verify_and_record_receipt, so both have ZERO runtime-reachable production callers; the box was false even on its own narrowed fail-closed terms. The refusal is deliberate and STAYS -- the row is open because the roadmap described a round-trip the gate forbids. The real end-to-end turn (run_live_turn.sh / live-governed-turn) is a different seam and is named rather than counted. The other five facts are recorded at their real width: bridge-result.schema.json is a TEST-ONLY contract, the bridge CI job runs but is not REQUIRED, the adapter and the governed transport are built and dead, and the UI badge can only ever paint demonstration_verified (Windows-only). check_reachability.py prints GREEN here and structurally cannot catch it -- its own LIMITS say it cannot see past an earlier return. HEAD-FLOOR TOPOLOGY (2026-08-10): the decision to consolidate bro_completion's configuration-impossible head floor onto the supervisor ledger was WRONG and was disproved by execution before any code changed. The two floors count different things -- ledger per INSTALL as an install-wide ceiling, completion per TASK with every first anchor at 1 -- so two real signed heads at sequence 1 are both accepted by the filesystem floor and the second is refused by the ledger with EvidenceFork; consolidating would make the SECOND task in any deployment permanently un-completable. The ledger is also unreachable from the completion path (Linux-only AF_UNIX + SO_PEERCRED, broker uid only), ABSENT on Windows (open finding R-42), and has no column for evidence_head_sha256. The docstrings that recommended that route now carry the four disproofs. The contradiction is unchanged and is OWNER decision 1b in docs/OWNER_ACTION_REQUIRED.md: a floor-writer service or a setuid helper, because the builder is the writer. PROMPT-FORGERY CLUSTER (2026-08-10): a message body could forge a speaker in the transcript, and on the demonstration path -- the bytes the chain binds and SIGNS -- a one-message conversation and a two-message conversation produced IDENTICAL bytes, a real collision in a signed digest. The turn encoding is now AUTHOR ': ' JSON-STRING (total, injective, one line per turn, U+2028/9 escaped), with the rule spliced into every system prompt so the description cannot drift from the encoding. Roster names are bounded at the write (rejected, not truncated) and again at the splice, because unbounded rows predate the bound. The coding agent's write grant over the trust code is BOUNDED not removed: BRO_PROTECTED_PATHS denies the write tools and TrustSurfaceGuard restores any protected file changed by any means, including Bash and Task subagents. FOUR DECISIONS taken under the Owner's standing approval: tools/ and .github/ stay protected (a model that can edit the gate can make the gate agree with it); the Edit(path)/Write(path) deny syntax is an UNVERIFIED assumption pending a live-CLI capture and the boundary rests on TrustSurfaceGuard meanwhile; revert-and-report rather than fail-the-turn; and the digest change is accepted since nothing re-derives a digest from persisted rows. Re-verified here: brops --lib 120, frontend 635, ai-surface/capability/reachability GREEN. WAVE 3b-1B STEP 1 (2026-08-10): brops.governed-turn-open.v1, the s2.4 governed_turn_staging states and the s4.10(a0) pre-accept open are IN; s4.10(b)(c)(d)(e)(f) are deliberately NOT built. P1-5 is refused by name -- a request carrying execution_attempt_id is malformed, no row, nothing published, and the table has no such column. accept_open is s5 acceptance, a different operation, reused part by part rather than copied. Four enforcements are DB-level triggers, not Python checks. The s4.2 challenge-key-registry VERIFIER was implemented because no brops.challenge-key-registry.v1 exists in the tree and two a0 refusals would otherwise have been unreachable stubs. Two encoders are both called canonical and diverge on non-ASCII ids; the strict INTERSECTION is enforced. 48 mutants, 48 killed, zero survivors. Engine suite 1414 OK (43 skipped) after this correction, 1406 before it; the converged cold baseline is 1328, and the suite is NOT deterministic from a cold state on this box. No governed surface became reachable. CHALLENGE_HANDLE CONTRADICTION RESOLVED (2026-08-10, OWNER_ACTION_REQUIRED 1c is CLOSED): rev-30 defined challenge_handle twice -- s3's artifact matrix, s4.10(a0) and Appendix B say SHA256(JCS({payload, sig})), while s5's summary table and the shipped accept_open used SHA256(JCS(payload)) -- so the staging and acceptance handles were digests of DIFFERENT strings. The Architect ruled the DEFINING sections normative. accept_open now computes the {payload, sig} form through one new definition, governed_supervisor.challenge_handle_for; the win-live kit's servers.rs accept_open was corrected the same way; s5's table is corrected under a visible CORRECTION block at the head of the addendum, because a silent edit to the normative source is the worst possible change. The decisive argument was not which section outranks which: s7's challenge predicate re-hashes the STORED {payload, sig} document and compares it to challenge_handle, so the payload-only form could never satisfy s7 for ANY turn. Losing the s5 form's signature-collapse costs nothing -- a re-signed replay now collides on UNIQUE(install_id, request_nonce) and is refused, still buying zero execution attempts. No durable artifact anywhere held a handle computed the old way. New engine/tests/test_challenge_handle_agreement.py drives BOTH real paths over ONE document into ONE ledger and asserts the rows agree; a Rust test pins the win-live half; 9 mutants, 9 killed, zero survivors. No gate moved and no governed surface became reachable. WAVE 3b-1B STEP 2 (2026-08-10): SS4.10(a) governed-staging-open, (b) the chunk upload and (c) the final are IN; (d)(e)(f) remain unbuilt. (a) was outside the brief and was built anyway because it is the only creator of the session row (b) and (c) operate on -- without it every refusal in them would have been an unreachable stub. A handler frame cap on (a)/(c) that could NEVER fire was DELETED rather than shipped, and replaced by a test proving the shape bound implies the frame bound. A mutant survived because the code it broke sat in a POSIX-only branch unreachable on Windows; extracted and now tested everywhere. Two real bugs found: a raw sqlite3.IntegrityError could escape handle_connection entirely, and IllegalTransition had the wrong arity. The DDL parity gate went from 17 to 40 load-bearing clauses -- gapless chunk sequence, no chunk rewrite, a cursor that may only advance by exactly one seq and exactly the recorded chunk_len, a published handle that must EQUAL the challenge-committed digest, and INPUTS_READY unreachable until all three are set. 29 named refusals all tested; 84 mutants, ZERO survivors. Design arithmetic corrections recorded: SS2.4's worst-case chunk frame is 245982 not 245963 and the headroom is 15.78 KiB not >=16 KiB, and MAX_STAGING_CHUNKS=46 binds only because the 8 MiB ceiling binds first. Engine suite 1551 OK (43 skipped). Nothing governed is minted. AUDIT F-30 CLOSED (2026-08-10): the self-approval guard compared origin_principal against confirmer_principal, and on the only production path those are 'webview:<label>' and 'native' -- the equality was evaluated on every approval and could never be true. Two tests claimed to lock it while driving a confirmer value no shipped caller emits, so they stayed green and mutating the real call site killed nothing. Replaced by two checks that CAN fail: approve_confirmed accepts NATIVE_CONFIRMER_PRINCIPAL and nothing else (strictly stronger -- the old rule still let webview:a confirm webview:b's request), and create refuses to store that name as an origin_principal. Composed, no row can exist whose origin equals the only accepted confirmer. Both mutants killed here, restores verified byte-exact. brops-core 312, brops 120, frontend 635. DESKTOP-SURFACE AUDIT SWEEP (2026-08-10, all marks are Builder's-claim only): accept_prepare's idempotency comparison called itself exhaustive over the durable request binding and hand-listed 16 of 24 bound columns; the five it omitted were challenge_accepted_at_ms and all four challenge_registry_* INCLUDING the anti-rollback epoch, so a retry under a ROLLED-BACK registry epoch was answered Idempotent. Now a derive(PartialEq) struct so the field list IS the comparison. The Python twin _BOUND_FIELDS omitted the SAME five and is now CLOSED: governed_supervisor_ledger._BOUND_FIELDS is DERIVED from NewAcceptance's dataclass fields minus two exclusions named in writing (the identity pair the lookup keys on, the payload blob compared by digest), with one test asserting the compared set equals the declared binding minus those exclusions and a second reading the INSERT's own column list out of the source and holding it against the same binding. Each of the five has its own named test and dropping any one kills that test alone, so none was masked by the lease_payload_sha256 compare; reuse_or_prepare's challenge-keyed replay still returns the ORIGINAL row per s5 and is now pinned. Rust and Python are level again. The renderer->broker read had no total budget (SO_RCVTIMEO restarts per byte: ~11.5 days) and its bound lived inside mod linux where no non-Linux test could reach it. The demonstration badge -- the only green badge the shipped app can display -- was a bare flag row whose artifacts are deleted before it is written; migration 0024 binds it to the sha256 of the bytes the chain bound, and pre-0024 rows LOSE the badge rather than have evidence manufactured. 12 findings were already closed and the ledger did not know; ONE ledger row (governed_turn_ipc.rs:239 bound tautology) is simply WRONG. Two mutants survived because two guards masked each other; the one that could never change an outcome was deleted. Recommended next: SS7.1's absent freshness step at governed_verification.rs:276. brops 124, brops-core 314, frontend 638. No gate touched. WAVE 3b-1B STEP 3 (2026-08-10): SS4.10(d) the evidence request is IN; (e), (f), (h) and SS5 acceptance are untouched. Building the gate exposed a REAL HOLE it would have trusted: VERIFYING said nothing about the three handle columns, and handles already equal to the committed digests pass the binding trigger, so a raw INSERT could plant a VERIFYING row with all three filled, walk it to INPUTS_READY, and SS4.10(d) would read it as proof of upload having published NOTHING. Closed by trg_governed_turn_staging_insert_handles; parity gate 40 -> 42 clauses. No handler frame cap was added: the largest legal request is 426 bytes against a 4096 cap, so such a check could never fire. SS4.10(h)'s 'disjoint namespace' claim is FALSE about values -- the intersection is {malformed, retry_conflict}. 72 mutants, 70 killed; the 2 survivors each edit the parity gate's own oracle and no gate can kill those. The SS5 continuation drive_acceptance is a seam with NO production supplier, named rather than left to read as complete. CORRECTION to the previous entry: _BOUND_FIELDS was 15 of 23 not 16 of 24, and the exploit path was overstated -- a replay of the SAME challenge returns the original lease per SS5 and never reaches the comparison; the comparison is reached by a DIFFERENT challenge on the same nonce, and there the omitted anti-rollback epoch did answer Idempotent. Now DERIVED from NewAcceptance's dataclass fields (15 -> 20 compared), with a test that parses the INSERT's own column list so the list cannot fall behind in either direction; 8 mutants, 8 killed, none of the five masked. Engine suite 1627 OK (43 skipped). SS7.1 FRESHNESS (2026-08-10): the mandatory freshness step was ABSENT from the governed path -- verify_and_accept was documented 'no clock' and FreshnessWindow was wired only to the v1 receipt_store path, so a receipt validly signed at ANY point in the past verified today. Now governed_verification::check_receipt_freshness bounds BOTH signed _ms fields independently under the SS1-LOCKED FreshnessWindow{future_skew_ms:60000, max_age_ms:300000}, at code step 4d -- after the attestation turn-binding, before the output digest and before the ledger claim, so a stale receipt burns neither the hashing nor the one-time nonce. Fail-closed: an unreadable clock Blocks rather than unwrap_or(0), and Freshness has private fields with one constructor so 'unbounded' is not expressible. The clock is the LOCAL host wall clock because that is what the design names; an attacker who can roll this machine's clock back can still widen the window, and that residual is written in the module. A win-live test fixture had been pinning the year 2030 as 'a plausible wall clock' and now uses the host clock. 18 mutants, 17 killed; the one survivor is behaviourally identical to the original and is reported, not papered over. brops-core 323, broker 31+3, win-live 83, brops 124. SS7.1 stays PARTIAL: no SS4.10(f) pull loop and no bridge echo-equality step exist on this path. WAVE 3b-1B STEP 4 (2026-08-10): SS4.10(e) the result frame is IN -- the complete tagged union, both arms exhaustive, SS4.6 encoded-byte caps, canonical-base64url on all five b64 fields, and GOVERNED_REFUSAL_REASONS defined ONCE with its ratified twelve compared IN ORDER against the frozen brops-sign-result.v1 schema enum, so 'verbatim' is machine-checked. SS4.10(f), (h), SS4.6's bridge frame, SS4.5's sign-result frame and SS5 acceptance stay unbuilt. SS4.10(d) now VALIDATES its post-acceptance arm instead of relaying it, so (e) is reached from production code. drive_acceptance is still an injected seam with NO production supplier -- now typed, not supplied. A first-pass mutant survived because a test got its TypeError from ten missing arguments rather than from the keyword-only marker it claimed to prove; rewritten, then 67 mutants 67 killed. NO frame check exists: the literal maximum signed frame is 74472 bytes against a 262144 cap, so none could fire -- the third ordered piece in a row to decline an unreachable check. SS4.10(h)'s 'disjoint namespace' claim is false about values by THREE: {malformed, retry_conflict, oversize}. SS2.2 names schema files that do not exist for the whole governed family. No member of the 29 refusal reasons is DECIDED anywhere in this tree, and a test says so by name. Engine suite 1681 OK (43 skipped). WAVE 3b-1B STEP 5 (2026-08-10): SS4.10(f)'s SUPERVISOR hop is IN; its DESKTOP hop, SS4.10(g), SS4.10(h) and SS5 acceptance are not. brops.governed-turn-output-read.v1 is the sixth name in SIDECAR_PROTOCOLS, served by engine/runtime/governed_output_read.py; the durable governed_output_streams table is in the canonical supervisor_ledger.sql (parity gate 42 -> 53 clauses) as an INSERT-ONCE row whose lifetime FOLLOWS from created_at_ms and whose digest must BE the handle it serves from, so a capability can be neither renewed nor minted with a life nobody authorized. The uncalled Rust ladder was DELETED, not wired: its CREATE TABLE IF NOT EXISTS governed_output_streams ran on the same connection one line before supervisor_ledger::create_schema in all four call sites, so keeping it would have made the canonical DDL a silent no-op; brops-core 323 -> 314 is exactly its nine tests, and rust_symbols is now empty. A REAL defect surfaced: the supervisor front door wrote every reply at the broker's 8192 bound while the sidecar's READ bound was 262144 -- a 245940-byte chunk reply would have degraded to 'reply exceeded frame bound' and the pull could never have completed; handle_connection now writes with the bound it reads with. Arithmetic first: max reply 245940 of 262144 (16204 headroom), max request 421 bytes, so no frame check exists in either direction and the ONE bound that can fire is on the chunk. Two DESIGN findings recorded rather than papered over: MAX_OUTPUT_STREAM_BYTES_PER_INSTALL is exactly 64 x MAX_OUTPUT_BYTES, so the byte limb of the quota can NEVER bind before the count limb (proved in a test; two mutants survive for that reason), and 4.10(f)'s reason set is a SUBSET of GOVERNED_REFUSAL_REASONS by design, which 4.10(h) confirms by calling an output-read refusal a genuine governed verdict. 63 mutants killed, 3 survivors all named. Engine suite 1786 OK (43 skipped). No gate moved and nothing governed is minted. SS4.10(f) DESKTOP HOP (2026-08-10): built, tested and NOT WIRED, and the reason is a missing FRAME not a missing hookup -- SS4.6's bridge.governed-turn-result.v1, the only frame that carries output_stream_id across the sidecar boundary, has no implementation on either hop, so the token exists on the supervisor side and cannot arrive. The dependency is TYPED: OutputStreamCapability cannot be constructed without a verified envelope plus a 43-char token, and the three symbols are declared in reachability-declarations.json so the gate REPORTS the gap. The digest gate cannot be pointed at SS4.10(e)'s transport echo by construction: pull_output takes a ReceiptEnvelope and reads the expected length and digest off it, so no call shape admits the echo. TWO DESIGN DISAGREEMENTS: SS4.10(f) says the pull is a private function of governed_turn_execute, which rev-30's own topology makes unsatisfiable (that command never sees a token, an envelope or a receipt id); and THE BROKER NEVER PULLS -- chain_executor reads the recorder's output straight off the local filesystem, so even a delivered token would not put the pull on the live path. ARITHMETIC: max reply 245940 (supervisor) / 245941 (bridge) against 262144, MAX_STDOUT_BYTES 9437184 -- but governed_supervisor_server's broker-facing cap and ipc_framing's payload cap are both 8192, THIRTY TIMES too small, which is why this hop is a subprocess stdio hop; pinned in tests in both languages. 29 mutants, 29 killed, including an untestable wiring line that was REMOVED rather than excused. bridge 95, brops 127, brops-core 343. Gate byte-identical. WAVE 3b-1B STEP 6 (2026-08-10): SS5 ACCEPTANCE IS IN -- drive_acceptance, the seam every earlier piece left unwired, now has a production supplier, governed_acceptance.AcceptanceDriver. It is a CLIENT of the SS5 ladder, not a second implementation: accept_open, reuse_or_prepare, mark_lease_ready, gate_and_start, mark_executing, record_completion, load_attestation_state and build_run_attestation are all called, and complete-run's body was EXTRACTED from the broker op into one shared complete_governed_run rather than copied, so the two callers cannot disagree about what a completed run is. execution_attempt_id is minted exactly once at the SS5 step-4 CAS, the acceptance clock is read exactly once at step 2 and is the instant the step-3 predicate is evaluated at, and the supervisor-side nonce consume is that same CAS. SS5 step 3's ACCEPTANCE-TIME predicate is real: a fresh registry re-resolve, key validity as of challenge_accepted_at_ms (not as of issue), and the acceptance-time registry epoch/handle/hash bound into the row instead of the deployment constant the shipped config carried -- so a rotation between open and acceptance is refused challenge_invalidated. Eighteen GOVERNED_REFUSAL_REASONS members now have a producer that reaches SS4.10(e), each reachable BY NAME in a test; the seven that do not are listed with reasons. THE ARITHMETIC FOUND A LOAD-BEARING BOUND: SS4.6 freezes envelope_jcs_b64 <= 2848 as a machine-checked derivation, and for the 23-key SS4.9 payload this tree's signer actually builds that derivation is WRONG -- nine of its string fields are ids capped at 128, and at 125 characters each the encoding is 2852, 2888 at the cap. It is refused as a governed oversize verdict rather than allowed to fault the frame validator, and the boundary is tested at 124/125. The attestation limb is 4032 against 4664 and got NO check, the fourth ordered piece in a row to decline one the numbers proved unreachable. THREE DESIGN-VS-TREE DISAGREEMENTS RECORDED, NOT PAPERED OVER: (1) SS5 step 6's lease is never SIGNED -- no lease-issuer key exists anywhere in the tree -- so lease_handle is SHA256(JCS(payload)) not SHA256(JCS({payload,signature})), the SAME contradiction the 2026-08-10 CORRECTION closed for challenge_handle, still open here; (2) the isolated signer's front door allowlists ONLY the broker uid, so there is no supervisor->signer transport and SS6.1 steps 11-12 are a typed seam with no production binding; (3) the shipped signer's evidence and envelope shapes differ from SS4.4/SS4.9 as written (builder_id present where the design says there is none, lease_id/runner_id/challenge_* absent). 82 mutants, 81 killed; the one survivor is equivalent (two reads of one column). Mutation found four real defects: a dead field carried out of the extraction that nothing read, an unreachable state guard deleted for a test that CAN fail, a check masked by its neighbour, and a test that passed because the SIGNER refused for a different reason than the check it claimed to prove. Engine suite 1878 OK (43 skipped), converged over three runs. NOTHING GOVERNED IS WIRED: run_supervisor.py still constructs no sidecar service, the shipped execution binding is RefusingExecutor (platform_unsupported, pre-record, no row), and the three refusals are byte-identical. WINDOWS-SURFACE AUDIT SWEEP (2026-08-10, Builder's-claim marks only): 39 of the 122 findings land in win-live/win-broker/provision/launcher/executor/audit-signer/proof; 21 were ALREADY CLOSED and the ledger did not know, 2 are misdescribed. NEW, in no audit round: BOTH Windows machine-proof harnesses could never report PASS -- each case function wrote its transcript to the success stream as well as returning its verdict, so the collected count was 4 against an expected 2, and the self-tests could not see it because they call the case function directly. A proof that cannot pass is a check that cannot fail with the sign flipped. R-42/R-24 CLOSED: there was no ledger floor on Windows at all, and head_sequence -- the only field that orders two runs -- was a constant, so a floor would have compared it against itself; complete_run now runs the same durable CAS the Linux supervisor uses, after the state decision and before the store publish, and Supervisor::new is fallible with no in-memory fallback. The isolated signer RESOLVED the containment report and discarded it under a comment claiming it was bound; now a fourth CHAIN_AGREEMENT entry. Nine dead Facts fields and four placeholder store blobs shaped like a completed run's artifacts were removed. CORRECTION to the previous sweep: its claim that windows_broker.rs:272 is 'already declared with written reasons' is FALSE -- the declarations file names no symbol there, and the path is core/src/windows_broker.rs. 20 mutants, 17 killed, 3 survivors each named. win-live 101 (from 83), launcher 32, executor 5, brops-core 376; both PowerShell self-tests PASS. No gate touched. SS4.6 FRAME (2026-08-10): bridge.governed-turn-result.v1 is built on BOTH hops -- the only thing that can deliver an output_stream_id to the desktop. The re-framer takes the supervisor's document and nothing else, and its receipt key tuple is DERIVED from SS4.10(e)'s, so 'copies everything, invents nothing' is checkable. The desktop type has NO accessor for the transport-only output_bytes/output_sha256 -- readable only through SS7.1's check_echoes -- so no output gate can be aimed at them. DESIGN FINDING NEEDING AN ARCHITECT RULING: SS4.6's receipt lists 28 fields, SS4.10(e) supplies 11, and SEVEN of the rest have NO SOURCE anywhere the sidecar can reach (status/exit_code/evidence[] come from a SupervisorResult the governed path never produces; the four challenge_registry_* are supervisor-internal and never returned). The other ten are NOT decoded out of the envelope on purpose, because SS7.1's equality over such a value would compare a document against itself and could not fail. The implemented frame is the intersection and the other 17 are a named tuple with a test asserting the union is exactly 28. The SS4.10(f) pull is STILL unreachable and the reason MOVED: it is now SS4.10(g) bridge.governed-turn-submit.v1, NOT IMPLEMENTED, and behind it chain_executor still reads output off the local filesystem. 34 mutants, 34 killed; one property does not COMPILE rather than being tested. bridge 140, brops-core 376, brops 127. BROKER EGRESS (2026-08-10): the broker CANNOT pull today and this is a TOPOLOGY change, not a substitution -- proved before anything was edited, tree left byte-identical. Five independent blockers: no sidecar principal exists in the live kit; the supervisor constructs no OutputReadService, so it serves no read AND mints no token; the broker's uid is refused by construction because the read gate requires the SIDECAR uid and SS2.6 requires those principals to differ; the ordering is CIRCULAR, since the mint happens inside complete-run which requires the output's own digest, so no stream can exist at the read site and no token exists after it; and the broker binary has no sidecar spawn and cannot acquire one. HONEST CORRECTION: 'the broker reads output off disk' reads worse than it is -- verify_and_accept still applies the SS7.1 gate against the SIGNED envelope and complete-run cross-checks the handle against the recorder's chain, so it is a CONFINEMENT divergence, not an integrity hole. The SHARPER violation, previously unnamed: the broker is a member of brops-store and WRITES THE SIGNER'S INPUTS, doing the recorder's SS2.3 publication duty inside the protected store. Recorded as OWNER decision 1d: build the designed topology (a 7th sidecar principal, the supervisor's services wired, SS4.10(g), and a supervisor-side ExecutionService so the SUPERVISOR spawns the recorder), or take the narrower confinement fix first by removing the broker from brops-store and moving publication to the recorder. Also recorded: SS4.10(f) specifies the pull adapter as a private function of a Tauri command, machinery that exists only in the renderer-hosting process, so the shipped adapter lives in the WRONG PROCESS -- SS0's LOCKED terminology binding calls that a wording residue. SS4.10(g) SIDECAR HALF (2026-08-10): one submit frame drives SS4.10(a0) open -> (a)(b)(c) staging -> (d) trigger -> the SS4.6 re-frame inside ONE one-shot subprocess, proven end to end against the real supervisor services, a real ledger and the real isolated signer; it is reframe_turn_result's FIRST caller. Three checks were deliberately NOT written because each would make a SUPERVISOR verdict unreachable through the only client there is. HARNESS DEFECT WITH REACH BEYOND THIS PIECE: a mutation run killed by the commit-limit crash left a LIVE MUTANT in the tree, because try/finally does not survive a killed process; found, restored, audited clean, and the harness is now crash-safe by construction (pristine bytes to disk before the first mutation, a sentinel, a --restore mode, a post-run byte-identity check). A sweep of the other pieces that ran harnesses on this box is warranted. brops_canonical gained four names and touched none: the governed family commits SHA256(JCS(flat object)), NOT the frozen raw-string hash, and reusing the frozen preparation would make every legitimate turn Block; canonicalizing the five frozen defaults reproduces the digest SS4.10(g) prints byte-for-byte. The staging-chunk frame at 245982 against 262144 is the ONE load-bearing cap on this path; SS4.10(g)'s 65536 config cap against a 349-byte field maximum could never fire and is pinned by a test instead. 43 mutants, 42 killed, the survivor provably equivalent. NOTHING calls the loop: prepare_governed_turn_v1b and governed_turn_submit_prepared have ZERO whole-tree hits. TWO ARCHITECT RULINGS NEEDED: SS4.10(h)'s 'disjoint namespace' claim is FALSE on three literals, and the SS4.10(d) hop's own budget already exceeds the 120s deadline the tree's only sidecar spawner applies. Engine 1895, bridge 210. MUTATION-LEFTOVER SWEEP (2026-08-10): after the commit-limit crash left a LIVE MUTANT in the tree -- try/finally does not survive a killed process -- the 30 commits since 5a72258 were swept for a leftover that had already been COMMITTED. A surviving mutant is by definition one no test catches, so the green suites were not evidence. The sweep read for the SHAPE of a mutation rather than re-running tests, using comment-versus-code disagreement as the primary signal, and found NOTHING; no file was changed. The commit messages proved a usable oracle: every specific number they claim still matches the code or a pinning test. Both DDL copies are byte-identical at sha256=3f2a50d7 with the parity gate GREEN at 53 clauses, and because a BOTH-copies mutant would pass that gate every trigger body was read by hand against its comment and the Python constants. NOT COVERED, stated rather than glossed: test files (a mutant planted in an ASSERTION would largely be missed), the two PowerShell proofs (+267 and +231 lines, neither read line by line nor executed, and the largest unexamined surface given that both were found unable to report PASS), prose outside the numbers, and Linux-only cfg branches. Residual: a mutant consistent with both its comment and its pinning tests would survive this method. SUBMIT WRITER (2026-08-12): prepare_governed_turn_v1b and governed_turn_submit_prepared EXIST, in brops-core -- the BROKER side -- per SS0's LOCKED terminology binding, not the renderer-hosting app crate where ai::governed_pull_output was wrongly placed by following SS4.10(f)'s literal wording. ONE of the six declared-unreachable symbols became genuinely reachable and its declaration was DELETED in the same change; the gate would NOT have caught it, because it matches governed_bridge_result::parse_frame( while an inherent associated call reads BridgeTurnResult::parse_frame( -- that blind spot is now in the declarations file's $comment. SECOND ARCHITECTURE FOUND: the broker's GovernedChain drives the same hops over direct AF_UNIX and spawns the recorder itself, never a sidecar, and its ProductionResolver supplies system/history/generation_config sha256 from STATIC DEPLOYMENT CONFIG -- so the live path's three input digests are not the conversation's. NONCE COLLISION: SS4.10(g) makes prepare mint request_nonce while broker_orchestrator already mints one into a durable row before the executor runs; both cannot be the authority. NOTHING BOUNDS THE SUBMIT FRAME: measured min 1233, ceiling 8651985 = 1056x the IPC payload cap and 116x the result cap, with no cap on either side; pinned by test, none invented. ARITHMETIC CORRECTION to the previous entry: the config cap's ratio is 187, not 188 (349*188=65612 > 65536). 37 mutants, 36 killed, under a CRASH-SAFE harness built for today's failure. STILL UNWIRED: no production transport implements the spawn (an Architect question -- the only seam is async tokio in the app crate), and SS4.10(g) steps 1-3 are unwritten because writing them means choosing between the two architectures. brops-core 420, brops 130. BROKER OUT OF THE STORE (2026-08-12): chain_executor no longer writes the output and containment blobs into the isolated signer's protected store, and ExecutionConfig no longer carries the store path AT ALL -- there is no path left to write through. The recorder publishes both itself, addressed under the store path in its ROOT-OWNED policy, so the broker cannot steer where a blob lands; the live kit's brops-store group is now supervisor + recorder. The recorder was NOT already publishing, so there was no smaller fix, and no consumer sees a change -- same digest, same bytes, only the creating uid changed. NO FALLBACK: a recorder->broker 'I published' manifest would be a CHECK THAT CANNOT FAIL, since the recorder exits non-zero before it could write one; the Block comes from three existing gates. WHAT THIS DOES NOT BUY: the store stays 2775 with other r-x, so the broker loses WRITE and keeps read/list; SS2.3's full no-read-write-list needs the sup/ and rec/ namespaces at 2750 plus moving the signer into brops-store -- a topology change, flagged not done. The SAME SHAPE exists at win-live/src/execution.rs:276,304 on the Windows topology and needs a decision. Residuals: a run that publishes then refuses leaves an inert orphan blob, and under the flat store the recorder can unlink a pinned input (lateral -- the broker could before). Every mod linux edit is UNCOMPILED on this box (no Linux gcc), verified by rustfmt parsing plus hand-checking -- syntax, not types. CORRECTION: governed_verification_unconfigured is at commands.rs:1161, not 1152 as several briefs said. brops-broker 33+3, brops-governed-live 23. ENGINE-SURFACE AUDIT SWEEP (2026-08-12, Builder's-claim marks only): 33 findings land in engine/runtime, engine/tools, engine/ci, bridge and tools; SEVENTEEN were already closed and the ledger did not know, INCLUDING THE P0 -- build_run_attestation has no facts parameter, evidence_from_state sources all 25 fields from durable state, and derive_evidence_from_chain refuses a completion whose output_handle is not the recorder's captured digest. WORST NEW FINDING: json.loads raises RecursionError on a body nesting ~3900 deep inside a LEGAL 8 KiB frame, RecursionError is in none of the caught classes, and serve_forever had NO except at all -- one frame from either authorized peer killed the process that issues every lease. Also fixed: no timeout of any kind on a serial accept loop (a per-recv timeout restarts per byte and bounds nothing; exhaustion is None, never 0.0, because settimeout(0) is non-blocking and SO_RCVTIMEO reads 0 as infinite); the head-floor comparison sat OUTSIDE any lock so the mark could go DOWN, and _index.json is one roster every task rewrites through one shared staging name, which reaches R-06 by timing alone; a reentrancy guard keyed on PID, which pid recycling defeats; and a check that COULD NOT FAIL inside the independent authorization gate, deleted. EIGHT deliberately not fixed with evidence, including a REFUSAL to extend the signer's cross-document checks because record, receipt and lease are all built by the same supervisor from the same rows, so those comparisons could not fail either. CORRECTION: the desktop sweep's row saying the Python _BOUND_FIELDS twin is not fixed is stale -- it was fixed at e4f73e2. 18 mutants, 16 killed, 2 named survivors. Engine 1915 OK (43 skipped), converged, from 1895. SLICE 2 PROOF KIT (2026-08-12): engine/ci/live/run_ladder_turn.sh + the ladder-governed-turn job drive ONE submit frame through the real one-shot sidecar from a SEVENTH brops-sidecar principal (uid 5003 -- the gap SS0's table already names), against the real Open/Staging/EvidenceRequest/OutputRead services and the real SS5 AcceptanceDriver, reaching the REAL SS6.1 step-5 contained execution with NO stand-in; ladder_evidence.py records the SS4.6 frame, the SS4.9 envelope, the digests and the SO_PEERCRED uid of EVERY hop. It is proven able to FAIL: every run drives the same verifier over an artifact the challenge never committed and requires a non-zero exit naming digest_mismatch. The seventh principal is STRUCTURAL -- handle_connection refuses 'principal collapse' when sidecar uid == broker uid, so on the six-account kit the whole SS4.10 surface is unreachable by construction. run_live_turn.sh and run_supervisor.py are UNTOUCHED, so both proofs stay independently meaningful. THIS EXERCISES OWNER DECISION 1d's Option 1, as a CI proof kit only -- the product is unchanged and no gate moved -- and it is the Owner's to accept or hold. THE SLICE 2 BOX IS NOT TICKED: the job has never run on a real runner, the Owner has not accepted the topology, and the box must be worded as the ADAPTER<->SUPERVISOR round trip because the desktop still does not write the frame in production. Substituted locally and named: the execution seam and load_tcb_json's writability check (NTFS reports 0666 for every file). The SS2.5 TCB floor is deliberately NOT evaluated by this job, because the pin manifest's role table binds supervisor.bin to run_supervisor.py and would measure files that are not serving. PLATFORM-PINNED TEST (2026-08-12): the hostile-frame test asserted the string RecursionError in stderr; it is green on Windows and RED on Linux CI, where the same body produced an EMPTY stderr because it was refused by the listed-exception branch, which does not log. The backstop is correct on both. Split rather than loosened: a DETERMINISTIC witness injects MemoryError -- in no explicit tuple, raised identically everywhere -- and proves the backstop logs to the operator and returns 'internal supervisor fault' to the peer; the real hostile frame now asserts only what holds everywhere. Mutation-proved: deleting traceback.print_exc kills the new test. FOURTH platform difference hidden in a test this week -- a mutant surviving in a POSIX-only branch, a bound in mod linux, a fixture pinning the year 2030, and now an exception name. A test that asserts HOW something failed can only pass where it was written. ONE SPAWN SEAM (2026-08-12): ai::governed_sidecar_call was the tree's only bridge spawn -- async tokio, in the renderer-hosting BINARY crate, carrying engine_trust::apply -- so the synchronous broker could not reach it and a second spawn there would have been a SECOND trust application, one path on the provisioned registry and the other on the committed development one. Both the spawn and the trust rule moved into brops-core; GovernedSidecar is now the single thing that starts the sidecar, and it implements SubmitTransport, so governed_turn_submit_prepared has a real production transport. BYPASS IS A COMPILE ERROR, NOT A CONVENTION: apply() returns a TrustEnvironment with no public constructor and GovernedSidecar's constructors require one (as_calling_principal / as_distinct_principal; new was DELETED 2026-08-12 so every call site must state its principal), so the delete-the-apply-line mutant is a BUILD_ERROR. Skipping it would fall back to the committed trusted-keys.json where production:false grants conductor-session to no key, so every check passes against a registry nobody chose while the turn still reports itself governed -- that is O-3. TWO MORE CHECKS THAT COULD NOT FAIL found on the way: one re-declared MAX_STDOUT_BYTES locally and compared a literal against itself, the other would have kept passing against the claude-CLI cap after the sidecar's cap moved. Plus a platform-branch bug in the agent's own new test (/abs/x.py is not absolute on Windows). BRO_PROTECTED_PATHS gained both new core files, or the decision about which registry the engine reads would have left the protected surface. WEAKER, NAMED: kill_on_drop cancellation -- no caller in the tree cancels this future, and a Drop guard restores kill-and-reap. 12 mutants killed, 4 survivors each named (deadline, non-zero-exit, two capped readers -- all need a real child). No gate moved; the transport gained NO caller. brops-core 442, brops 117. LADDER FIRST LINUX RUN (2026-08-12): the negative case refused with digest_mismatch and exited non-zero -- the proof CAN fail -- and the positive went RED without fabricating anything. THE EVIDENCE CARRIES A BIGGER FACT THAN THE FAILURE: all ten SS4.10 frames were served to peer_uid 5003 under supervisor_euid 5004, and the REAL SS6.1 step-5 contained execution RAN AND COMPLETED -- six uids, setuid launcher, caps_all_zero, no_new_privs, exit 0 -- driven by the supervisor through the SS4.10(d) hop. That is the intersection Slice 2 asks for and it is no longer hypothetical. What raised was the ISOLATED-SIGNER TRANSPORT: AcceptanceDriver.sign_result is documented as handed a sign-request and returning the signer's own reply, and the only transport implements NEITHER half -- dispatch routes on op, and the wire reply is the BROKER's shape (signature not signature_b64, ok not status). No provisioning gap was at fault. No test caught it because the harness wired sign_result IN-PROCESS while the deployment uses the SOCKET -- the fourth 'second architecture for the same hop' this week. The new transport test requires the decoded reply to be BYTE-IDENTICAL to IsolatedSigner.sign_result on both arms. The op-shaped error was also a defect: the fault existed NOWHERE, so SupervisorError alone now reaches stderr while peer-attributable classes stay SILENT (logging them is a flooding vector), with a test pinning both directions. THE BOX STILL CANNOT BE TICKED: attest and sign through the corrected transport have never been seen to pass on Linux. Engine 1932 OK (43 skipped), from 1915. ONE-STANDARD SWEEP (2026-08-12): the Owner named the pattern -- one contract, two implementations, found six times in three days -- so it was swept deliberately, map first. WORST FIND: chain_hops::reply_status parsed a 'status' field NO SERVER IN THIS TREE HAS EVER EMITTED (all three Python principals and all three win-live twins reply {ok, op}); its only callers were its own tests, which hand-built the invented shape -- a double GREEN BY CONSTRUCTION -- while chain_executor::hop open-coded the correct check beside it. Now ONE parse_reply on the production path, which also checks the OP ECHO, something nothing did before. A FALSE SENTENCE IN THE CODE: the isolated signer caps frames at 512 KiB and says its cap 'sits comfortably above' so a well-formed request always reaches its oversize gate -- but the Rust broker, the only production client, caps at 8192 BOTH directions, so nothing between 8193 and 524288 can ever cross and that gate is unreachable. Fail-closed, so not a hole; pinned as an asymmetry because changing either number changes what the deployment accepts. TWO CANONICALIZER FAMILIES differ only on non-ASCII, so a one-word swap is invisible to every existing test -- the same latent shape as challenge_handle; NOT collapsed (separate principals, separate TCBs) but pinned as a REQUIRED DIFFERENCE, with a corpus guard that fails if anyone trims it to ASCII-only. Seven unifications at strength 1-2 including LEASE_FIELDS derived from the dataclass (it was a hand-typed mirror with ZERO readers) and win-live pub use-ing three brops-core constants so rustc refuses a second value. THREE LEFT DELIBERATELY: a third open-coded parser inside cfg(linux) that this box CANNOT COMPILE (no edit shipped unbuilt); provision's verify() vs the workspace's verify_strict(), which is CORRECT because it is the Rust half of a Python authority and a reviewer applying policy would break the pair; and two claude-CLI timeouts (900s vs 120s), an Owner call. 15/15 mutants killed after the pass found a gap in its OWN tests. Engine 1953 OK, broker 36+3. SLICE 2 TICKED (2026-08-12): the ladder ran GREEN on a real Linux runner, both halves, at 59dc394 -- ok=true reason=none, one submit frame became one SS4.6 frame whose envelope verifies, and the negative refused with digest_mismatch and exited non-zero. The REAL SS6.1 step-5 contained execution ran: six uids, setuid launcher, caps_all_zero, no_new_privs, no stand-in. The box is ticked with its limits written INSIDE it: it does NOT say the shipped app takes this path, because governed_turn_submit_prepared has a transport and NO CALLER, ChainExecutor still drives direct AF_UNIX, and governed_verification_unconfigured still returns Some(...) unconditionally. What is proven is the ADAPTER<->SUPERVISOR round trip. Phase 1 is down to four open rows. OUTPUT-PULL DRIVER (2026-08-12): SS4.10(f)'s three pieces had never been driven against each other; they are now, by a Rust driver in the crate that owns the loop. FIRST FINDING: nothing about the protocol needed changing -- the ladder already mints the token (one OutputReadService instance is handed to both the acceptance driver and the front door), and check_frame already refused a frame without a truthy output_stream_id, so the earlier green run is itself proof the capability was there. The driver is Rust deliberately: pull_output takes a ReceiptEnvelope and has NO length or digest parameter, so a Python driver would have re-implemented the loop AND the gate -- an eighth instance of one contract with two implementations. It removed a SEVENTH on the way: chain_executor's private OwnedEnvelope, a second 23-key SS4.9 deserializer, is now governed_verification::OwnedReceiptEnvelope and the broker uses it. Five modes, four negatives refused BY NAME, plus a sign-flip control. The evidence is cross-checked against the SUPERVISOR's hop log -- the SO_PEERCRED uids the driver cannot write -- and check_pull refuses without it. TWO CAVEATS: the live pull will be SINGLE-CHUNK (322-byte output, seq never exceeds 0), though a forced 400000-byte output gave 3 reads locally; and the driver runs as root to sudo the sidecar, so it proves nothing about who may read the store. Declarations moved with the code: pull_output flipped to must_have_caller (verified RED if the driver is deleted) and two symbols hand-deleted because the gate cannot see inherent calls. 21 mutants killed, zero survivors, after three first-pass survivors were closed -- two real test gaps, one equivalent mutant whose redundant guard was DELETED. Slice 3 can be ticked once CI is green; 'Governed output delivery through the wall' STAYS OPEN -- this is a CI proof, not a product path. Engine 1979 OK. LADDER BUILD FLAG (2026-08-12): the ladder job went RED because cargo's --bin filters the WHOLE invocation, not the package it follows -- so naming three packages plus --bin ladder_output_pull built ONLY that bin, skipped the launcher, recorder and executor, and reported 'Finished in 0.11s'. The script's own binary check caught it and refused with 'missing built binary', which is what that guard is for: a build command can succeed while building the wrong thing. Split into two invocations in BOTH places it appeared, with the reason written beside each. SLICE 3 TICKED (2026-08-12): run 31621209556 at 5090e53 -- pull=ok chunks=1 served_to_uid=5003, negatives digest_mismatch/length_mismatch/stream_binding_mismatch/stream_unknown, PULL: GREEN. The SS4.10(f) pull is driven end to end and gated against the SIGNED envelope, never SS4.10(e)'s transport echo, which the API makes impossible by having no digest parameter. THREE LIMITS IN THE BOX: the live pull is SINGLE-CHUNK (322-byte output, seq never exceeds 0; a forced 400000-byte output gives 3 reads locally, so the striding loop is proven but NOT by CI); the driver runs as ROOT to sudo the sidecar, so it proves nothing about who may read the store and the egress claim rests entirely on the supervisor's SO_PEERCRED hop log; and this is a CI proof, not a product path. PHASE 1 IS DOWN TO THREE OPEN ROWS, two of which are the same fact: the shipped app does not take this path -- the broker still reads output with fs::read, the submit writer has no caller, and governed_verification_unconfigured still returns Some(...) unconditionally. Closing those is the Owner's architecture choice, not more building. BROKER ON THE LADDER (2026-08-12): the Owner ruled SS4.10(g) the production path (1d), and build_governed_executor now builds LadderChain -- prepare_governed_turn_v1b, resolve_governed_generation_config_v1b and governed_turn_submit_prepared had ZERO non-test callers between them and have one now, with their declarations flipped to must_have_caller. The decisive reason was VERIFIED: ProductionResolver::resolve ignores its request entirely when producing facts, so the shipped path really did sign what the config says. TWO PROPERTIES DO NOT SURVIVE, both recorded: the broker-side IDX-4 lease-pin compare (its stronger twin lives in the launcher, against a root-owned config the broker cannot redirect), and F-26's expected_execution_attempt_id -- which became Option and is passed None, so the TYPE records the loss instead of feeding the check the value it is meant to check, which is the F-01 oracle shape; the substitute is three UNIQUE constraints on governed_turn_acceptance. NO FALLBACK, enforced by a source-scan test. STEP 3 (removing the old code) is deliberately NOT DONE: live_turn.rs imports the direct chain from brops-broker, so removal means relocating ~1900 cfg(linux) lines into a crate with no [lib] target, unverifiable here, against the one job that proves the SS2.5 TCB floor -- but NO PRODUCTION WIRING REACHES THE DIRECT CHAIN any more. The gate did not move: src/ has ZERO diff. One wiring step deliberately not taken: the broker never calls engine_trust::record, so apply() fails and the ladder BLOCKS at transport construction -- correct fail-closed, and provisioning the broker's trust environment is a posture change, the Owner's not a Builder's. 6 mutants killed, 5 survivors named; two first-round survivors were the agent's own test bugs. brops-broker 46+3, brops-core 443. BROKER TRUST ENVIRONMENT (2026-08-12): authorised, investigated, NOT DONE -- it is the wrong question, and nothing changed. BRO_CONDUCTOR_SESSION_TOKEN binds agent_id bro-000 and role bro, which IS the conductor identity, so the broker either never claims it (token INERT -- authorize_conductor_stop returns False on is_conductor BEFORE the token is read) or claims it and becomes the conductor. No third state, and no second token can be minted. The 0700 tree holding the token also holds EIGHT retained private authority seeds, so no grant yields one without the others. DECISIVE: the transport the broker builds never sends a frame that reads any of the five variables -- governed_turn_submit.py has ZERO BRO_*/os.environ reads -- so a broker-side derivation would add a SECOND record() source, and resolve cannot tell a four-member set from a five-member one, so 'a variable added to the provisioned set is automatically covered' would silently stop covering the broker. THE REAL BLOCKER, BIGGER: GovernedSidecar::command() builds a plain Command::new(python), so the sidecar runs AS THE BROKER'S OWN UID, while SS2.6 requires broker != sidecar and every supervisor gate compares the peer uid against one configured sidecar uid -- a broker-spawned sidecar is refused at the first hop. Whose trust environment the sidecar carries cannot be answered while it is not the sidecar principal. Trap named: BROPS_BROKER_CONFIG's [trust] block is the SS4.2/SS7 key-manifest root, a different trust root; putting the engine variables there would be 'handed one by whoever starts it' wearing a config file. SIDECAR PRINCIPAL (2026-08-12): the broker now spawns the sidecar AS THE SIDECAR PRINCIPAL. The claim was verified first -- all four sidecar-facing services gate on peer_is_sidecar with strict int equality, and the front door answers 'principal collapse' BEFORE reading a frame, so the config escape is refused too. THE MAP FOUND SOMETHING THE BRIEF DID NOT: sudo RESETS the environment, so Command::env() sets variables on the process about to be discarded -- the trust set has to travel as ARGUMENTS or it silently vanishes, which is the half-wired state engine_trust's own docs call worse than no export at all. That is mutant M3 and it is killed. Shape chosen is the working reference's own: sudo -n -u as a config-supplied argv prefix, the same mechanism run_ladder_turn.sh uses. ONE SPAWN, ONE TRUST APPLICATION: SidecarPrincipal has private fields and NO value meaning 'the caller', and GovernedSidecar::new was DELETED so every call site became a compile error until it stated its principal. Refusals include a RELATIVE invoker[0] ($PATH is not a TCB input), a prefix not ending in env (it could not carry the trust set), and a prefix that never names the account. The desktop's spawn is provably unchanged three ways. 14 mutants, 14 killed. HONESTLY UNVERIFIED: build_governed_executor is cfg(linux) and was never type-checked here; mitigated by rustfmt parsing, three source-scanning guards, and a cfg(test) twin with the same types that DOES compile. The Windows half was deliberately not built -- spawn_as would need the sidecar account's PASSWORD, new secret custody, and there is no Windows broker binary that spawns a sidecar. THIS UNBLOCKS ONE OF TWO BLOCKERS: the broker still cannot resolve engine_trust::apply(), so the ladder is unreachable from brops-broker for a SECOND named reason. No gate moved. brops-core 458, broker 46+7. TRUST ARM SPLIT (2026-08-13): GovernedSidecar now takes a SidecarTrust, so the requirement follows the PROTOCOL being spawned rather than every spawn alike. Provisioned(TrustEnvironment) is the only variant holding one and bypassing it on the trusted paths is STILL a compile error; RelayFramesOnly carries nothing and buys only a NARROWER DOOR. Not a hatch, because admits() runs before the spawn and keys on THE SAME FIELD the child's _dispatch keys on -- pinned by a test that reads engine_sidecar.py and asserts both protocol checks precede the op fall-through. A task-request has no protocol and cannot grow one (additionalProperties:false, asserted); one with a relay protocol bolted on is routed BY THAT FIELD to the relay handler, which reads nothing. The zero-reads claim was verified by an AST transitive-import-closure analyser, not grep: 16 modules, zero reads, including chasing bro_signature which IS in the closure. CORRECTION TO THE BRIEF: the five BRO_* in _real_callables are NOT the provisioned trust variables -- different sets, conclusion unaffected. The child-side check was MEASURED: adding it produces 14 bridge + 6 engine failures because those suites legitimately exercise the frozen path without the set, so it is an Owner call, not a silent skip. COST STATED: the broker no longer calls engine_trust::apply, removing its one UNCONDITIONAL refusal; the ladder is now unreachable for the remaining CONFIGURATION reasons instead. 8 mutants, 8 killed, including task-request-through-the-relay-arm and deleting the admits() line. brops-core 471, broker 46+9. BROKER BINARY IN CI (2026-08-13): driving the real brops-broker to a COMPLETED governed turn in the live kit is IMPOSSIBLE, and not for want of provisioning -- two blockers inside the binary, both MEASURED not read, and nothing was built. (1) build_governed_executor can only reach ProductionResolver::provisioned, which hard-pins brops-tcb-root-1 / 3c83c2bc; the private half is the Owner's OFFLINE root, and the one constructor taking another anchor is pub(crate) in the LIBRARY so the bin cannot reach it (measured: E0624; resolve_keys over a kit-signed manifest returns UpstreamBlocked; UnknownRoot then RootSignatureInvalid when relabelled; the pinned key IS a valid Ed25519 point, so it is custody not a bad constant). Satisfying it would need an Owner-in-the-loop ceremony EVERY RUN, or committing the production signer's private half, which would make forging a production-class envelope trivial. (2) No config key wires custody: the executor is built without it, so committed_label() is None and persist_committed refuses -- the real binary cannot render a committed reply under ANY config. THE REFUSAL IS THE DESIGN HOLDING. The 16-input configuration map is complete and satisfiable (7 new to the kit) and produced three findings: trust.floor_path is root-owned 0644 so the broker CANNOT persist the anti-rollback advance; the uids block is hardcoded DEFAULT_UIDS with NO sidecar, so the SS2.5 floor's runtime-uid set is not the real accounts; and build_tcb_pin_manifest.py pins bin/live_turn as trusted-verifier-broker.bin. INSTEAD: a ladder_turn driver in brops-governed-live builds the SAME LadderChain with a kit-generated anchor -- the precedent live_turn already sets, honest because a kit_generated anchor may never render production_verified=true -- and must never be described as the broker binary. LADDERCHAIN RUN LIVE (2026-08-13): proof/src/bin/ladder_turn.rs drives the SAME LadderChain the broker builds, and it was ACTUALLY RUN on WSL2 Ubuntu on this box -- whole script, SCRIPT_EXIT=0, outcome=committed, demonstration_custody, kit_generated anchor, production_verified=false, bound=true, 30 sidecar frames every one served to SO_PEERCRED uid 5003. The three input digests are DERIVED by the real code from the fixture row, not written into the config, and equal the staged bytes and the launcher lease pins. It is NOT the brops-broker binary and says so in three places; provisioned_with_pin is still pub(crate) and no gate moved. SHIPPING DEFECT FOUND: MAX_STAGING_SESSIONS_PER_INSTALL = 6, count_install_sessions has NO liveness predicate, and governed_staging_ledger.py contains NO DELETE AT ALL -- SS2.4 makes staging recovery 'operator-sweep only' and NO SWEEPER EXISTS. One turn stages three artifacts, so AN INSTALL SUPPORTS EXACTLY TWO COMPLETING GOVERNED TURNS, EVER. Verified independently: constant at line 369, zero DELETE in the file; measured as staging-open quota_sessions with six ARTIFACT_READY sessions. A model_profile_unknown control could not run honestly as a result and is absent with the reason stated. ALSO: the kit leaves trust.floor_path root-owned 0644 so the broker cannot persist the anti-rollback advance (control drives the ORIGINAL path so it cannot be fixed by weakening the persist); and Ubuntu 26.04's sudo-rs rejects * in command arguments, so the kit's EXISTING recorder vector would fail visudo on such a runner. 13 mutants, 10 killed, 3 named survivors. SS2.4 SWEEP (2026-08-13): an install supported exactly TWO completing governed turns ever; it now supports as many as the deployment runs, proven by THREE consecutive completing turns on one install_id, run for real in WSL with KIT_EXIT=0. THE OBVIOUS FIX WOULD HAVE DELETED THE CAP WHILE APPEARING TO REPAIR IT: SS2.4 gives the liveness tolerance to the TURN cap explicitly and to nothing else, and says the session/byte caps RELY on expired rows being gone -- so count_install_sessions counting every row is the DESIGN. A liveness predicate would also make quota_sessions structurally unreachable (at most 2 live turns x UNIQUE = at most 6 live sessions), which is mutant M6 and is killed. What was missing is what SS2.4 SPECIFIES BY NAME: a 60s background sweep plus a startup pass, deleting expired rows WITHOUT consuming the challenge nonce (which is what denies the sidecar a nonce-burning DoS). THE DESIGN NAMED THE RECLAIMER AND NOBODY BUILT IT. The sweep predicate is the exact SQL complement of the one the turn cap reads, so 'counted' and 'collected' cannot drift; it refuses a connection with foreign_keys off, the one way this fail-closed cap fails OPEN; and two constants became DERIVED. One SS2.4 self-contradiction is named and left to the Architect (SESSION_CORRUPT must answer session_corrupt forever AND be deleted so the challenge can be re-used -- a deleted row answers session_unknown). The cap STILL BITES: a new test requires quota_sessions BY NAME before the sweep and admission after, both halves load-bearing. 11 mutants killed plus a kit-level no-sweeper mutant that makes the kit exit 1 naming the breached SLA; raising the cap to 600 is killed. Zero Rust and zero SQL bytes changed. Engine 1995 OK (43 skipped) from 1979. PLATFORM-PINNED TESTS, FIFTH TIME (2026-08-13): CI went RED on two tests green on Windows. Both asserted a relay frame 'reached the spawn' by matching 'Could not run the governed engine sidecar' -- but Windows has no sudo, so the distinct-principal invoker cannot start, while Linux HAS one, starts it, and dies at 'unknown user brops-sidecar', a CRASH not a spawn failure. The property those tests own is that the DOOR admitted the frame, which admits() already proves; the second half now asserts only that the refusal is not the door's, the same shape the sibling assertion already used. FIFTH platform fact hidden in a test this week: an exception's NAME, a bound in a platform branch, a fixture pinning the year 2030, /abs/x.py not being absolute on Windows, and now the presence of sudo. A test that asserts HOW something failed can only pass where it was written. PHASE 2 CHECKED AGAINST THE CODE + A-05 SECOND HALF (2026-08-15, T-019, all marks are the Builder's own claim): the roadmap-order exemption opened Phase 2 while all four of its pages already existed, so the first act was VERIFICATION not construction. Eleven boxes checked against the source with evidence beside each; SIX ticked. The five that are not reduce to TWO FACTS. (1) The approval-REQUEST path exists on NEITHER SIDE: no approval-request schema in engine/schemas (21 schemas, none is one), no desktop->engine command, and the approvals page's grant/deny/escalate drive the DESKTOP'S OWN approval system (T-010/T-011 over local SQLite behind a native confirmation the webview cannot forge) - a real authority, correctly gated, and not a request across the wall. Phase 2's own Contracts row pre-authorised this: a shape needing an engine schema change is 'an audited engine task, flagged, not done here'. (2) security's SS-D sigbreathe integrity pulse is DELIBERATELY NOT APPLIED - Security.tsx renders a non-live wire because the posture is blocked, and the motion would make the page say something the data does not support; a wording question, not a build task, left to the Owner. ONE SS-D GAP WAS CLOSED: g was bound to grant (it was missing, so a keyboard owner could deny and escalate by keystroke and not grant) and it STAGES the same confirm dialog d/e stage rather than committing on one keypress, which would have made the deliberate press-and-hold bypassable by the binding meant to complete it; two tests, both mutants killed. A STALE CLAIM CORRECTED: governance.rs opened with 'the Phase-2 engine read endpoints do not answer yet' - they answer (bro_control_room_api.GOVERNANCE_SURFACES:47, governance_read:568, engine_sidecar.py:477 relays verbatim); what is still true is narrower, a shipped install reaches Blocked because nothing sets BROPS_GOVERNANCE_STATE_DIR, so the reason is a deployment input and not a missing endpoint. TWO BOXES GAINED THE CHECK THEY WERE RESTING ON: 'no desktop-side decision authority' is now read out of the module's own source (every command must take nothing but an optional task_id; a mutant growing a key_id parameter is killed), and 'chain-break -> blocked' gained the second door (a malformed link arriving in the records, with a positive control) - with the limit written INSIDE the box: the desktop does NOT walk the chain, and re-deriving a head from records it cannot authenticate would be a check that cannot fail, so fork detection stays the supervisor's on both platforms. A-05 SECOND HALF CLOSED (the ledger's own 'no test feeds a Linux-written chain to the Windows parser'): servers.rs::linux_written_chain_tests builds the chain with the LINUX RECORDER'S shapes and ITS OWN ENCODER (serde_json::to_vec, never crypto::jcs - the parser's rule would have made it a tautology) and feeds it to derive_evidence; the fixture cannot drift because one test READS proof/src/bin/governed_recorder.rs and asserts every event type, payload key and the serde rule still appear in it. 6 MUTANTS, 4 KILLED, 2 NAMED SURVIVORS - AND THE SURVIVORS ARE THE FINDING: M1/M2 put the parser back on serde_json::to_vec and SURVIVE, because with preserve_order off a Map IS a BTreeMap and the two calls are byte-identical for every input, so the A-05 fix changed the RULE NAMED IN THE CODE and not one byte on the wire. What it genuinely bought is now guarded: M6 turns preserve_order on and kills the_parsers_rule_is_canonical_rather_than_textual and ONLY that test, because crypto::jcs sorts ONE LEVEL and hands the nested payload to serde untouched (its doc comment says 'a FLAT object'; an evidence event is not flat). M4 SURVIVED ITS FIRST RUN, masked by its neighbour - editing a payload breaks the link too, so the link check refused first - and was closed by a negative only that check can catch: a recorder that chains correctly and LIES about its own payload_sha256. B-02 LOOKED AT AND DELIBERATELY LEFT OPEN: moving the pin into the supervisor stops the authority refusing a foreign install_id at the door (a misconfiguration then travels three hops before failing), duplicating it creates a second site that must agree with the first about which install_id is this install's - the 'one contract, two implementations' shape found EIGHT times now, in the one place where disagreement silently widens an anti-rollback scope. Both change WHICH PRINCIPAL IS AUTHORITATIVE, which is SS-I items 1 and 2, and SS-I makes bypassing it a stop condition; the 1b decision settles it. Measured: brops --lib governance 29 (from 27), brops-win-live 107 (from 103), Approvals frontend 5 (from 3); all 19 gates run - sixteen GREEN, three print usage, one RED for the documented check_bundle_budget reason. NOTHING GOVERNED BECAME REACHABLE and no gate moved. CANONICAL-LAW WALL WAS OFF FOR T-019, AND TWO TENSES WERE WRONG (2026-08-15, Builder's own claim): .claude/settings.json wires five events and every one is addressed $CLAUDE_PROJECT_DIR/.claude/hooks/, so a session opened at a DIFFERENT root that then works inside OS/ gets none of them. T-019 ran that way (project root Desktop\\ENGER, work in Desktop\\OS, CLAUDE_PROJECT_DIR empty), so for the whole task there was NO full-read receipt, NO roadmap-phase declaration, NO prior-art check and NO Stop coordination guard; check_read_receipt.py --verify answers 'RED: no full-read receipt for this session'. Same shape as the engine/ hook bullet one level up: hooks load from the root the session was opened at and their absence announces itself nowhere. THE CI BACKSTOP HELD and that is the difference between a gap and an incident - check_canonical_sync at every commit, check_coordination and check_repo_state at every push, 34 checks on the exact merged head - which is exactly the division of labour the gate's own SHELL IS NOT GATED limit describes. CONSEQUENCE TAKEN RATHER THAN ARGUED AROUND: a session that cannot produce a receipt MAY NOT create a new canonical file (rule 1 wants the receipt, rule 4 a recorded prior-art search); --record would forge it in a keystroke and the gate's own text names that hole, so the floor-writer design proposal is NOT written by that session and needs one opened at Desktop\\OS where the hook fires and records the receipt honestly by construction. AND 1b IS NOT PENDING - it was DECIDED 2026-08-14 (floor-writer service, option 1 taken), which makes the B-02 row's closing sentence wrong by one tense; the Owner's reason 3 already answers B-02, naming A-01 and B-02 as two faces of one defect - the floor is controlled by its own subject - and calling a resident principal that owns the marks directory the natural place to pin install_id from trusted config, both defects closed at the same boundary. WHAT IS ACTUALLY MISSING IS SS-I STEP 2: the decision ships with 'Owner approval (given) -> Architect audit -> implement, no implementation lands on this decision alone', and the Architect has nothing to audit because NO floor-writer design exists (docs/design/ holds twelve documents, none is one; 'floor-writer' appears in exactly one file, the page that decided it). Authoring that proposal is the Builder's per the roadmap ownership matrix (proposal 🔨, audit 📐 mandatory) and is the single highest-value next task.",
  "settled_at_main_head": "3da4cd10e9093731ade1dc8d0ffd5ca3dcbb39cd",
  "sync": {
    "baseline_main_head_at_sync": "3da4cd10e9093731ade1dc8d0ffd5ca3dcbb39cd",
    "snapshot_branch": "phase4-analytics-scrubber",
    "live_main_resolution": "Read settled_at_main_head: live main is b3010f6 (PR #81 merged, 2026-08-09), baseline_main_head_at_sync now equals it, and the only open pull request is the self-carrier #82 that records the settle. No implementation work is queued. Until 2026-08-09 this field said the baseline WAS the live HEAD 0efa99e (PR #65 merged) and named an active branch chore/staleness-sweep that no longer exists - stale by roughly sixteen merged PRs, in the machine-readable file the human docs are checked against.",
    "verification": "check_repo_state is EXACT-HEAD fail-closed. There are no durable child PRs in prs[] right now (the single active self-carrier is PR #122). PR #122's own exact-head is anchored on its branch via its PR-body AUDIT_CANDIDATE_HEAD marker, kept in lockstep with every push (event head == live headRefOid == marker). Nothing is exempt."
  },
  "active": {
    "wave": "3b-1B",
    "task": "T-017",
    "branch": "phase4-analytics-scrubber"
  },
  "prs": [
    {"number": 112, "branch": "design/floor-writer-service", "base": "main", "draft": false, "merge_state": "open", "role": "design", "head": "3d0f053a79e1e3de118c6ce6ebab8e17e039c1f2", "why_listed": "Open and NOT the carrier: The floor-writer service has a design now (T-020) \u2014 a proposal, not an implementation. Listed so the settled snapshot names it; its exact head is anchored."}
  ],
  "status_tokens": {
    "_note": "Every human state doc's CURRENT region must contain each KEY: value token verbatim. No carrier-transition is modeled (PR #48 is a self-carrier that does not merge to repair main), so there are no CARRIER_IF_* tokens.",
    "CURRENT_ACTIVE_TASK": "T-017",
    "CURRENT_ACTIVE_WAVE": "3b-1B",
    "CURRENT_PHASE0": "done",
    "CURRENT_DESIGN_GATE": "OWNER_APPROVED_NOT_ARCHITECT_AUDITED",
    "CURRENT_DESIGN_CANDIDATE": "rev-30",
    "CURRENT_LAST_REVIEWED": "rev-30",
    "CURRENT_LAST_VERDICT": "OWNER_APPROVED_NOT_ARCHITECT_AUDITED",
    "CURRENT_DESIGN_PR": "48",
    "CURRENT_IMPL_PR": "48",
    "CURRENT_IMPL_STATE": "consolidated",
    "CURRENT_CODE_AUDIT": "ARCHITECT_PENDING",
    "CURRENT_LINUX_E2E": "proven",
    "CURRENT_WINDOWS_LIVE_PROOF": "proven",
    "CURRENT_PRODUCTION_VERIFIED": "false",
    "CURRENT_VERIFY_SEAM": "complete",
    "CURRENT_RECEIPT_PLUMBING": "complete",
    "CURRENT_GOVERNED_ROUNDTRIP": "complete"
  },
  "design_gate": {
    "candidate": "rev-30 (docs/design/WAVE_3B1B_EXECUTION_BINDING_ADDENDUM.md) carried on PR #48",
    "candidate_head_note": "The exact audit HEAD is PR #48's live head 38d5d71504ba68b70b015b958cb09109c80e595a, recorded in the PR #48 body AUDIT_CANDIDATE_HEAD marker.",
    "last_reviewed_candidate": "rev-30",
    "last_architect_verdict": "GREEN",
    "last_architect_verdict_scope": "rev-30 = Architect DESIGN GREEN (relayed by the Owner). Supersedes the stale rev-27 RED / rev-28 pending history.",
    "current_candidate": "rev-30",
    "current_candidate_gate": "GREEN",
    "note": "Design-GREEN is NOT code-GREEN. The consolidated implementation on PR #48 still owes the external Architect CODE-audit. Do NOT claim Architect code-GREEN, and do NOT expose production trusted_verified in the shipped app."
  },
  "code_audit": {
    "builder_passes": "3 independent adversarial security passes converged: 10 -> 6 -> 1 P1, ALL fixed; trust-boundary / chain / manifest CLEAN.",
    "authority": "This is the BUILDER's evidence only. The external Architect CODE-audit gate is still PENDING; builder convergence is NOT the Architect verdict.",
    "gate": "ARCHITECT_PENDING"
  },
  "custody_and_audit_2026_08_04": {
    "production_custody_proven_local": "The Owner ran the offline-root ceremony live: win_provision verified the offline seed against the TCB pin 3c83c2bc (else exit 3) + signed the manifest offline; win_live_turn over real named pipes (3 servers + executor, same-account) reached RESULT: trusted_verified(production signer id brops-live-signer-1, epoch 2) production_verified=true bound=true under the REAL production root — the honest graduation from demonstration custody. This is a LOCAL proof on the Owner's box, NOT a CI artifact and NOT the shipped badge.",
    "builder_audit": "Owner-designated builder-side adversarial audit (5 fresh-context reviewers, one per dimension, tasked to BREAK the chain). Verdict NOT GREEN but CENTRAL GUARANTEE HOLDS: no fresh production forgery, root custody + output->receipt binding + server-side peer-SID gate all sound. Full verdict: apps/desktop/src-tauri/win-live/proof/BUILDER_AUDIT_VERDICT_2026-08-04.md. This is builder evidence, NOT the independent Architect verdict (still PENDING).",
    "fixed": "P0-2 pipe squat+impersonation (f28b559: client SECURITY_IDENTIFICATION + server FILE_FLAG_FIRST_PIPE_INSTANCE); P0-1 anti-rollback FALSE CLAIM corrected (f28b559: floor signature is a corruption check, the real boundary is the deploy-dir OS write-ACL — no fake verification added; real closure in WINDOWS_ANTIROLLBACK_HARDENING.md); dim1-P2 production_verified/demonstration_custody UI honesty (6fdedb8).",
    "open_gated": "P0-2/P0-3 executor->real-model + wire containment into the live spawn (needs model image + session-0 isolation); seed-at-rest plaintext TOFU window (cross-account tradeoff); anti-rollback real closure (ACL + per-deploy sealed floor key + TPM counter); receipt/attest key_usage encoding. Shipped platform_governed_execution_supported() stays FALSE until these land + an INDEPENDENT audit passes + Owner approval. Badge never faked."
  },
  "roadmap_dod_verified_2026_08_04": {
    "method": "Evidence-based per-phase verification (5 independent reviewers) of MASTER_EXECUTION_ROADMAP.md Definition-of-Done vs the actual codebase; roadmap checkboxes were NOT trusted.",
    "tally": "Phase 0 Locked; Phase 1 (Bridge) 4/7 MET (contracts+adapter+GovernedEngine variant+bridge CI real; round-trip only in the live kit, streaming absent, badge 2/5 states, Settings toggle disabled/read-only); Phase 2 (Gov Sidecar) 0/5 full (pages exist but the engine-mirror read IPC is unconfigured+fail-closed so governance data is local SQLite, and grant/deny is adjudicated locally); Phase 3 (Desktop Integration) 5/5 substantially MET (shell+router+22 pages+chat; gaps: route ErrorBoundary [now CLOSED 3ecf8de], 1..9 shell keys); Phase 4 (UI/UX) 3/5 MET (component lib+design docs; gaps CLOSED 3ecf8de: token-drift+contrast CI gate, per-page i18n parity guard; still open: orphaned ThemeProvider/tokens.ts, Analytics splits); Phase 5 (Memory/Knowledge) 1/5 (pages+CRUD real; governed research receipts + chat recall NOT built); Phase 6 (Multi-Agent) 0/5 full (agents/command/tasks/projects real data; governed pack dispatch + per-agent receipts NOT built); Phase 7 (Group Chat) 0/4 full (room+roster+@mention+isolation+attribution real; handoff/consensus + verified-in-room NOT built); Phase 8 (Automation) 1/5 (display+CRUD only; NO scheduler/execution at all); Phase 9 (Integrations) 1/5 (page complete; connector backend/secret-delegation NOT built); Phase 10 (Production) 0/6 (signed build/updater/onboarding/T-005/contracts-dedupe/O-1..O-5 all open).",
    "headline": "~40% genuinely done (foundation + bridge contracts + 22-page shell + component library + local CRUD + the proven crypto chain). The remaining ~60% is the GOVERNANCE SPINE + PRODUCTION. The majority of remaining DoD items REQUIRE verified receipts / governed dispatch / engine adjudication, which cannot be honestly closed until the production trust gate is real — and that gate legitimately requires executor->real-model + session-0 isolation + an INDEPENDENT audit + Owner approval (Owner approval alone does not satisfy the technical/audit prerequisites). The shipped Verified gate stays fail-closed; the badge is never faked.",
    "closed_this_pass": "Phase-3 route ErrorBoundary; Phase-4 token-drift+contrast CI gate (design-gates.yml) + per-page i18n parity guard; bridge/README stale 'receipt.verified==true' claim corrected to the real desktop-verifies-signed-receipt design; docs/SECURITY_MODEL.md created (Phase-10 doc DoD)."
  },
  "linux_e2e": {
    "status": "proven",
    "note": "The FULL 7-service production governed turn ran GREEN on real Linux — the FIRST production trusted_verified proven live (real service accounts, real setuid launcher -> executor, real ed25519 keys + root-signed manifest, verify_and_accept). Proven via engine/ci/live/run_live_turn.sh.",
    "shipped_desktop": "The SHIPPED desktop app's production 'Verified' is STILL fail-closed: the broker serves UpstreamBlockedExecutor unless $BROPS_BROKER_CONFIG names a deployment config with a TCB-root-signed manifest, which nothing in the shipped app sets; the live chain is proven in the live kit, not yet wired into the desktop runtime. CURRENT_PRODUCTION_VERIFIED stays false for the shipped app."
  },
  "waves": {
    "3a": {
      "status": "complete",
      "code_exists": true,
      "note": "slices 1/2/3 merged; migrations through 0014"
    },
    "3b-0": {
      "status": "design_green_merged",
      "code_exists": false,
      "note": "isolated-signer design rev 5 GREEN, merged PR #30"
    },
    "3b-1A": {
      "status": "code_green",
      "code_exists": true,
      "note": "isolated-signer boundary code; Architect Code GREEN; consolidated onto feat/cockpit-pages"
    },
    "3b-1B": {
      "status": "design_green_rev30_code_consolidated_audit_pending",
      "code_exists": true,
      "impl_pr": 48,
      "design_pr": 48,
      "note": "Design rev-30 = Architect DESIGN GREEN. Implementation consolidated on feat/cockpit-pages (PR #48); 3 builder security passes converged (all P1 fixed) but the external Architect CODE-audit is still pending. Design-green != code-green."
    },
    "3b-2": {
      "status": "implemented_live",
      "code_exists": true,
      "note": "signed manifest / anti-rollback implemented + wired in the live kit; proven in the Linux end-to-end production turn. Not yet wired into the shipped desktop runtime."
    },
    "3b-3": {
      "status": "implemented_live",
      "code_exists": true,
      "note": "production trust resolver implemented + wired in the live kit; drove the first live production trusted_verified. Not yet wired into the shipped desktop runtime."
    }
  },
  "closed_stale_prs": [
    {
      "number": 13,
      "reason": "superseded"
    },
    {
      "number": 12,
      "reason": "superseded after salvage"
    },
    {
      "number": 46,
      "reason": "superseded — the 3b-1B implementation snapshot consolidated into PR #48 (feat/cockpit-pages)."
    },
    {
      "number": 31,
      "reason": "superseded — the 3b-1A code + 3b-1B design (now rev-30 GREEN) consolidated into PR #48."
    },
    {
      "number": 32,
      "reason": "superseded — the execution-binding WIP consolidated into PR #48."
    }
  ],
  "merged_prs": [
    {
      "number": 33,
      "reason": "Phase 0 repository-truth remediation; Owner-approved GREEN at 45f3793; merged b6c6712."
    },
    {
      "number": 30,
      "reason": "Wave 3b-0 isolated-signer design GREEN; merged df3c0ac."
    },
    {
      "number": 54,
      "reason": "Release-workflow supply-chain hotfix; the setup-node pin named actions/setup-PYTHON's commit so no v* tag push ever produced an installer. Merged 2026-08-06 at a79c662."
    },
    {
      "number": 53,
      "reason": "Windows LIVE governed-turn machine-proof + the 2026-08-06 independent-audit remediation (F-01/F-02/F-06/F-08/F-13/F-14/F-17/F-07/F-28 and more). Merged 2026-08-06."
    },
    {
      "number": 55,
      "reason": "Audit F-10 second half: the §2.5 TCB pin manifest and real per-service IPC policies. Merged 2026-08-06."
    },
    {
      "number": 56,
      "reason": "Round-1 response to the RED remediation audit: P0 closed on both platforms, ledger corrected. Merged 2026-08-06."
    },
    {
      "number": 57,
      "reason": "Round-2 response: seed-custody P1, F-08 enforcement tests + negative live case, governed-crates CI job, answer-provenance honesty. Merged 2026-08-06."
    },
    {
      "number": 58,
      "reason": "Ledger accuracy + the ✅/◑ legend after rounds 1-2. Merged 2026-08-06."
    },
    {
      "number": 59,
      "reason": "The spec-conformance gate: a § reference is a claim the build checks. Merged 2026-08-07."
    },
    {
      "number": 60,
      "reason": "R3-01: the isolated signer re-verifies chain documents instead of checking existence. Merged 2026-08-07."
    },
    {
      "number": 61,
      "reason": "Round-3 waves 1-2: the recorder identity borrow, the trust-verdict tautologies, Windows custody and the UI's false governance claims. Merged 2026-08-07."
    },
    {
      "number": 62,
      "reason": "Round-3 wave 3: durable replay defences, inode-bound store inputs, the Windows twin's missing checks, §1.5 step 4. Merged 2026-08-07 with the live-turn job RED — fixed by PR #63."
    }
  ],
  "product_roadmap": {
    "note": "MASTER_EXECUTION_ROADMAP.md v1.0 Locked. Whole v1 app NOT finished — large multi-wave build remains. The chain is now proven live on Linux, but the shipped desktop 'Verified' stays fail-closed until the live chain is wired into the desktop runtime and the Architect CODE-audit passes.",
    "phase_0": "done",
    "phase_1": "in_progress",
    "phases_2_to_10": "not_done",
    "cockpit": "22-page cockpit built + wired to real backends; app functional. Phase-4 shared component-library primitives added (DataTable, Drawer, Rail, StatTile/TileGroup, Mark/LiveMark, InlineAlert, and a Beatline chart with accessible summary + table fallback) with full state/keyboard/aria/reduced-motion coverage, catalogued in docs/DESIGN_SYSTEM.md, plus a tokens.ts<->tokens.css drift gate (tools/check_token_parity.py). The library was then EXTENDED (interactive StripChart, horizontal BarChart, a panel-variant Rail, a responsive Mark) and the six Phase-3/4 pages refactored onto the primitives (Home tiles, Conversations/Command rails, Activity strip, Analytics bars, Shell mark; the Agents radial lattice is intentionally bespoke, documented). Phase-2 (Governance Sidecar) desktop READ-ONLY governance IPC added (src-tauri/src/governance.rs: read decision-ledger / evidence-chain / verifier-verdicts / engine approval-queue -> typed Ok|Blocked|Unreachable, schema-validated, mirror-never-decide by construction) and the four governance pages wired to it, rendering honest blocked/unreachable from the real result (the engine has no governance.read op yet, so every surface is blocked today -- the expected Phase-2 DoD state). frontend typecheck + 153 tests + 11 axe specs + bundle-budget (84.6/130) all green; cargo test -p brops 59 (17 new governance).",
    "ai_entry_points_governed": "Phase 2 COMPLETE on main (#50/#51/#52 merged): all four AI surfaces — stream_reply (main seam), reply_in_conversation (#50), stream_ask (#51), stream_run_step (#52) — now route through ai::governed_turn. The generic provider entries (ai::generate / ai::generate_stream) are a dev-only fallthrough (BROPS_ALLOW_UNGOVERNED), fail-closed by default; no production command reaches the model provider except through the governance wall. NoTrustedManifest fail-closed means production still Blocks (no trusted_verified ships). Higher-phase surfaces (automations, group chat, integrations) are built in Phases 6-9 and are governed by construction there.",
    "windows_production_isolation": "governed-turn machine-PROVEN in the live kit; SHIPPED gate still false (release blocker for the shipped app). The PURE §0.W core (core/src/windows_broker.rs) covers all five primitives cross-platform + unit-tested; TWO real syscall primitives (named-pipe peer-SID auth via ImpersonateNamedPipeClient; Authenticode via WinVerifyTrust) run on a windows-latest CI runner (crate brops-win-broker). NEW: the brops-win-live crate (apps/desktop/src-tauri/win-live) is the Rust twin of the Linux engine/ci/live kit and PROVES the FULL Windows governed turn to production trusted_verified: (1) an in-process full crypto chain (challenge -> lease -> attest -> sign -> verify_and_accept -> production_verified=true bound=true) that is HOST-INDEPENDENT and runs on the Linux CI runner (cargo test -p brops-win-live), so the one cross-impl boundary (the signer's JCS(payload) vs brops-core ReceiptEnvelope::payload_jcs) is byte-exact; (2) the same GovernedChain over REAL \\\\.\\pipe\\ named pipes across three separate Windows processes, same-account -> trusted_verified (production signer key, epoch 2); (3) the peer-SID gate fail-closed BOTH directions (correct broker SID -> verified, wrong broker SID -> blocked); (4) CROSS-ACCOUNT -> challenge-authority/supervisor/isolated-signer each as a DISTINCT dedicated Windows service account (session-0, batch logon), peer-SID authed across real account boundaries -> trusted_verified (see win-live/proof/CROSS_ACCOUNT_PROOF.md + win_live_proof.ps1). REMAINING to flip platform_governed_execution_supported() true in the SHIPPED app: the broker as its OWN dedicated service account (blocked by a session-0 console-launch STATUS_DLL_INIT_FAILED 0xC0000142 limitation that also affects the previously-working pipe_proof — environment, orthogonal to the chain), CreateProcessAsUser under a restricted token + STARTUPINFOEX handle list, CNG key custody, and a SEPARATE Architect audit of the Windows broker. Until then platform_governed_execution_supported() stays false on Windows and the shipped app fails closed.",
    "windows_live_proof": "PROVEN in the brops-win-live kit (PR #53): in-process CI-portable crypto chain + real-named-pipe same-account (3 processes) + peer-SID gate both directions + cross-account (3 distinct dedicated service accounts). An INDEPENDENT adversarial audit (Architect-role, zero-trust) returned Linux/core-chain GREEN (verify_and_accept unforgeable by an in-scope adversary; shipped app genuinely fail-closed) and Windows-kit YELLOW with three P1/P2 gaps, ALL now ADDRESSED: (P1-a) the production-trust root is TCB-PINNED (compiled-in crate::tcb, never config.json; a config root that disagrees is refused); (P1-b) a production ManifestResolver now verifies the manifest vs the TCB root + anti-rollback + resolves the production key INSIDE the chain resolution, feeding the pinned key verify_and_accept verifies under (no more verify-beside-acceptance side-call); (P2) the anti-rollback floor is now PERSISTED atomically on resolve. A follow-up INDEPENDENT RE-AUDIT confirmed all three originals CLOSED and surfaced two residuals, now also fixed: R1 (the anti-rollback floor is TCB-SIGNED — floor.sig under a compiled-in TCB floor key — so a config-dir adversary who resets floor.json to replay an older genuinely-root-signed manifest is REJECTED on load; unit-tested) and R2 (the supervisor-attestation key is now resolved through resolve_production_key, enforcing trust_class/validity-window/revocation, not a bare key_id lookup), and R3 (attest-run is now BOUND to a supervisor-minted accept-open lease — one-time consume of the leased execution_attempt_id — so the supervisor never attests a run it did not itself lease). The restricted-token executor launch primitive (CreateRestrictedToken + CreateProcessAsUserW, §0.1 primitive 4) is also proven (win-broker restricted_launch). The broker-in-session-0 concern is closed by running the broker as SYSTEM (the TCB principal). The broker-in-session-0 concern is now CLOSED: the full cross-account turn is proven with the broker running as SYSTEM (LocalSystem) — the correct TCB deployment (holds SeAssignPrimaryToken/SeImpersonate, runs cleanly in session 0) — plus the three servers as distinct dedicated service accounts, peer-SID authed (allowlist = the broker SYSTEM SID S-1-5-18) -> trusted_verified. The earlier 0xC0000142 limitation only affected a NON-privileged service-account CONSOLE process doing child-spawning, not SYSTEM. Seed custody is now DONE: each principal seed is under per-user DPAPI (CryptProtectData, seal-on-first-use TOFU) so it is no longer plaintext at rest — only the owning service account can decrypt it (proven: a 64-byte plaintext seed becomes a 262-byte DPAPI blob after first server read, and a restart transparently unseals it). INDEPENDENT AUDIT VERDICT: GREEN for the shipped fail-closed posture + the crypto-verification core — three independent adversarial passes confirm NO in-scope adversary can forge a production trusted_verified, and ALL findings (P1-a, P1-b, P2, R1, R2, R3, plus A root-private-off-the-box and D atomic-signed-floor) are closed and wired; see win-live/proof/WINDOWS_BROKER_AUDIT_VERDICT.md. The Owner waived the external ChatGPT/Architect audit; this independent audit stands as the gate verdict. platform_governed_execution_supported() stays FALSE (correctly); flipping the Windows governed gate to true is a separate deployment-hardening step; enablement work is UNDERWAY: (1) production key custody ROOT DONE — the TCB compiles in only the root PUBLIC key, the operator's offline root private signs the manifest via win_provision --root-key and is NEVER on the serving box (proven: keys/root.seed absent, turn still trusted_verified); the runtime floor-integrity key remains a compiled-in constant to harden. (2) DONE — the broker now runs as a dedicated NON-SYSTEM service account (brops-broker), proven cross-account -> trusted_verified with the three servers as their own accounts (this required building the win-live bins with -C target-feature=+crt-static; the earlier 0xC0000142 was a debug-CRT DLL dependency a limited session-0 service account could not load), so the broker cannot read the signer service's memory/DPAPI seed and allowed_broker_sid is that account's exclusive SID. (3) closed against the in-scope login-user adversary via an ACL-protected TCB-signed floor (the deploy ACL grants floor write only to the broker principal and denies the interactive login user; floor.sig adds tamper-evidence), with hardware TPM/monotonic freshness remaining only for the full-admin-compromise case (out of scope, same class as admin-defeats-DPAPI). Net: all three enablement conditions are addressed end-to-end; what remains is the runtime floor-key custody sub-item, TPM anti-rollback for admin compromise, and the Owner decision to flip the shipped gate + wire the chain into the desktop runtime. Does NOT ship trusted_verified; the shipped Windows gate stays fail-closed."
  },
  "desktop_release": {
    "tag": "brops-desktop-v0.1.0",
    "name": "BroPS 0.1.0 desktop",
    "published_at": "2026-08-03",
    "target": "feat/windows-broker-machineproof",
    "assets": [
      "BroPS_0.1.0_x64-setup.exe",
      "BroPS_0.1.0_x64_en-US.msi"
    ],
    "note": "First tagged GitHub Release (2026-08-03): the Windows installer (NSIS .exe + MSI). Cut from the feat/windows-broker-machineproof branch BEFORE PR #53 merged; PR #53 has since merged to main, and this note said 'the branch is NOT merged to main' until 2026-08-09. Shipping the installer does NOT change the fail-closed posture: no function named platform_governed_execution_supported() exists (it is the section 0.1 spec symbol), and what refuses is governed_verification_unconfigured() returning Some(...) unconditionally, connect_broker() refusing off Linux, and the broker falling back to UpstreamBlockedExecutor because nothing sets $BROPS_BROKER_CONFIG. No production trusted_verified renders in the shipped app."
  },
  "shipped_this_cycle": {
    "range": "1bda438..462edc5",
    "branch_head": "462edc5",
    "ci": "green",
    "custody": "Production key custody graduated: offline root-key ceremony (win_gen_root + CUSTODY_CEREMONY.md); the operator's production root is TCB-pinned with its private half OFFLINE, and a SEPARATE compiled-in demonstration anchor drives the in-process proof/tests; per-principal seeds sealed at rest via DPAPI (TOFU).",
    "in_app_agent": "The in-app Bro became a bounded, owner-authorized coding agent with file tools + Bash in acceptEdits, behind a hard --disallowedTools deny-list (never push/delete/install); the agent-mode 'claude CLI timed out' bug is fixed (runs with --setting-sources \"\" + a 900s budget vs a chat's 180s).",
    "cockpit_ux": "Chat Stop button (cancels the turn, keeps the partial via cancel_reply) + type-while-thinking queue + 1-click copy, in direct and group chat; responsive hamburger drawer below 860px + 1560px wide-screen cap + lighter ambient; right-click open-in-new-window (open_window) + route<->URL-hash sync + Esc-close. i18n/nav/capability(cancel_reply+open_window declared for the T-010 gate)/undici(7.29.0) fixes.",
    "audits": "Every finding from TWO independent adversarial audits fixed; the Windows-broker independent verdict is GREEN and the Owner waived the external ChatGPT/Architect audit (three adversarial passes stand as the gate verdict). Group-chat speaker attribution preserved in the model transcript.",
    "schema_at_head": "Migrations run through 0020 (…0018_demonstration_verified, 0019_approval_escalated_status, 0020_automation_runs — the automation execution run-log) and SCHEMA_VERSION=20, committed on PR #53. brops-core migration tests updated to 19 and assert the new tables/guards; capability gate GREEN at 80 gated commands + 6 explicitly allowlisted-ungated (the check_capabilities checker now sees module-scoped commands and enforces an INTENTIONALLY_UNGATED allowlist).",
    "post_97483f2_this_cycle": "Six commits after the earlier 97483f2 pointer, all on PR #53: 10de331+62f31a4 supply-chain reconciliation (cryptography kept at 46.0.3 — the 50.0.0 bump broke the engine governance runtime — with the 3 X.509/PKCS#7 advisories GHSA-m2h6/g6cj/jwv3 waived in pip-audit-ignore, justified because the engine uses only Ed25519; undici pinned ^7.29.0); 3a68bc1 group-chat speaker-attribution hardening (sanitize colon-strip + roster authority guard so the renderer can't forge a 'Name:' attribution, roster carried into the model system prompt, participant rail made truthful); d658de2 annotated the Wave-3a governed-accept author hardcode as a documented latent gap (P0-1; fail-closed today); a6e69c6 added proof::in_process_turn_output — the governed chain can now sign+bind a REAL reply's bytes (the wiring foundation), still fail-closed and unused by the shipped runtime. No gate/trust-semantics change; shipped 'Verified' stays fail-closed. Then an audit-remediation pass (from the comprehensive what's-left audit) added 7ed23a3 (this head-pointer refresh + roadmap canonical banner — P1-5/P2-4) and 75af658 (per-page smoke tests for Approvals/Command/Files locking their mirror-never-decide honesty invariants + jsdom matchMedia/scrollIntoView test stubs — P2-1; frontend suite 14 files / 166 tests green, tsc clean). Two further coverage passes extended P2-1 to the next-tier surfaces: c89f818 (Security integrity-never-fabricated + Notifications mark-read + Agents roster; ResizeObserver test stub — CI 19/19 green) and 7e95316 (Projects + Tasks store-mirror). Eight previously-untested pages now have smoke tests locking their mirror-never-fabricate invariants; full suite 19 files / 172 tests green, tsc clean. The audit's P1-2 (consolidate the 4 copy-pasted governed blocks in commands.rs) and P0-1 (Wave-3a governed-accept author) were DEFERRED as trust-critical refactors whose payoff is gated on the live-wiring that needs the sidecar/custody infra — not landed unreviewed. Then d2364d7 closed the ENTIRE P2-1 gap (smoke tests for the remaining 11 pages — Knowledge/Memory/Research/Library/Calendar/Automations/Integrations/Analytics/Activity/Settings/Home; every feature page now has a dedicated test; 30 files / 183 tests green), and 6a79780 landed a safe pure-verifier Windows-broker hardening slice (P1-3): windows_broker.rs verify_startupinfo_handle_list now rejects a DuplicateHandleSlot (a repeated 0..=6 slot would have smuggled an extra inherited handle past the FD-count floor) — strictly more fail-closed, no trust/gate change, brops-core 15/15 green. The remaining Windows hardening (real CreateProcessAsUser + restricted-token + STARTUPINFOEX syscalls, CNG key custody, dedicated service account) stays gated on a Windows session-0 test environment + a separate Architect audit; P2-2 (engine test worktree skips) stays deferred as it is tied to the enforcement-wall root-model (Phase-1 decision), not a harness bug.",
    "session_audit_and_features_2026_08_04": "Latest additive work on PR #53 (head 9a67a91): (1) shipped the opt-in live-CHAT DEMONSTRATION-verified reply — a 'Demo-verify' action produces one reply INSIDE the in-process governed chain over the configured model (BROPS_SELFTEST_MODEL_CMD), verifies it, and surfaces a DB-derived 'demonstration_verified' 'Verified · demo' info-badge via MESSAGE_RECEIPT_PROJECTION COALESCE over the additive 0018 table; real crypto + real answer but demonstration custody, NEVER production trusted_verified; command demonstration_verified_reply, migration 0018, T-010-declared. (2) Wired the Approvals Escalate action for real (escalate_approval command, repo::approvals::escalate → status 'escalated' + level A3 + owner notification, non-verdict/decides-nothing, migration 0019 status guard). (3) Ran TWO multi-agent code audits (an area-based sweep = 20 confirmed; a 10-dimension deep audit = 19 confirmed incl. 1 HIGH), each finding adversarially verified before fixing, and fixed every confirmed non-gated finding: HIGH honesty (demonstration_verified_reply trusted the renderer author verbatim → author-forgery + transcript injection; now sanitize_author + roster validation + byte-exact verified body + atomic post+record); the T-010 capability-checker BLIND SPOT (its regex matched only commands::/files::, so governed_turn_execute + the 4 governance::read_* mirrors + governed_trust_selftest were outside the manifest AND invisible to the checker — now captures every module prefix + reconciles against an explicit INTENTIONALLY_UNGATED allowlist so a new module-scoped command that is neither declared nor allowlisted fails CI; build.rs invariant + checker self-tests updated); state-machine guards (set_step_status rejects moving a live-claimed step — closes a T-011 in-flight side-step; set_run_status rejects forcing 'succeeded'/un-terminating a terminal run); Rust fail-closed (servers.rs accept_open non-string identity panic → refuse; restricted_launch integrity hard-fail + privilege fail-open → fail-closed; atomic reject-fail; open_window count-then-build TOCTOU → mutex; demonstration subprocess bounded — backgrounded stdin + 9MiB stdout cap + 180s recv_timeout, std-only); frontend (MessageThread unmount stream-cancel leak; recall reflects session turns; receiptBadge demo + fail-closed honesty tests; DirListing.truncated + Approval T-011 provenance TS fields + Files truncation banner); dead-code (5 unused desktop wrappers removed; set_step_result removed — a dead fn that duplicated the gate and lacked the execution-attempt guard, a latent T-011 bypass). Plus a tauri-dev launch fix (vite server.watch.ignored ['**/src-tauri/**'] so the watcher can't crash on brops_lib.dll during a cargo rebuild). Then (f56a1b8) also DID the previously-deferred audit P1-2: consolidated the ~90-line copy-pasted governed conversation pipeline in stream_reply + reply_in_conversation into one shared run_governed_conversation_turn helper (byte-identical verify wiring, callers only deliver the ReceiptOutcome; stream_ask/stream_run_step left out as they use the held-answer verify path) — host 62 + core 235 green. All CI green, PR #53 CLEAN/MERGEABLE. Cardinal held: production trusted_verified never became fakeable; shipped 'Verified' stays fail-closed. Only genuinely owner-gated engine-gaps remain (win_executor placeholder output + verify_image not wired at runtime → need pinned model image + Windows session-0 containment). BEYOND the audit (9a67a91): shipped Phase-8 automation EXECUTION — a real 'Run now' (repo::automations::run + run_automation/list_automation_runs commands, migration 0020 automation_runs, SCHEMA_VERSION 20) that fires an automation's action via a small honest verb vocabulary: notify/task run LOCALLY and reversibly, an unknown verb is a recorded 'failed' run, a disabled automation refuses. The page's governance copy was corrected to the truth (local actions run directly; any model/engine-reaching action routes through the governed, fail-closed chain). No AI action fires ungoverned; production trusted_verified untouched. Roadmap Phase 8 moves from display-only toward execution. Then the local SCHEDULER (0442d89): a 60s background tick in setup() fires each enabled automation whose interval trigger (every: N{m|h|d}) is due, running its LOCAL action and logging it (repo::automations::run_due + parse_interval_ms; internal, no new command); only local non-AI actions fire unattended. Phase 8 now has real execution AND scheduling. Also (3cd1a10) surfaced the real automation run-history in the conduit detail, and (6df1a2f) added a first-run ONBOARDING overlay (Phase 10) — a localStorage-gated 3-step honest intro (local actions run directly; AI is governed/fail-closed; production Verified stated as not-yet-enabled, never faked)."
  },
  "next_action": "NO independent audit has passed. This field opened \"The independent CODE-audit is GREEN\" until 2026-08-09 -- which contradicted code_audit.gate (ARCHITECT_PENDING) in this same file, and was in any case about a WAIVER, not a pass: the Owner waived the external ChatGPT audit and let three converged BUILDER passes plus win-live/proof/WINDOWS_BROKER_AUDIT_VERDICT.md stand in for it. Two audits by someone who did not build this have since run, and the second returned RED (see purpose). Broker chain-wiring code is DONE + fail-closed: the broker crate has a production TurnResolver (broker/src/manifest_resolver.rs ProductionResolver) + a compiled-in TCB root (broker/src/tcb.rs), and main() build_governed_executor is config-driven -- $BROPS_BROKER_CONFIG with a TCB-root-signed manifest serves the real LinuxGovernedTurnChain, otherwise it falls back to UpstreamBlockedExecutor (Blocks every turn), so shipped behaviour is UNCHANGED until a trusted manifest is provisioned. REMAINING to actually expose production trusted_verified in the shipped app (all Owner-gated): (a) the shipped AI surfaces route through Path B (commands.rs stream_reply -> ai::governed_turn -> receipt_store NoTrustedManifest), NOT the broker binary -- either provision a real ReceiptKeyAuthority for Path B or route the surfaces through the broker; (b) carry the per-conversation request-envelope facts on the renderer->broker request (the resolver uses config-static facts today); (c) run the challenge-authority/supervisor/isolated-signer servers + provision a trusted manifest; (d) an INDEPENDENT audit of the whole chain that returns something other than RED; (e) OWNER approval to expose 'Verified'. Do NOT expose 'Verified' before (d) and (e).",
  "stop_gates": [
    "Shipped-desktop production trusted_verified stays fail-closed (build_governed_executor falls back to UpstreamBlockedExecutor because nothing sets $BROPS_BROKER_CONFIG, governed_verification_unconfigured() returns Some(...) unconditionally, and connect_broker() refuses off Linux) until the live-proven chain is wired into the desktop runtime AND an INDEPENDENT audit passes AND the Owner approves.",
    "The standing independent-audit verdict is RED. apps/desktop/AUDIT/2026-08-06-remediation-audit.md (main @ 219c763) confirmed 4 of 18 blockers closed and left 45 surviving findings, 1 P0. No independent audit has been run since, on that head or any later one. Closures claimed after it are the Builder's; the ledger marks them and they are not a pass.",
    "Design rev-30 is Architect DESIGN GREEN, but design-GREEN is NOT code-GREEN. The external Architect CODE-audit on PR #48 was never run: the Owner waived it (CURRENT_CODE_AUDIT: ARCHITECT_PENDING records that it did not run). A waiver is not a pass, and this file said \"the independent CODE-audit is GREEN\" in next_action until 2026-08-09.",
    "PR #48 is MERGED into main (with Phase-2 slices #49/#50/#51/#52), as is everything through PR #81. The remaining shipped-app gate is wiring the live-proven chain into the desktop runtime before production trusted_verified can render; do NOT expose 'Verified' before that + an independent audit + Owner approval.",
    "CI-green is NOT design/audit-green. The Windows LIVE kit (brops-win-live) machine-proves the Windows governed turn to trusted_verified over named pipes, but the SHIPPED app stays fail-closed. Do not cite platform_governed_execution_supported() as the flag holding that line: NO FUNCTION OF THAT NAME EXISTS in the tree (config/spec-conformance.json records section 0.1 as partial for exactly this reason, and this list named it as though it were real until 2026-08-09). The Windows broker remains a release blocker until the remaining hardening (broker as its own dedicated service account, CreateProcessAsUser + restricted token + STARTUPINFOEX, CNG key custody) lands AND a SEPARATE independent audit of the Windows broker passes. A proof kit is not the shipped gate."
  ],
  "current_workflow_pr": {
    "number": 122,
    "branch": "phase4-analytics-scrubber",
    "base": "main",
    "exact_candidate_source": "The exact head is the single 'AUDIT_CANDIDATE_HEAD: <40-hex>' marker in the PR #122 body.",
    "note": "Phase 4: the analytics page had no scrubber at all, and section D names one. Built as a full ARIA slider over the RANK cut-off rather than a timeline, because get_analytics returns one all-time aggregate with no time dimension and drawing an axis the engine does not expose is what this page already refuses to do in three other panels."
  },
  "next_action_by_carrier": {
    "_note": "The carrier is current_workflow_pr (#122 on phase4-analytics-scrubber). This block is rewritten by tools/sync_active_pr.py whenever the carrier moves: it modelled PR #48 as the open carrier for three days after #48 merged, because nothing updated it. No merge-transition is modeled (there is no carrier_transition block), so the branches below are advisory prose, not gate inputs.",
    "open": "merge #122 so main records this state; see next_action for what follows",
    "merged": "re-run tools/sync_active_pr.py --settled --pr <next> --branch <next> so the snapshot stops naming a carrier that has merged"
  },
  "keystone_next_2026_08_05": "Production trusted_verified model-image slice (P0-2/P0-3), owner-approved to resume in a fresh focused session. FINDING: the session-0 governed chain (win-live) runs ENTIRELY over HASHES (ResolvedTurn/win_live_turn/executor carry only system_sha256/history_sha256/generation_config_sha256, never raw prompt; win_executor emits a constant, executor build_output is a SHA-256 stand-in). Wiring a real model = a trust-chain extension: carry raw system/history in the request envelope + verify sha256==pinned hash at the boundary; plumb raw prompt request->broker->resolved->execution->contained executor stdin; win_executor runs an env-gated GENERIC local model command (owner's choice; mirror demo run_demonstration_model/BROPS_SELFTEST_MODEL_CMD) and emits the reply for content-addressing. ALL fail-closed; platform_governed_execution_supported() STAYS false, main() keeps UpstreamBlockedExecutor (win-live PROOF KIT, not shipped runtime). Trust-critical: design->implement->verify, never rush. Exposing production Verified still needs owner pinned MODEL + 3 servers running + INDEPENDENT audit + owner approval. Linux broker already config-driven drop-in; broker+desktop client LINUX-ONLY (SO_PEERCRED); Windows session-0 cross-account containment PROVEN (0xC0000142 was a debug-CRT DLL dep, fixed by release bins)."
}
