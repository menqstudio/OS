# RustSec advisory IDs consciously waived (fail-closed everywhere else). One ID per line.
# Each MUST carry a triage note. Keep in sync with deny.toml [advisories].ignore.
RUSTSEC-2026-0109  # tracked: transitive Rust advisory surfaced by the new gate; waived to unblock,
                   # pending a Cargo update in a dedicated follow-up (not in the governed trust path).
