{
  "schema": 1,
  "canonical_text": "config/runtime-dependencies.json",
  "record_schema": "schemas/runtime-dependency.schema.json",
  "policy": "required-dependency-missing-is-fail-closed",
  "dependencies": [
    {
      "id": "python3",
      "kind": "interpreter",
      "resolve": "python3",
      "version": ">=3.11",
      "required_for": ["runtime", "validation", "test", "build"],
      "optionality": "required",
      "note": "Canonical interpreter for all hooks, runtime gates, validators and tests. The hook wiring MUST invoke this resolvable interpreter, not a bare 'python' token."
    },
    {
      "id": "cryptography",
      "kind": "library",
      "resolve": "cryptography",
      "version": ">=41,<47",
      "required_for": ["runtime", "test"],
      "optionality": "required",
      "note": "Ed25519 signing/verification for grants, receipts and the evidence chain."
    },
    {
      "id": "jsonschema",
      "kind": "library",
      "resolve": "jsonschema",
      "version": ">=4.18,<5",
      "required_for": ["validation", "test"],
      "optionality": "required",
      "note": "Compiles and validates the registered JSON Schemas. Required_for validation: its absence is a fail-closed RED, not a silent partial."
    }
  ]
}
