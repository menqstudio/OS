{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "bridge.result",
  "description": "What the sidecar returns to the desktop. Fail-closed + VERIFIED-receipt-mandatory: `result` is non-null ONLY when `ok` is true and a `receipt` with `verified: true` is present; a failure never carries a result. `verified` is set by the adapter only after an injected verifier confirms the run's signed evidence.",
  "type": "object",
  "additionalProperties": false,
  "required": ["ok", "result", "receipt", "error"],
  "properties": {
    "ok": { "type": "boolean" },
    "result": {
      "type": ["string", "null"],
      "description": "The AI turn's output. Non-null iff ok=true."
    },
    "receipt": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "required": ["task_id", "status", "evidence", "verified"],
      "properties": {
        "task_id": { "type": "string" },
        "status": { "type": "string" },
        "exit_code": { "type": ["integer", "null"] },
        "evidence": { "type": "array", "items": { "type": "string" } },
        "verified": {
          "type": "boolean",
          "description": "True only after the injected verifier confirmed the run's signed evidence. `ok: true` REQUIRES `verified: true`; the adapter never returns a result otherwise."
        }
      },
      "description": "The governed-run receipt (from bro_supervisor.SupervisorResult), verified. Mandatory (with verified:true) alongside any result."
    },
    "error": { "type": ["string", "null"] }
  },
  "allOf": [
    {
      "comment": "ok:true REQUIRES a non-null string result AND a receipt object with verified:true.",
      "if": {
        "properties": { "ok": { "const": true } },
        "required": ["ok"]
      },
      "then": {
        "properties": {
          "result": { "type": "string" },
          "receipt": {
            "type": "object",
            "required": ["verified"],
            "properties": { "verified": { "const": true } }
          }
        },
        "required": ["result", "receipt"]
      }
    },
    {
      "comment": "ok:false NEVER carries a result: result must be null.",
      "if": {
        "properties": { "ok": { "const": false } },
        "required": ["ok"]
      },
      "then": {
        "properties": { "result": { "type": "null" } }
      }
    }
  ]
}
